# 新代 MACRO 语法规范手册 · 变量系统与运算子

> 本文件为 [新代 MACRO 语法规范手册](../新代MACRO语法规范手册.md) 的拆分子主题（变量系统与运算子）。
> 状态标记规则与维护流程见 [原手册索引 §0](../新代MACRO语法规范手册.md#0-状态标记)。

## 2. 变量系统

### 2.1 区域变量 `#`

状态：已确认

```iecst
#1 := 100;
#[10 + #3] := 200;
```

基本范围：

| 编号 | 说明 | 规则 | 型态 |
| --- | --- | --- | --- |
| `#0` | VACANT 空白特殊值 | 只读；表示未赋值或未传入 | - |
| `#1` ~ `#26` | 系统内定宏程序引数区域变量 | 读写；宏呼叫引数自动写入 | Double |
| `#27` ~ `#400` | 宏程序与副程序使用的一般区域变量 | 读写；用于暂存、计算、内部流程 | Double |

规则：

- 区域变量使用数字编号，可用固定编号 `#1` 或表达式索引 `#[表达式]`。
- `#1` ~ `#400` 共 400 个可用区域变量；`#0` 另作为 VACANT 常数。
- 区域变量初始状态为 VACANT，MACRO 执行完毕离开时自动恢复 VACANT。
- `#TEMP` 这类命名变量不支持，插件报 error。
- 推荐赋值运算符为 `:=`。
- 赋值使用 `=` 可识别但不推荐，插件 warning。

### 2.2 宏程序引数映射

状态：已确认

当通过扩充 G 码、`G65`、`G66`、`G66.1`、M 码宏程序等方式呼叫宏程序时，传入引数会自动映射到 `#1` ~ `#26`：

| 引数 | 区域变量 | 引数 | 区域变量 | 引数 | 区域变量 |
| --- | --- | --- | --- | --- | --- |
| `A` | `#1` | `J` | `#5` | `S` | `#19` |
| `B` | `#2` | `K` | `#6` | `T` | `#20` |
| `C` | `#3` | `L` | `#12` | `U` | `#21` |
| `D` | `#7` | `M` | `#13` | `V` | `#22` |
| `E` | `#8` | `N` | - | `W` | `#23` |
| `F` | `#9` | `O` | - | `X` | `#24` |
| `G` | - | `P` | `#16` | `Y` | `#25` |
| `H` | `#11` | `Q` | `#17` | `Z` | `#26` |
| `I` | `#4` | `R` | `#18` | | |

规则：

- `G`、`N`、`O` 为控制器语法关键字，无法作为宏程序引数，没有对应的 `#` 变量。
- `#10`、`#14`、`#15` 没有引数字母对应，可作为一般区域变量使用。
- `L` 为特殊引数，用于设定宏程序连续执行次数；未提供时系统自动补 `L=1`。
- 若同一指令中某地址已有固定语义，例如 `P` 作为程序号，应以该指令语义为准。

引数分类：

| 类型 | 包含引数 | 特点 |
| --- | --- | --- |
| 轴向引数 | `X` `Y` `Z` `A` `B` `C` `I` `J` `K` `U` `V` `W` | 只要其中一个被某宏程序占用，其他轴向引数均一起被占用 |
| 条件引数 | `F` `S` `T` `D` `E` `H` `P` `Q` `R` `M` `B` | 彼此独立，不会因任一被占用而连带 |
| 特殊引数 | `L` | 设定宏程序连续执行次数，未提供时系统自动补 `L=1` |
| 例外引数 | `G` `N` `O` | 控制器关键字，无法作为引数 |

扩充引数：

- `A`、`B`、`C`、`F`、`S`、`U`、`V`、`W`、`X`、`Y`、`Z` 可在字母后附带数字形成扩充引数，例如 `X1=50.`、`Z1=30.`。
- 扩充引数不直接映射到 `#1` ~ `#26`，需在宏程序内使用 `GETARG` 读取。
- `D`、`E`、`H`、`I`、`J`、`K`、`L`、`M`、`P`、`Q`、`R`、`T` 不可附带数字，例如 `D1=50.` 会报警。

```iecst
// 父程序
G200 X1=50. Y1=30.;

// G0200 宏程序内部
#27 := GETARG(X1);
#28 := GETARG(Y1);
```

### 2.3 区域变量生命周期

状态：已确认

区域变量的生命周期仅限该 MACRO 执行期间。执行完毕离开时，该区域变量自动恢复成 VACANT 状态。

| 呼叫方式 | 区域变量行为 | 生命周期结束时机 |
| --- | --- | --- |
| `M98 P_` 副程序呼叫 | 继承主程序的 `#1` ~ `#400`，共用同一块变量 | 主程序执行完毕时 |
| `M198 P_` 副程序呼叫，未被登录时 | 继承主程序的 `#1` ~ `#400` | 主程序执行完毕时 |
| `G65 P_` 非模态宏程序呼叫 | 创建独立的 `#1` ~ `#400`，引数写入 `#1` ~ `#26` | 宏程序 `M99` 返回时 |
| `G66 P_` 模态宏程序呼叫 | 创建独立的 `#1` ~ `#400`，从 `G66` 到 `G67` 期间共用 | 执行 `G67` 后回收清除 |
| `G66.1 P_` 每单节模态宏程序呼叫 | 与 `G66` 相同 | 执行 `G67` 后回收清除 |
| 扩充 G 码宏程序 | 每次呼叫都创建新的 `#1` ~ `#400` | 该 MACRO 结束时恢复上一层 |
| M 码宏程序 | 每次呼叫都创建新的 `#1` ~ `#400` | 该 MACRO 结束时恢复上一层 |
| T 码宏程序，`Pr3215=2` | 每次呼叫都创建新的 `#1` ~ `#400` | 该 MACRO 结束时恢复上一层 |
| T 码副程序，`Pr3215=1` | 继承主程序的 `#1` ~ `#400` | 主程序执行完毕时 |

示例：

```iecst
// M98 继承：O8000 可读写主程序 #1
%@MACRO
#1 := 100;
M98 P8000;
M30;

// G65 独立：O8000 内 #1 来自 A，引数不影响主程序 #1
%@MACRO
#1 := 100;
G65 P8000 A50.;
M30;
```

### 2.4 VACANT `#0`

状态：已确认

规则：

- `#0` 永远为 VACANT，只读。
- 区域变量初始状态均为 VACANT。
- VACANT 不等于 `0`，表示未定义或未传入。
- 可用 `#0` 判断引数是否被传入。

```iecst
IF #24 = #0 THEN
  // X 引数未被传入
ELSE
  // X 引数已被传入
END_IF;
```

### 2.5 公用变量 `@`

状态：已确认 / 部分版本差异

```iecst
@1 := 100;
@[#3] := 567;
```

基本范围：

| 编号 | 说明 | 规则 | 型态 | 断电记忆 |
| --- | --- | --- | --- | --- |
| `@0` | VACANT 空白特殊值 | 只读；表示未赋值 | - | - |
| `@1` ~ `@400` | 一般运算用变量 | 读写 | Double | 默认无；可通过 `Pr3811` 设定保留 |
| `@401` ~ `@655` | 对应 PLC 寄存器 `R1` ~ `R255` | 读写 | Long | 依 R 寄存器性质 |
| `@656` ~ `@999` | 有记忆性变量 | 读写 | Double | 有 |
| `@1000` | 旧版系统变量，请勿使用；新版改为有记忆性变量 | 旧版只读；新版读写 | Double | 版本相关 |
| `@1001` ~ `@1999` | 有记忆性变量 | 读写 | Double | 有 |
| `@10000` ~ `@14095` | 对应 PLC 寄存器 `R0` ~ `R4095` | 读写 | Long | 依 R 寄存器性质 |
| `@60000` ~ `@79999` | 扩充公用变量，需配合 `Pr3813`，仅适用 CE 系统 | 读写 | Double | 依配置 |
| `@100000` ~ `@165535` | 对应 PLC 寄存器 `R0` ~ `R65535` | 读写 | Long | 依 R 寄存器性质 |

生命周期：

- 公用变量使用数字编号，可用固定编号 `@1` 或表达式索引 `@[表达式]`。
- `@0` 永远为 VACANT，只读；VACANT 不等于 `0`，表示未定义。
- 公用变量范围为 `@0` ~ `@165535`，所有公用变量除非遇到关机，否则没有生命周期限制。
- 公用变量可跨 MACRO、跨轴群读写；系统复位后仍保留。
- `@656` ~ `@999`、`@1001` ~ `@1999` 原生具备断电记忆。
- `@1` ~ `@400` 默认无断电记忆，可通过 `Pr3811` 设定保留。
- `@60000` ~ `@79999` 为 CE 系统专用扩充区段，需配合 `Pr3813` 使用。
- `Pr3811`/`Pr3813` 的功能本体由 [Macro变数规格](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44106246/Macro) 证实，但其精确的参数条目格式、颗粒度与默认值在 CF 公开页面并未列出，需《控制器参数总表》PDF 或实机页面查表确认。
- `@TEMP` 这类命名变量不支持，插件报 error。

R 寄存器对应：

| 公用变量范围 | 对应 R 寄存器 | 型态 | 映射公式 |
| --- | --- | --- | --- |
| `@401` ~ `@655` | `R1` ~ `R255` | Long | R 号 = @ 号 - 400 |
| `@10000` ~ `@14095` | `R0` ~ `R4095` | Long | R 号 = @ 号 - 10000 |
| `@100000` ~ `@165535` | `R0` ~ `R65535` | Long | R 号 = @ 号 - 100000 |

R 寄存器完整区段语义（CF [PLC 介面说明](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44105796) 证实，用于区段选型与静态警告）：

| R 寄存器范围 | 语义 | 读写规则（人机/PLC/Macro） | 位元(Bit) 存取 | 永久保存 |
| --- | --- | --- | --- | --- |
| `R0` ~ `R39` | CNC 系统介面区 | 依 PLC 介面说明各 R 定义 | 是 | 否 |
| `R40` ~ `R49` | PLC 警报讯息区 | 依具体 R 定义 | 是 | 否 |
| `R50` ~ `R80` | 使用者自行定义 | 读/写 | 是 | 否 |
| `R81` ~ `R100` | 对应控制器参数 `Pr3401` ~ `Pr3420` | 唯读 | 是 | 是 |
| `R101` ~ `R102` | 刀具状态（存于 FRAM） | 唯读 | - | 是 |
| `R103` ~ `R511` | 使用者自行定义 | 读/写 | `R103` ~ `R255` 是、`R256` ~ `R511` 否 | `R103` ~ `R255` 是，`R256` ~ `R511` 否 |
| `R512` ~ `R639` | CNC 系统介面区 | 依 PLC 介面说明各 R 定义 | 否（不支持位元存取，唯读） | 否 |
| `R640` ~ `R1023` | CNC 系统介面区 | 依现状定义 | 否 | 否 |
| `R1024` ~ `R4095` | 使用者自行定义 | 读/写 | 否 | 否 |
| `R520` ~ `R529` (第一轴群) 与其他主系统轴群 | 轴群控制介面命令寄存器 | 依 PLC 介面说明各位元定义，多位元取值 | 是 | 否 |

R 寄存器「经 @ 映射后 Macro 可写」范围（CF [Macro 变数规格](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44106246/Macro) 证实）：

- `R50` ~ `R80`、`R101` ~ `R511`、`R1024` ~ `R4095`、`R5800` ~ `R7999`、`R10000` ~ `R10999`、`R15000` ~ `R65535`。
- 其中 `R101` ~ `R102` 主体在 PLC 介面侧为唯读 FRAM 刀具状态，在 Macro `/` `@` 侧记载为「可写」是因为「该区段从 Macro 变量侧可写」项允许覆盖写入，但写入会与 FRAM 刀具状态语义冲突。出于保险原则，插件后续不应把 `R101` ~ `R102` 列为「可写」推荐区段。语义上「可写」不等于「写入安全」。

区段选型：

| 需求 | 推荐 R 区段 |
| --- | --- |
| MACRO 临时记存 | `R1024` ~ `R4095`,或 `@1` ~ `@400`（不对应 R） |
| 需位元级存取的使用者自定义 | `R50` ~ `R80`、`R103` ~ `R255` |
| 需断电保存 | 对应到 `@1001` ~ `@1999` 原生断电区段，或检查 R 区段的「永久保存」列 |
| PLC/Macro 交换 | `R50` ~ `R80`、`R103` ~ `R511`、`R1024` ~ `R4095`、`R5800` ~ `R7999`、`R15000` ~ `R65535`这五段均兼具可写与位元存取 |
| 仅技术人员读 | `R81` ~ `R100` 对应 `Pr3401` ~ `Pr3420`、`R101` ~ `R102` 刀具状态 |

未列出的 R 寄存器区段，例如 `R0` ~ `R49`、`R81` ~ `R102`、`R512` ~ `R1023`、`R11000` ~ `R14999`、`R13001` ~ `R14095` 等，或未启用 Ben函数区段，写入可能导致不可预期行为。DOS 系统仅支援 `R0` ~ `R7999`；WinCE / Linux 系统支援完整 `R0` ~ `R65535`。

注：完整 R 资源表请直接查 [PLC 介面说明](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44105796/PLC) 的 Register Interface 节；本节只采编全局语义与可写区段，人机/PLC 特定使用场景的细项不在静态诊断范围内。

型态与赋值：

```iecst
@1 := 123;       // Long 型态
@1 := 123.0;     // Double 型态
@10 := "12";     // 字串写入时会先进行 ASCII 转码

#1 := 123;
@[#1] := 567;      // 等同 @123 := 567
@[#1 + 7] := 890;  // 等同 @130 := 890
```

规则：

- 不同型态变量做四则运算时，会强制转型为 Double 再计算。
- 两个 Long 做四则运算时，结果维持 Long。
- R 寄存器对应区段强制为 Long。
- `MOD` 仅适用于 Long 型态，对 Double 型态使用 `MOD` 会触发警报。
- 公用变量存入字符串时会进行 ASCII 转码，读取时需使用 `SCANTEXT` 函数还原字符串内容。

应用场景：

| 场景 | 建议区段 | 原因 |
| --- | --- | --- |
| MACRO 内部暂存运算 | `@1` ~ `@400` | 通用运算区，无断电记忆负担 |
| 跨 MACRO 资料传递 | `@1` ~ `@400` 或模态变量 `#2001` ~ `#3100` | 公用变量全局可见；模态变量复位消失更安全 |
| 与 PLC 交换数据 | `@401` ~ `@655`、`@10000` ~ `@14095`、`@100000` ~ `@165535` | 直接映射 R 寄存器 |
| 需要断电保存的工艺参数 | `@656` ~ `@999`、`@1001` ~ `@1999` | 原生断电记忆 |
| 关机后仍需保存的计数器或统计值 | `@656` ~ `@999` | 断电记忆且无需额外参数配置 |
| XML 数据读入 `DBLOAD` | 例如 `@1200` 等自定义区段 | 可配合 Schema 文件指定存放位置 |

### 2.6 变量类型对比

状态：已确认 / 部分待确认

| 特性 | 区域变量 `#` | 公用变量 `@` | 轴群使用者记忆变量 | 模态变量 | 客制参数 | AR / MAR |
| --- | --- | --- | --- | --- | --- | --- |
| 范围 | `#0` ~ `#400` | `@0` ~ `@165535` | `#401` ~ `#999` | `#1080` ~ `#3100` | `#4001` ~ `#5500` | 依 APP 配置 |
| 型态 | Double | Double / Long | Double | Long / Double | Double | 依 APP 配置 |
| 断电记忆 | 无 | `@656` ~ `@1999` 有；`@1` ~ `@400` 可配置 | 有，对应 L 值 | 无 | 有，对应参数 | MAR 断电记忆 |
| 系统复位后 | 消失 | 保留 | 保留 | `#1080` ~ `#1099` 断电消失；`#2001` ~ `#3100` 复位消失 | 保留 | 依 APP 配置 |
| 跨 MACRO | 不可，除非副程序继承 | 可 | 可 | 可 | 可，通常只读 | 可，仅 APP Macro |
| 与 PLC 交互 | 无 | R 寄存器映射 | 无 | 无 | 无 | 无 |
| 引数映射 | `#1` ~ `#26` 自动映射 | 无 | 无 | 无 | 无 | 无 |

### 2.7 变量使用建议

状态：已确认

- MACRO 内优先使用区域变量 `#1` ~ `#400`，除非有跨 MACRO 需求才使用 `@`。
- 执行 MACRO 副程序时，若模式 G 码会被改变，例如 `G90/G91`、`G40/G41/G42`，应在进入 MACRO 时备份当前状态，离开前恢复。
- 对长度或角度引数，运算前建议使用 `STD` 函数将单位标准化。
- 控制器核心会预解 MACRO 内容，若变量读写需与 G/M 码动作同步，应在读写前加入 `WAIT()`。
- 多个 MACRO 间交换数据时，优先使用模态变量，例如 `#2001` ~ `#2100`、`#3001` ~ `#3100`，系统复位时消失，可节省变量资源。
- MACRO 需要内定初始值时，可利用使用者参数，例如 `#4001` ~ `#4100`、`#5001` ~ `#5100`。

插件策略：

- 固定编号与 `#[表达式]` 均识别为区域变量。
- 固定编号与 `@[表达式]` 均识别为公用变量。
- 命名变量 `#TEMP`、`@TEMP` 报 error。
- `#0 := ...`、`@0 := ...` 后续可加入 warning，提示 VACANT 不建议写入。
- 区域变量固定编号超过 `#400` 时，后续可依变量区间提示用途或风险；对表达式索引不做静态范围诊断。
- 对静态可判定的 R 寄存器映射保留区写入，后续可加入 warning，提示可能导致不可预期行为。

### 2.7.1 系统变数 bit 级规格

状态：已确认 / 部分版本差异

来源：CF [Macro变数规格](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44106246/Macro)（v221, 2026-07-09）与 LTP [Macro变数规格 (LEANTEC)](https://syntecclub.atlassian.net/wiki/spaces/LTP/pages/64815277/Macro)（v221）。

控制系统变数（`#1500` ~ `#1820` 区段）以位元（bit）粒度承载运行时控制旗标，写入前必须确认版本门槛与 Pr 联动关系；以下表为静态可校验参考，实际位元定义以控制器版本 [Macro变数规格](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44106246/Macro) 页 §系统变数 為准。

| 变数 | 用途 | bit 级规格 | 版本门控 / Pr 联动 |
| --- | --- | --- | --- |
| `#1500` | 宁静模式（Quiet Mode） | bit0: 0=关闭；1=开启（系统仅记录程序座标，不进行动程规划） | 无 Pr 强制条件 |
| `#1502` | 单步执行控制（Macro Stepping） | bit0: 0=执行单步执行功能(预设, 系统复位自动回覆)；1=不执行单步执行功能 | `#1502` 仅于 `Pr3221=0` 时有效；`Pr3221` 其他设定下为 MACRO 语法除错仍会单步执行(bit0=1 也无效)。设为不执行时图形模拟步进也不支援 |
| `#1504` | Feedhold 与 Override 控制 | bit1: 0=Feedhold与Override有效(预设)；1=暂停无效且百分比固定100%。bit2: 0=Feedhold有效(预设)；1=Feedhold无效。bit3: 0=快动Override有效；1=快动百分比固定100%。bit4: 0=切削Override有效；1=切削百分比固定100%。bit5: 0=主轴Override有效；1=主轴百分比固定100% | `#1504.2~#1504.5` 于 `10.114.51` 之后始有效。bit1/.2 暂停无效时加工中 Reset 也失效；bit1/.3~.5 百分比固定100%时对应手轮模拟失效 |
| `#1510` | FileOperationControlWord（档桉控制字） | bit0: 0=关闭主程序Reload；1=启动主程序Reload。bit1: 0=关闭副程序Reload；1=启动副程序Reload。bit2: 0=更新主+副程序信息(档名/行号/序号)；1=只更新主程序信息。其他保留 | 与 `#1517` 系统变数联动；加工中修改需等处理 `#1510` 前产生的预解指令执行完才有效 |
| `#1820` | 静音插补模式（Silent Interpolation Mode） | 值 0 关闭、值 1 仅 G01 静音插补、值 2 全模式静音插补 | `10.120.24E` / `10.118.28A` / `10.118.31+` 及后续版本可用 |
| `#1901` ~ `#1918` | G92 / G92.1 座标系偏移量（各轴向） | r/w，对应 1~4 轴群；记载各轴 `G92`/`G92.1` 座标偏移 | 见 [程序座标设定 G92/G92.1](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44135263/G92+G92.1) |
| `#1930` | G92.1 座标系旋转角度 | r/w，对应 1~4 轴群；跟 `#1931~#1933` 旋转中心一起决定 `G92.1` 的旋转座标变换 | 同上 |
| `#1931` ~ `#1933` | G92.1 座标系旋转中心轴向 | r/w，对应 1~4 轴群；标记 `G92.1` 动作中止旋转中心所在轴 | 同上 |

注：`#1901~#1918` 的分类在 [Macro变数规格](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44106246/Macro) 中属《轴群使用者记忆变数》子段；与 `#401~#999` 主体区别在于该子段直接绑定 `G92`/`G92.1` 指令的偏移状态，不应被普通区域运算逻辑覆写。
使用注意：

- 在 LTP 机身机器人风险性高的场景下，若 `#1504 bit2`（Feedhold 无效）或 `#1504 bit1`（Feedhold+Override 总开关无效）被置位，呼叫 `G10 L18xx` 系列 MACRO IO 时 MOVJ/MOVL 不可响应即时停止；写入前应搭配 `R615` MST 请求旗标（`R615` 与 `R615.8~.11` 多轴群旗标）做跨轴群一致性检查。
- `#1510` 写入应 `WAIT()` 至少一个单节后才读回 `#1517` 的状态，否则核心执行表尚未同步；在 `#1500=1` 宁静模式下，`#1510` 仍按档桉控制字生效。
- `#1820` 在低于 `10.118.31` 的旧版本写入值 1 / 2 会被忽略，并不报警；静态诊断可向用户提示版本门控。
- 这些 bit 级变数仅適用控制器核心状态操作，不应使用 `MOD` （`#1500` ~ `#1820` 为 Double 型态，Long/Double 混型的 `MOD` 会触发 `COR-054`）。

示例（宁静模式 + 单步调试）：

```iecst
#1500 := 1;   // 启用宁静模式，隐藏 G/M 码输出
#1502 := 1;   // bit0=1 不执行单步（仅 Pr3221=0 时有效）
#1504 := 4;  // bit2=1 Feedhold 无效（暂停进给）
// 业务逻辑...
#1504 := 0;  // 清除 Feedhold 后继续
#1500 := 0;  // 关闭宁静模式
```

插件策略：

- 固定编号 `#1500` / `#1502` / `#1504` / `#1510` / `#1820` 识别为系统控制变数，后续可加入对应推荐代码片段。
- 写入 `#1820` 之前，插件静态展示可附带 hint，提示版本门控（`10.118.31+`）。
- 不对 bit 赋值的十进制值（例如 `#1504 := 4` 这类 bit2 mask）做大于范围诊断；插件只提示语义不要胞定。

### 2.8 常见变量警报

状态：已确认 / 部分待确认

| 场景 | 警报 |
| --- | --- |
| 存取超出开放范围或当前应用不允许的变量 | `COR-016` 不合法的变数存取 |
| ISO 格式档中使用 MACRO 语法存取 `#` / `@` 变量 | `COM-003` 句法错误 |
| 非 APP Macro 存取 AR / MAR | `COR-016` |
| 不同型态，Long / Double 做 `MOD` 运算 | 报警；`MOD` 仅适用 Long |
| Long 与 Double 型态做 `MOD` | `COR-054` 资料型别不相容 |
| 存取超出范围的公用变量 | `COR-016` 不合法的变数存取 |
| 写入 R 寄存器系统保留区段 | 可能导致不可预期行为，未必有警报保护 |
| 副程序超过 60KB 且包含巢状语法，10.120.32 之前 | `COR-204` 档案太大 |

### 2.9 AR / MAR

状态：已确认 / 部分版本差异

```iecst
AR1 := 789;
MAR1 := 789;
AR[#3] := 789;
MAR[#3] := 789;
```

基本概念：

AR 和 MAR 是 APP 架构下的专用变量，用于避免传统公用变量 `@` / PLC `R` 在多个 APP 或客制功能之间产生变量冲突。每个 APP 拥有独立的 AR/MAR 空间，彼此互不干扰。

| 项目 | AR | MAR |
| --- | --- | --- |
| 全称 | APP 区域变量 | APP 永久变量 |
| 断电记忆 | 无，关机后消失 | 有，永久储存 |
| 储存位置 | 内存 | `DiskC\app\%AppName%\usr\MAR.dat` |
| 型态 | Double；后续版本支援字符串 | Double |
| 编号方式 | 0-based，例如 `AR0`、`AR1` | 0-based，例如 `MAR0`、`MAR1` |

数量规格：

- 数量由每个 APP 的 `app.acfg` 配置文件定义。
- AR 默认上限 128 个，扩充后最多 2048 个。
- MAR 默认上限 1024 个，扩充后最多 2048 个。
- 新版本支援 `InitValue` 属性，例如 `<AR InitValue="Vacant">48</AR>`、`<MAR InitValue="0">512</MAR>`。

```xml
<?xml version="1.0" encoding="utf-8"?>
<Configuration Ver="1">
  <Load>TRUE</Load>
  <AR InitValue="Vacant">48</AR>
  <MAR InitValue="0">512</MAR>
  <APPVer>21213adad</APPVer>
  <CNCVer>10.118.12E</CNCVer>
</Configuration>
```

存取限制：

- AR/MAR 仅能在 `DiskC\app\%AppName%\bin\Macro\` 路径下的 APP Macro 文件中存取。
- 在其他路径的加工程序中存取 AR/MAR，会触发 `COR-016` 不合法的变数存取。
- AR/MAR 仅支援 MACRO 格式，第一行须有 `%@MACRO`；ISO 格式档中使用 AR/MAR 语法会触发 `COM-003` 句法错误。
- 每个 APP 的 AR/MAR 空间完全独立，APP A 的 Macro 无法直接存取 APP B 的 AR/MAR。
- 主程序无法直接存取任何 APP 的 AR/MAR，需通过呼叫 APP Macro 间接存取。

语法规格：

```iecst
%@MACRO
AR0 := 100;
AR46 := 72;
MAR53 := 10;
MAR0 := 3.14;

#1 := 10;
AR[#1] := 500;       // 等同 AR10 := 500
MAR[#1 + 5] := 200;  // 等同 MAR15 := 200

#27 := AR0;
#28 := MAR53;
@1 := AR5;
```

字符串规则：

- AR 后续版本支援储存字符串，每个变量可存 4 bytes。
- 写入较长字符串时会依 4 bytes 分段写入连续 AR 变量。
- 在 APP Macro 中，`SCANTEXT` 读取 AR 变量字符串；在一般 MACRO 中，`SCANTEXT` 读取公用变量 `@` 字符串。
- MAR 不支援储存字符串；对 MAR 赋值字符串时，程序内部会挡下，该变量值不受影响。

```iecst
AR100 := "ABCDEFGH";
// "ABCD" 存到 AR100
// "EFGH" 存到 AR101
// "\0"   存到 AR102

#30 := SCANTEXT(100);
```

生命周期：

| 事件 | AR | MAR |
| --- | --- | --- |
| 开机 | 初始为 VACANT 或 0，依 `InitValue` 设定 | 从 `MAR.dat` 载入上次储存值 |
| 系统复位 | 保留，不清除 | 保留 |
| 关机 | 消失 | 自动储存到 `MAR.dat` |
| 加工中断电 | 消失 | 保留；`OP-023` 不支援检查 MAR 变量 |

APP 文件结构：

```text
DiskC\app\%AppName%\
  bin\
    app.acfg
    Macro\
      G0400
      G0500
    res\
      Schema\
      Screen\
  usr\
    MAR.dat
    CycData\
```

外部系统交互：

- APP Plugin 可通过 API 读写单个 AR/MAR、批量读写 AR/MAR，并取得 AR/MAR 容量。
- ORX 可直接读写 AR/MAR，与 APP Macro 交握。
- XMLDB Schema 支援 `AR1`、`MAR1` 等作为 `Storage` 来源。
- APP Macro 的 Modbus 语法支援将读取结果存入 AR/MAR。
- AR/MAR 可上传到云端平台，变量类别可区分为 `AR_%AppName%` 或 `MAR_%AppName%`。
- DS Logger 支援 AR 比较事件；事件引数仅支援整数型态，浮点数 AR 会四舍五入转型为整数处理。
- 人机画面 Display、VisibleRule、装置取值群组、图表元件可支援 AR/MAR 作为数据来源。

常见应用：

| 应用 | AR 用途 | MAR 用途 |
| --- | --- | --- |
| 智慧进给 AFC | 操作状态交握、错误处理、示波器控制 | 多组参数、模式选择 |
| 品质追溯 | 画面选项、ACTION 控制 | 功能上云设定 |
| 暖机健检 | 检测结果暂存 | 历程数据永久储存 |
| 断刀回退 | 攻牙位置、转速、进给率等运动参数备份 | - |

常见警报：

| 场景 | 警报 |
| --- | --- |
| 非 APP Macro 路径中存取 AR/MAR | `COR-016` 不合法的变数存取 |
| 存取超出 `app.acfg` 定义范围的变量号码 | `COR-016` 不合法的变数存取 |
| 变量号码不是整数，例如 `MAR1.1` | 直接定址 `COM-003`；间接定址 `COR-016` |
| 变量号码为负数，例如 `MAR-1` | 直接定址 `COM-003`；间接定址 `COR-016` |
| ISO 格式档中使用 AR/MAR 语法 | `COM-003` 句法错误 |

插件策略：

- AR/MAR 为 APP 专用变量。
- 固定编号与表达式索引均识别为 APP 变量，例如 `AR1`、`MAR1`、`AR[#3]`、`MAR[#3]`。
- 静态可判定的负数或小数直接编号可报 error。
- 非 APP Macro 路径识别需结合工作区路径，后续可加入路径级 warning/error。
- 未读取 `app.acfg` 前，不应对 AR/MAR 最大编号做强诊断。

### 2.10 PLC ↔ 系统变数映射

状态：已确认 / 部分版本差异

来源：CF [PLC介面说明](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44105796/PLC)（v771, 2026-08-19）。该页 §C Bit Interface / §S Bit Interface / §R Register Interface 节列出 CNC-PLC 接口的全表；本节只收录对静态诊断有价值的映射子集。

#### 2.10.1 C/S Bit Interface（PLC-CNC 交互旗标）

PLC 通过 C Bit 向 CNC 採集状态、通过 S Bit 向 PLC 输出状态。以下仅列出与 Macro 变量 / 多轴群场景强相关的旗标示意；完整 bit 表需查 [PLC介面说明](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44105796/PLC) §C Bit Interface 与 §S Bit Interface 节。

| PLC/CNC 旗标 | 方向 | 语义 | Macro 使用场景 |
| --- | --- | --- | --- |
| `C101 ~ C132` | PLC → CNC | 对应 `#6001 ~ #6032` 系统变数 | Macro 可通过 `#6xxx` 读到 PLC 请求状态 |
| `R514 ~ R518` | CNC → PLC | 现存警报（由人机写入） | Macro 不应覆盖写，读取仅供诊断 |
| `R518` | CNC → PLC | 手动坐标 | Macro 与 `#1901 ~ #1918` 坐标变数联动使用 |
| `R530` | Macro/PLC 交握 | 上升沿触发外部事件（如轴群切换、宏调用） | 写入后须 `WAIT()` 等待原点 / 锁定完成 |
| `R629` | CNC → PLC | PLC 轴 M 码（类比 `S29`） | PLC 轴动作联动 Macro 代码执行 |
| `R633 ~ R636` | CNC → PLC | 参考点到达旗标 | Macro 回零 / 定点使用 `#1900~#1918` 读机械坐标 |
| `R633.x` | CNC → PLC | 轴向位移禁止旗标 | 机器人风险性高的场景需诊断 bit 赋值 |
| `R615` | CNC → PLC | MST 请求旗标（Master Spindle / Tool / M-Code） | 多轴群场景下读取 `R615.8 ~ R615.11` 的辅助轴群请求位 |
| `R16140` / `R16150` | CNC → PLC | 软面板倍率（SOFTKEY 与 Override） | Macro 调用 `G10 L18xx` 后联动面板显示倍率 |
| `R661 ~ R676` | CNC → PLC | G00 / G01 进给倍率 | PLC 轴 `G00` / `G01` 联动 Macro 代码 |

与 Pr 联动：

- `Pr732` 与 M30 计数联动：M30 为主程序结束，详细计数规则由 PLC 介面不同版本实现，需查 [控制器参数总表](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44105816) 与 [PLC介面说明](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44105796/PLC)。
- `Pr701 ~ Pr720` 轴群归属、`Pr721 ~ Pr730` MST 通道、`Pr731` 主系统轴群数，详细设定参考 [新代 81RA 多轴群设置步骤](https://syntecclub.atlassian.net/wiki/spaces/SZJS/pages/834429119/81RA)。

#### 2.10.2 系统变数 #6001~#6032 ↔ C101~C132 推荐使用技巧

```iecst
%@MACRO
#1 := #6025;   // 读取 PLC C125 请求状态
IF #1 = 1 THEN
  #1500 := 1;  // 请求启动宁静模式
  #1504 := 4;  // bit2=1 Feedhold
  WAIT();
  // ...
END_IF;
```

注意：

- `#6xxx` 系统变数只应读，不应写；写入会与 PLC 决策冲突。
- 不应在 `#1500=1` 宁静模式下跳过 `R615 MST 请求旗标`，可能导致 MST 请求丢失。

#### 2.10.3 G10 L1900/L1901/L1910/L1911 Modbus 实践映射

来源 [新代控制器 Modbus 通讯SOP（Macro语法与PLC实现）](https://syntecclub.atlassian.net/wiki/spaces/~C1720/pages/483251200/Modbus+SOP+Macro+PLC)，微量保留在此节依据。R 寄存器 32 位以 16 位拆分：

- Modbus RTU / TCP 读写结果以 32-bit 整数存入 1 个 R 变量。
- 高 / 低 16 位拆分：`R2` 为低位、`R1` 为高位。一般公式 `Rn 高位 = R(n*2)`，`Rn 低位 = R(n*2 + 1)`。
- 软位元件 `M` → Coil 区段直接处理。

```iecst
// G10 L1900 写入 Modbus，从 R1000 开始存
G10 L1900 R1000 ;
// L1901 以 32-bit 读取、拆位 Loop
#1 := @14000;  // = R1000 在 @ 映射后 high 16 bit
#2 := @14001;  //  = R1001 low 16 bit
```

接线（DB9）：7=+ / 6=-。适用新代全系列。

#### 2.10.4 插件策略

- 固定编号 `#6001` ~ `#6032` 识别为 PLC 读取变数，后续可加 hint 提示「只读」。
- 固定编号 `@10000 ~ @10031`（对应 `R0 ~ R31`）为 CNC 系统介面区，插件可加 warning 提示使用区段系统保留、写入高风险。
- 不对 `R615.x` 等位元级 R 表达做静态诊断，避免错误负阳性。

#### 2.10.5 M30 与 R615 / Pr732 计数联动（partial 待确认）

状态：部分待确认

来源：CF [PLC介面说明 §R615](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44105796/PLC)（v771）已证实「M30 → R615 MST 请求旗标」路径；[控制器参数总表 §Pr732](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44105816) 侧面提到与 M30 计数联动，但详细位元拆位公式未公开。

已证实部分：

- `M30` 作为主程序末节指令，同时承担：生成 PLC 主轴 / M 码完成旗标（`R615` 上升沿）+ 触发多轴群辅助旗标位（`R615.8 ~ R615.11`，见 §2.10.1。
- 多轴群场景下，`Pr701 ~ Pr720` 轴群归属 + `Pr731` 主系统轴群数配置 + `R615` 联动决定哪个轴群的 MST 计数被更新。

待 PLC 实机二次确认部分：

- CF 公开页面未列出 `Pr732` 与 M30 计数的详细位元拆位公式、各轴群选择性计数动作（PLC 实作差异）。
- 模态变量 `#1108` 等计数累积的具体绑定需实机验证。

示例：

```iecst
%@MACRO
// 主程序末节，触发 M30 → R615 MST 完成旗标 → PLC 计数
M30;
// 此段在父程序返回后执行，运行时应在 PLC 侧监听 R615 上升沿
WAIT();
#1 := @10615;  // 读 R615 状态，看 MST 请求是否完成
IF #1 = 1 THEN
    // ...
END_IF;
```

插件策略：不对此 partial 项做强诊断；提供 hover + snippet 提示「M30 可联动 `Pr732` / `R615` 上报计数，需实机二次确认」。

## 3. 运算子

### 3.1 运算子优先级

状态：已确认

数字越小，优先级越高。同一优先级的运算子从左到右依序执行。

| 优先级 | 运算子 | 符号 | 类别 |
| --- | --- | --- | --- |
| 1 | 括号 | `( )` `[ ]` | 分组 / 间接定址 |
| 2 | 函数赋值 | `Identifier(引数)` | 函数呼叫 |
| 3 | 负号 | `-` | 一元运算 |
| 3 | 补数 | `NOT` | 一元运算 |
| 4 | 乘号 | `*` | 算术运算 |
| 4 | 除号 | `/` | 算术运算 |
| 4 | 模数 | `MOD` | 算术运算 |
| 5 | 加号 | `+` | 算术运算 |
| 5 | 减号 | `-` | 算术运算 |
| 6 | 大小比较 | `<` `>` `<=` `>=` | 关系运算 |
| 7 | 等于 | `=` | 关系运算 |
| 8 | 不等于 | `<>` | 关系运算 |
| 9 | 布林且 | `&` `AND` | 逻辑运算 |
| 10 | 布林互斥 | `XOR` | 逻辑运算 |
| 11 | 布林或 | `OR` | 逻辑运算 |

### 3.2 赋值运算子

状态：已确认

| 符号 | 说明 | 范例 |
| --- | --- | --- |
| `:=` | 变量赋值 | `#1 := 100;`、`@10 := 3.14;`、`AR0 := 50;`、`MAR1 := 200;` |

规则：

- MACRO 使用 `:=` 进行赋值。
- 单独的 `=` 是等于比较运算子，不是正式赋值运算子。
- 赋值使用 `=` 可识别但不推荐，插件 warning。

### 3.3 算术运算子

状态：已确认

| 符号 | 说明 | 范例 | 结果 |
| --- | --- | --- | --- |
| `+` | 加法 | `#1 := 10 + 20;` | `30` |
| `-` | 减法 | `#1 := 100 - 30;` | `70` |
| `*` | 乘法 | `#1 := 5 * 6;` | `30` |
| `/` | 除法 | `#1 := 10.0 / 3.0;` | `3.333...` |
| `MOD` | 取余数 | `#1 := 10 MOD 3;` | `1` |

一元负号：

```iecst
#1 := 10;
#2 := -#1;
#3 := -100;
```

### 3.4 除法

状态：已确认 / 部分待确认

规则：

- 使用 `/` 时，若分子与分母都是整数，结果仍为整数。
- 整数与非整数的差别在于有无小数点。
- 涉及除法时，建议至少在分子或分母加小数点，避免意外截断。

```iecst
#1 := 1. / 2;       // 结果 0.5
#2 := 1 / 2.0;      // 结果 0.5
#3 := 1 / 2;        // 结果 0，整数截断
#4 := (1/2) * 1.0;  // 先算 1/2=0，再乘 1.0，结果 0.0
```

不支持：

```iecst
#1 := 100 DIV 7;
```

插件策略：

- `DIV` 报 error。
- hover 说明 `/` 的整数除法规则。

待确认：

- 负数整数除法取整方向。
- 除以 0 的实际警报或行为。
- Long 纯运算溢出行为。

### 3.5 MOD

状态：已确认 / 待落地

规则：

- `MOD` 仅适用于 Long 型态。
- 任一操作数为 Double 型态时会触发 `COR-054` 资料型别不相容。
- 静态数值带小数点视为 Double。

```iecst
#1 := 10 MOD 3;    // 合法，结果 1
#2 := 10. MOD 3;   // 不合法，Double 不可使用 MOD
#3 := 10 MOD 3.0;  // 不合法，Double 不可使用 MOD
```

插件策略：

- 静态数字操作数带小数点时 warning/error。
- 变量参与时不静态判断。

### 3.6 关系运算子

状态：已确认

| 符号 | 说明 | 范例 |
| --- | --- | --- |
| `=` | 等于 | `IF #1 = 10 THEN` |
| `<>` | 不等于 | `IF #1 <> 0 THEN` |
| `<` | 小于 | `IF #1 < 100 THEN` |
| `>` | 大于 | `IF #1 > 0 THEN` |
| `<=` | 小于等于 | `IF #1 <= 50 THEN` |
| `>=` | 大于等于 | `IF #1 >= 1 THEN` |

不支持：

```iecst
#1 == 100;
```

VACANT 比较：

```iecst
IF #1 = #0 THEN
  // #1 未赋值
END_IF;

IF @100 = @0 THEN
  // @100 未赋值
END_IF;
```

规则：

- VACANT 不等于 `0`。
- VACANT 表示未定义，`0` 是确定数值。

插件策略：

- `==` 报 error，提示使用 `=`。

### 3.7 逻辑运算子

状态：已确认

| 符号 | 说明 | 范例 |
| --- | --- | --- |
| `&` `AND` | 逻辑且 | `IF (#1 > 0) & (#2 < 100) THEN` |
| `OR` | 逻辑或 | `IF (#1 = 1) OR (#1 = 2) THEN` |
| `XOR` | 逻辑互斥或 | `IF #1 XOR #2 THEN` |

规则：

- `&` 等同 `AND`。
- `NOT` 是一元数值补数，优先级见 3.1，不作为布林逻辑非推荐写法。
- 符号型运算符也应提供 hover。

取反推荐写法：

| 场景 | 推荐写法 | 范例 |
| --- | --- | --- |
| 布林 0/1 翻转 | `1 - #变量` | `#100 := 1 - #100;` |
| 条件式布林取反 | 使用相反条件或与真值互斥 | `IF (#100 = 0) XOR (1 = 1) THEN` |
| 单 bit 翻转 | `XOR 1` 或对应 bit mask | `#100 := #100 XOR 1;` |
| 16 位整字翻转 | `XOR 0xFFFF`，mask 依实际字宽调整 | `#100 := #100 XOR 0xFFFF;` |
| 数值补数 | `NOT 数值`，结果为 `-(x + 1)` | `#2 := NOT #1;` |

```iecst
IF (#14 <= #12) AND (#15 <= #13) THEN
  #1 := 1;
END_IF;

REPEAT
  #14 := #14 + 1;
UNTIL (#14 > #12) OR (#15 > #13) END_REPEAT;

IF ((#14 = 4) & (#16 = 1)) THEN
  EXIT;
END_IF;
```

### 3.8 括号运算子

状态：已确认

| 符号 | 用途 | 范例 |
| --- | --- | --- |
| `( )` | 运算分组 / 函数引数 | `#1 := (10 + 20) * 3;`、`SIN(45.0)` |
| `[ ]` | 间接变量定址 | `@[#1] := 500;`、`AR[#1 + 5] := 200;` |

G 码引数中进行运算时，括号可省略：

```iecst
#1 := 3.0;
G01 X(1.0 + 3.0);
G01 X30.0 / 2.0;
G01 X(4.0 * #1);
```

### 3.9 型态转换规则

状态：已确认

| 运算情况 | 结果型态 | 范例 |
| --- | --- | --- |
| Long 运算 Long | Long | `@1 := 2 * 100;` |
| Long 运算 Double | Double | `@1 := 1 + 2.0;` |
| Double 运算 Double | Double | `@1 := 1.5 + 2.5;` |
| Long `MOD` Long | Long | `@1 := 10 MOD 3;` |
| 含 Double 的 `MOD` | 警报 | `@1 := 10. MOD 3;` |

### 3.10 运算子常见应用

状态：已确认

```iecst
IF #1 = 1 THEN
  #2 := 1;
ELSEIF (#1 >= 2) AND (#1 <= 5) THEN
  #2 := 2;
END_IF;

WHILE (#14 <= #12) AND (#15 <= #13) DO
  #14 := #14 + 1;
END_WHILE;

FOR #6 := 0 TO 3 BY 1.0 DO
  #7 := #7 + #6;
END_FOR;

CASE #1 OF
1:
  X(1.0 * #1) Y(1.0 * #1);
2:
  X(2.0 * #1) Y(2.0 * #1);
3, 4, 5:
  X(3.0 * #1) Y(3.0 * #1);
ELSE
  X(4.0 * #1) Y(4.0 * #1);
END_CASE;
```

### 3.11 常见错误与警报

状态：已确认 / 部分待确认

| 错误场景 | 警报 |
| --- | --- |
| Double 型态使用 `MOD` | `COR-054` 资料型别不相容 |
| 运算式过于复杂或嵌套过深 | `COM-005` 数学式太复杂 |
| ISO 格式中使用 MACRO 运算子 | `COM-003` 句法错误 |
| 巢状语法超过 10 层 | `COM-007` 重覆回圈太深 |
| 缺少结尾符号，例如 `END_IF` / `END_FOR` | `COM-014` / `COM-016` / `COM-018` / `COM-020` / `COM-021` 等 |

### 3.12 实用建议

状态：已确认

- 除法建议写成 `1.0 / 2`，避免 `1 / 2` 整数截断。
- 使用 `MOD` 前确认两侧均为 Long 型态，即静态数值不带小数点。
- VACANT 判断使用 `#0` / `@0`，例如 `IF #1 = #0 THEN`。
- 复合条件建议加括号，例如 `IF ((#1 > 0) & (#2 < 100)) THEN`。
- 赋值用 `:=`，比较用 `=`，两者不可混淆。

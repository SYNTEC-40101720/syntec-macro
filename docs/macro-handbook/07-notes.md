> 本文件为 [新代 MACRO 语法规范手册](../新代MACRO语法规范手册.md) 的拆分子主题（撰写注意事项与待确认项）。
> 状态标记规则与维护流程见 [原手册索引 §0](../新代MACRO语法规范手册.md#0-状态标记) 与 [macro-knowledge/README](../macro-knowledge/README.md#文档分层与职责边界)。

## 10. MACRO 撰写注意事项

### 10.1 档案格式与命名

状态：已确认

| 项目 | 正确做法 | 风险 |
| --- | --- | --- |
| 格式宣告 | 第一行使用 `%@MACRO` | 否则视为 ISO 格式，MACRO 语法触发 `COM-003` |
| 扩充 G 码 | 使用无后缀 `G0200`、`G200001` | 加后缀会导致控制器无法按扩充 G 码规则查找 |
| O 码副程序 | 使用无后缀 `O8000`、`O0010` | 加后缀会导致副程序查找不一致 |
| 一般加工档 | 使用 `.nc`、`.cnc`、`.tap` 等加工档后缀 | 后缀不决定 MACRO 格式，仍需看首行 |
| 系统 MACRO | 放在 `DiskC:\OpenCNC\MACRO` | 人机中不可编辑 |
| APP Macro | 放在 `DiskC:\app\%AppName%\bin\Macro\` | 只有该路径可存取 AR/MAR |

### 10.2 预解与 WAIT

状态：已确认

核心会预解 MACRO 内容，变量运算速度会超前于 G/M 码实际运动执行。读取运动后的座标、状态或系统变量前，应使用 `WAIT()` 同步。

```iecst
%@MACRO
G01 X100. F500;
WAIT();
#1 := #1321;
G01 X(#1 + 50.);
M30;
```

规则：

- `WAIT()` 可等待前方 G/M 码执行完成后再继续解译。
- `M98`、`M99`、`M198` 不受 `WAIT()` 影响。
- 运动后立即读取机械座标或状态时，省略 `WAIT()` 容易读到旧值。

### 10.3 模态备份与还原

状态：已确认

MACRO 内部若改变 `G90/G91`、`G40/G41/G42` 等模态 G 码，离开 MACRO 后不会自动还原，会影响父程序后续执行。

常用模态系统变量：

| 系统变量 | 对应模态 | 说明 |
| --- | --- | --- |
| `#1000` | 插值模式 | `G00/G01/G02/G03` |
| `#1003` | 绝对/增量 | `G90/G91` |
| `#1004` | 绝对/增量 | `G90/G91` |
| `#1015` | 刀具补偿 | `G40/G41/G42` |
| `#1016` | 刀长补偿 | `G43/G44/G49` |

```iecst
%@MACRO
#10 := #1004;
#11 := #1015;

G91 G00 Y10.;
G41 D1;

G40;
G#10;
M99;
```

G 码宏程序返回前还原状态示例：

```iecst
// Main
G90;
G01 X10. F100.;  // X=10
G200;            // 呼叫 G0200，X 增量移动 10，执行后 X=20
X-20.;           // G0200 已还原 G90/G01，因此此行按主程序预期执行
M30;

// G0200
%@MACRO
#101 := #1000;   // 备份插值模式 G00/G01/G02/G03/G33/G34/G35
#102 := #1004;   // 备份 G90/G91
G91 G00 X10.;
G#101;           // 还原插值模式
G#102;           // 还原 G90/G91
M99;
```

### 10.4 座标系统禁忌

状态：已确认

| 禁止或慎用项目 | 原因 | 建议做法 |
| --- | --- | --- |
| `G92` | 图形模拟功能可能失效 | 改用 `G91` 相对移动 |
| `G54` ~ `G59` | 影响父程序工件座标系 | 进入前备份，离开前恢复；能避免则避免 |
| `G52` | 影响父程序局部座标系 | 改用增量移动或显式还原 |

### 10.5 引数处理

状态：已确认

规则：

- 长度或角度引数运算前建议使用 `STD` 标准化。
- 未传入的引数为 VACANT，应以 `#0` 判断并设定默认值。
- 轴向引数 `X/Y/Z/A/B/C/I/J/K/U/V/W` 只要一个被某宏程序占用，其余轴向引数会一起被占用。
- `L` 未指定时系统自动补 `L=1`；`L=0` 时宏程序不执行；`L=3` 时连续执行 3 次。
- 同一行不建议同时写会争用引数的 G 码宏程序和 M 码宏程序。

```iecst
IF #24 = #0 THEN
  #24 := 0.;
END_IF;

IF #9 = #0 THEN
  #9 := 1000.;
END_IF;

#27 := STD(#24);
#28 := STD(#25);
```

引数让宏程序通用化示例：

```iecst
// Main
G90 G00 X0. Y0.;
G200;       // 固定切削 10 单位正方形
G201;       // 固定切削 20 单位正方形
G202 X30.;  // 使用 X 引数指定任意边长
M30;

// G0200
%@MACRO
#101 := #1000;
#102 := #1004;
G91 G01 X10.;
G91 G01 Y10.;
G91 G01 X-10.;
G91 G01 Y-10.;
G#101;
G#102;
M99;

// G0201
%@MACRO
#101 := #1000;
#102 := #1004;
G91 G01 X20.;
G91 G01 Y20.;
G91 G01 X-20.;
G91 G01 Y-20.;
G#101;
G#102;
M99;

// G0202
%@MACRO
#101 := #1000;
#102 := #1004;
#103 := #24;  // X 引数作为正方形边长
G91 G01 X#103;
G91 G01 Y#103;
G91 G01 X-#103;
G91 G01 Y-#103;
G#101;
G#102;
M99;
```

### 10.6 变量使用建议

状态：已确认

| 需求 | 建议 |
| --- | --- |
| MACRO 内部暂存与计算 | 优先使用区域变量 `#1` ~ `#400` |
| 跨 MACRO 短期交换 | 优先使用模态变量 `#2001` ~ `#2100`、`#3001` ~ `#3100` |
| 较长生命周期共享 | 使用公用变量 `@1` ~ `@400` |
| 断电保存 | 使用 `@656` ~ `@999`、`@1001` ~ `@1999` |
| 默认初始值 | 使用客制参数 `#4001` ~ `#5500` |

客制参数对应：

| 变量范围 | 对应参数 | 说明 |
| --- | --- | --- |
| `#4001` ~ `#4100` | `Pr4001` ~ `Pr4100` | 使用者参数，只读 |
| `#4101` ~ `#4200` | `Pr4101` ~ `Pr4200` | 使用者参数，只读 |
| `#5001` ~ `#5100` | `Pr5001` ~ `Pr5100` | 使用者参数，只读 |
| `#5101` ~ `#5200` | `Pr5101` ~ `Pr5200` | 使用者参数，只读 |
| `#5201` ~ `#5500` | `Pr5201` ~ `Pr5500` | 使用者参数，只读 |

### 10.7 运算与型态陷阱

状态：已确认

```iecst
#1 := 1 / 2;         // 结果 0，整数除整数
#1 := 1. / 2;        // 结果 0.5
#1 := 1 / 2.0;       // 结果 0.5
#1 := (1/2) * 1.0;   // 结果 0.0
#1 := (1./2) * 1.0;  // 结果 0.5

#2 := 10 MOD 3;      // 合法
#2 := 10. MOD 3;     // COR-054 风险
```

规则：

- 除法至少让分子或分母带小数点，避免整数截断。
- `MOD` 仅适用于 Long 型态。
- 括号内表达式会先计算，括号内若发生整数截断，后续乘以 Double 也无法恢复精度。

### 10.8 循环与格式

状态：已确认

规则：

- `WHILE`、`REPEAT`、`FOR`、`GOTO` 都有无穷循环风险，循环中建议适时加入 `SLEEP()`。
- 不要在 `FOR` 循环内使用跳转进出循环的命令，包括往返 `GOTO`、复式切削循环 `G72` ~ `G78`、`M98 H_`。
- `IF/CASE/WHILE/FOR/REPEAT` 嵌套上限为 10 层，超过会触发 `COM-007`。
- 使用空格或 Tab 缩排，提高可读性。
- MACRO 格式注释使用 `//` 或 `(* ... *)`，不要与 ISO 的 `( ... )` 注释混用。

需要分号的语法：变量赋值、G/M 码指令、`GOTO`、`EXIT`、`END_IF`、`END_FOR` 等结尾语句。

不需要分号的语法：`IF ... THEN`、`ELSEIF ... THEN`、`ELSE`、`WHILE ... DO`、`FOR ... DO`、`REPEAT`、`CASE ... OF`、`N:` 条件标签行。

### 10.9 调试与排查

状态：已确认 / 部分版本差异

| 现象 | 可能原因 | 排查方向 |
| --- | --- | --- |
| 变量值不正确 | 预解导致读取时机错误 | 加 `WAIT()` |
| 宏程序不执行 | 引数被同一行其他宏程序占用 | 拆成多行 |
| 人机锁死 | 无穷循环 | 加 `SLEEP()` |
| `COM-003` | ISO 格式档使用 MACRO 语法 | 检查首行 `%@MACRO` |
| `COR-016` | 变量存取超出范围或路径不合法 | 检查变量编号和 APP Macro 路径 |
| 父程序行为异常 | MACRO 内改变模态 G 码未还原 | 进入时备份，离开前还原 |
| 图形模拟失效 | MACRO 内使用 `G92/G54/G52` 等 | 改用 `G91` 增量模式 |
| FOR 步进值异常 | FOR 内使用跳出再跳回 | 避免在 FOR 内跳转 |

常用调试手段：`MSG("...")`、`SLEEP(ms)`、写入公用变量 `@`、单节执行、观察 `D978/D979`。

### 10.10 撰写模板

状态：已确认

```iecst
%@MACRO
// 功能说明: [描述此 MACRO 的用途]
// 引数说明: X=..., Y=..., Z=..., P=..., Q=...

// 1. 引数检查与默认值
IF #24 = #0 THEN
  #24 := 0.;
END_IF;
IF #25 = #0 THEN
  #25 := 0.;
END_IF;
IF #9 = #0 THEN
  #9 := 1000.;
END_IF;

// 2. 单位标准化
#27 := STD(#24);
#28 := STD(#25);

// 3. 模式备份
#30 := #1004;
#31 := #1015;

// 4. 主体逻辑
G91;
G01 X#27 Y#28 F#9;
WAIT();
#32 := #1321;

// 5. 模式还原
G#31;
G#30;

// 6. 返回
M99;
```

## 11. 已落地规则索引

| 规则 | 状态 |
| --- | --- |
| `DIV` 不支持 | 已落地 |
| `==` 不支持 | 已落地 |
| `DEFAULT` 不支持 | 已落地 |
| CASE 分支同行陈述 warning | 已落地 |
| 机器人移动族旧式 `=` 诊断 | 已落地 |
| `TOOLCOR T_` 诊断 | 已落地 |
| `TOOLCORON` warning，且不再补全/高亮 | 已落地 |
| `TOOLCOR CLEAR` warning | 已落地 |
| `OBJCORON X=...` 诊断 | 已落地 |
| `G68.18 P=...` 诊断 | 已落地 |
| `G43.16 P=...` 诊断 | 已落地 |
| `POSEMAP X=...` 诊断 | 已落地 |
| `MOVC Xp/Yp/Zp` 诊断 | 已落地 |
| `MOVC` 成对诊断 | 已落地 |
| `PL/PQ/PR` 同行互斥诊断 | 已落地 |
| `SHIFTON`、`SKIPCOND`、`SWAITSIG`、`STITCHON`、`WEAVEON` 旧式 `=` 诊断 | 已落地 |
| `STITCHON L/K` 互斥诊断 | 已落地 |
| `WEAVEON` 双语法混用诊断 | 已落地 |
| 同一移动单节多个 `SWAITSIG` 诊断 | 已落地 |
| 同一移动单节超过 10 个 `SYNCOUT` 诊断 | 已落地 |
| `STITCHON` / `WEAVEON` 区间禁用指令诊断 | 已落地 |
| `USERCOR/TOOLCOR` 静态编号范围与坐标语法边界 | 已落地 |
| `G68.18` `P/R` 范围与轴向/G 码混用诊断 | 已落地 |
| `POSEMAP` `Q/R` 静态范围诊断 | 已落地 |
| `SHIFTON`、`CIRMODE`、`WAITSYNC/ENDSYNC` 静态范围诊断 | 已落地 |
| `SKIPCOND/SWAITSIG/SYNCOUT` 静态范围与 Q 联动诊断 | 已落地 |
| `G192.1` 静态范围诊断 | 已落地 |
| `tests/fixtures/test-demo.nc` 零诊断回归 | 已落地 |

## 12. 待落地优先级

1. `G10 L1000` / `G10 L1021` / `G10 L1022` / `G10 L1803` / `G10 L1805` / `G10 L1810` / `G10 L1820` / `G10 L1910` / `G10 L1911` 静态范围检查；`L1900/L1901` 其余未确认上限不做臆测校验。
2. `G04.102 I` 静态范围检查。
3. `STITCHON/WEAVEON` 更完整的静态范围检查。
4. `MOD` 明显 Double 操作数检查。
5. `AXID/STDAX` 字符串轴名 warning。
6. `OPEN("COM1")` warning。
7. `ALARM/MSG` 静态 ID 和字符串长度检查。
8. `#0` / `@0` 作为赋值目标 warning。
9. 固定编号 `#401` 以上依变量区间提示用途或风险。
10. 扩充引数数字后缀合法性检查，例如 `D1`、`P1` 报错提示。
11. 公用变量 `@` 固定编号区间提示：R 寄存器映射区、断电记忆区、CE 扩充区。
12. 静态可判定的 R 寄存器系统保留区写入 warning。
13. 字符串写入公用变量时提示 ASCII 转码与 `SCANTEXT` 读取规则。
14. AR/MAR 固定编号负数、小数诊断。
15. AR/MAR APP Macro 路径诊断：非 `DiskC\app\%AppName%\bin\Macro\` 路径提示 `COR-016` 风险。
16. 读取 `app.acfg` 后对 AR/MAR 固定编号做容量范围检查。
17. AR 字符串写入与 APP Macro 中 `SCANTEXT` 读取规则提示；MAR 字符串写入提示不支援。
18. 运算式复杂度与嵌套深度提示，对应 `COM-005` / `COM-007` 风险。
19. G 码引数运算提示：括号可省略但复杂表达式建议加括号。
20. IF / CASE / REPEAT / WHILE / FOR 单档组数上限提示。
21. 单组 `ELSEIF` 与 CASE 条件语句数量上限提示。
22. FOR 循环内跳转进出风险提示：`G72` ~ `G78`、往返 `GOTO`、`M98 H_`。
23. 缺少分号的版本差异提示，尤其 10.120.45 ~ 10.120.60 的 `COM-008` 风险。
24. `GOTO` 无穷循环风险提示，建议循环中加入 `SLEEP()`。
25. 路径扩充引数诊断：允许 `,C_`、`,R_`、`,A_`，未确认引数如 `,Z_` 提示 `COR-034` 风险。
26. 预解风险提示：运动后立即读取座标或状态时建议加入 `WAIT()`。
27. 宏程序内改变 `G90/G91`、座标系或 `#1000` 时提示备份与还原风险。
28. 大档案范围语法跨页与动态建表降速风险提示。
29. 同一单节 G 码宏程序 / M 码宏程序 / 插值码引数占用顺序提示。
30. MACRO 撰写模板检查：缺少 `M99`、未还原模态、运动后读状态未 `WAIT()` 等风险提示。
31. `L` 引数特殊值提示：未指定为 `L1`，`L0` 不执行。
32. 客制参数只读区间 hover 与写入 warning。
33. 扩充 G 码 / 登录 G 码 / 登录 M 码 / T 码宏程序文件名规则诊断。
34. 登录 M 码系统保留码检查：`M00`、`M01`、`M02`、`M30`、`M96`、`M97`、`M98`、`M99` 不可登录。
35. 登录 M 码执行后引数占用提示：除 T 码外所有引数视为占用。
36. 同一单节多个 M 码宏程序只执行第一个、多个 G 码宏程序只执行最后一个的提示。
37. `Pr3598` M 码引数模式对 `#13` 的影响提示。
38. 登录 G 码宏程序内改变 `#1000` 未还原的风险提示。
39. 数学函数静态域检查：`ATAN2(0,0)`、`POW` 负基底、`LN` 非正数。
40. `PARAM`、`CHKINF`、I/O 读写函数参数型态与范围检查。
41. `SYSDATA` / `DRVDATA` 前缺少 `WAIT()` 的预解风险提示。
42. `READDI` / `READDO` / `READABIT` / `READRREGBIT` 预解减速风险提示。
43. `SETDO` / `SETABIT` / `SETRREGBIT` 与 PLC 混用风险提示。
44. `PRINT` 字符串逃脱字符与小数位 `[n]` 格式高亮 / hover。
45. DB 函数图形模拟固定回传 0 的提示。
46. `ALARM` / `MSG` 自订警报字串档路径 hover。
47. `SETDRAW` BGR 颜色码 hover。
48. 维持 `MOVJ-II` / `INCMOVJ-II` 非正式指令诊断，同时补齐 `MOVJ` / `INCMOVJ` 第二语法与替代语法识别。
49. 移动附加指令范围检查：`FJ/FEJ/FL/FR`、`ACC/DEC`、`PL/PQ/PR`。
50. `WAITSYNC/ENDSYNC`、`CIRMODE`、`G142.101/G142.102` 等机器人指令补全、hover 与诊断。
51. 机器人互斥关系完整诊断：`WAITSYNC`、`G192.1`、`SHIFTON` 与 `STITCHON/WEAVEON` 区间规则。
52. RBT 警报码 hover：`RBT-102`~`RBT-327`、`RBTWRN-010`、`RBTCRI-011`。

## 13. 待确认项

> 路径提示：CF 检索状态以 [Atlassian-MACRO知识记录](macro-knowledge/Atlassian-MACRO知识记录.md) 的来源清单和 [能力矩阵](macro-knowledge/MACRO能力矩阵.md) 为准；CF 未在官方页面找到的项目保持待确认，不进入强诊断。

| 项目 | 原因 |
| --- | --- |
| SWAITSIG 与运动单节关系边界 | 需要确认注释/赋值/WAIT 是否切断 |
| SYNCOUT 数量上限的“同一移动单节”定义 | 需要确认边界 |
| STITCHON/WEAVEON 区间禁用指令完整行为 | 已落地核心禁用项；WAITSYNC/ENDSYNC、手轮反转等待补 |
| G04.102 的 `X` 数值范围 | CF 页面仅给「时间」与 `I` 引数语义，与同语系时间引数一致采用 `ms`；具体数值范围未列出。 |
| G193.110 | CF 目前未找到独立正式规格页面；「语法指令规格」总表未列出。 |
| `#401` ~ `#999` 轴群使用者记忆变量的 L 值对应关系 | 需查完整系统变量表 |
| `#1080` ~ `#3100` 模态变量各区间型态与清除时机细节 | 需查完整系统变量表 |
| `@1000` 版本差异的精确启用边界 | 旧版系统变量，新版有记忆性变量；需按控制器版本确认 |
| `Pr3811` 保留范围表达方式与参数颗粒度 | 《Macro变数规格》证实功能本体（控制 @1~@400 保留），但未列出参数条目格式、颗粒度与默认值；《控制器参数总表》入口页面在 CF 可访问但未提供详细 Pr 页面；待实机页面查表或对应 PDF 补齐。 |
| `Pr3813` 启用 CE 扩充区段的参数格式与颗粒度 | 同上；CF 仅证实需配合 `Pr3813` 且仅适用 CE 系统；非 CE 系统使用该区段的行为 CF 未说明。 |
| AR 字符串支援起始版本 | 需按控制器版本确认 |
| APP Macro 路径在 Windows 工作区中的映射方式 | 需确认本地项目如何对应 `DiskC\app\%AppName%\bin\Macro\` |
| `OP-023` 对 MAR 的不检查边界 | 需确认相关断电检查流程 |
| `D978` / `D979` / `D994` / `D995` 执行线程诊断编号 | CF 未在《控制器诊断变数》页面直接证实这 4 个编号；执行线程诊断优先使用 CF 已证实的 `D388`（Script 平均执行时间）与 `D932`/`D933`（NetPLC 同步时间）；手册原标注的 D978/D979 大于4倍PLC扫描时间的原则同样缺乏 CF 背书。待实机或控制器内部规格复核。 |
| `#1000` 自动改写与扩充 G 码自动再呼叫的完整边界 | 需实机验证 |
| `M98`、`M99`、`M198` 不受 `WAIT()` 影响的细部行为 | 需实机验证 |
| V12 `P10201` ~ `P10210`、`P10301` ~ `P10310` 的完整范围与生效时机 | 需查 V12 参数规格 |
| 登录 G 码在各版本的解译顺序差异 | 10.114.50 前后行为需实机或版本资料确认 |
| `Pr3598=1` 下 `COR-371` 的完整触发条件 | 需实机验证 |
| 登录 G 码不支援车铣加工循环的完整清单 | 需补完整控制器规格 |
| 负数整数除法取整方向 | CF 手册确认「结果为整数」但不明确取整方向，需实机验证 |
| 除以零行为 | 需实机验证 |
| Long 溢出行为 | 需实机验证 |

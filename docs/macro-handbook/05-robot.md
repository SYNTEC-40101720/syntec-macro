# 新代 MACRO 语法规范手册 · 机器人移动、坐标系与应用指令

> 本文件为 [新代 MACRO 语法规范手册](../新代MACRO语法规范手册.md) 的拆分子主题（机器人移动、坐标系与应用指令）。
> 状态标记规则与维护流程见 [原手册索引 §0](../新代MACRO语法规范手册.md#0-状态标记)。

## 6. 机器人移动指令

### 6.0.1 指令总表

状态：已确认 / 部分待同步插件

| 说明 | 指令 | 替代语法 | 支援版本 |
| --- | --- | --- | --- |
| 末端直线运动 | `MOVL` | `G01.102` | 10.118.0A 起 |
| 关节运动 | `MOVJ` | `G53.102` | 10.118.0A 起 |
| 关节运动第二语法，末端位置输入 | `MOVJ` | `G53.102` | 10.118.0A 起 |
| 外部轴运动 | `MOVJ` | - | 10.118.0A 起 |
| 圆弧运动 | `MOVC` | - | 10.118.0A 起 |
| 增量末端直线运动 | `INCMOVL` | `G01.101` | 10.118.9 起 |
| 增量关节运动 | `INCMOVJ` | `G53.101` | 10.118.9 起 |
| 增量关节运动第二语法，末端位置输入 | `INCMOVJ` | `G53.101` | 10.118.9 起 |

说明：`MOVJ-II`、`INCMOVJ-II` 只是文档中用于说明第二语法的称呼，实际指令仍为 `MOVJ`、`INCMOVJ`；插件不应把带 `-II` 的写法当作正式指令补全或高亮。替代语法用于配合人机编辑/显示机制，插件可识别但不应优先生成替代语法。

`INCMOVJ` 第二语法版本细节：

| 项目 | 支援版本 |
| --- | --- |
| 基本指令，`P/X/Y/Z/A/B/C/FJ/FEJ/PL` | 10.118.9 起 |
| `A/B/C` 引数 | 10.118.12A、10.118.12 及之前版本请使用 `U/V/W` |
| `ACC/DEC` | 10.118.25 起 |
| `SKIP` | 10.118.33、10.118.28G 起 |
| `Q` 指定工具 | 10.118.82B 起 |
| `I` 姿势解域、`W1=`~`W6=` 同界角 | 10.118.86 起 |
| 替代语法 `G53.101` | 10.120.32M、10.120.42A、10.120.43、12.0.3 起 |

### 6.0 路径扩充引数

状态：已确认 / 待落地

控制器支援在路径单节后使用逗号形式的路径扩充引数，用于转角倒角、转角圆角、直线角度等 C-Type 语法。

```iecst
G01 X100. Y100., C10.;
G01 X100. Y100., R10.;
G01 X100. Y100., A45.;
```

规则：

- 目前确认支援 `,C_`、`,R_`、`,A_`。
- 不支援的路径扩充引数会触发 `COR-034` 路径扩充引数不存在。
- 逗号后的扩充引数不是一般轴地址引数，插件应独立识别。

错误示例：

```iecst
G91 G01 X100. Y100, Z100;
```

插件策略：

- 识别路径单节中的 `,C_`、`,R_`、`,A_` 为合法路径扩充引数。
- 对静态可判定的 `,Z_` 等未确认路径扩充引数报 error 或 warning，提示 `COR-034` 风险。

### 6.1 MOVJ 第一语法

状态：已确认

```iecst
MOVJ C1=_ C2=_ C3=_ C4=_ C5=_ C6=_
     [A1=_ A2=_ A3=_ A4=_ A5=_ A6=_]
     [Q_] [FJ_] [FEJ_] [PL_] [ACC_] [DEC_] [SKIP];
```

规则：

- `C1~C6`、`A1~A6` 使用 `=`。
- `Q/FJ/FEJ/PL/ACC/DEC` 为直接引数。
- 不支持 `P_`。
- 不支持 `PQ_`、`PR_`。

插件策略：

- `MOVJ X=...`、`FJ=...` 报 error。
- `P/PQ/PR` 禁用诊断。

### 6.2 MOVJ 第二语法

状态：已确认

```iecst
MOVJ [LP=_] [GP=_] X_ Y_ Z_ A_ B_ C_
     [A1=_ A2=_ A3=_ A4=_ A5=_ A6=_]
     [I_] [W1=_ W2=_ W3=_ W4=_ W5=_ W6=_]
     [P_] [Q_] [FJ_] [FEJ_] [PL_] [ACC_] [DEC_] [SKIP];
```

规则：

- `X/Y/Z/A/B/C/P/Q/FJ/FEJ/PL/ACC/DEC/I` 为直接引数。
- `A1~A6`、`W1~W6` 使用 `=`。
- `LP/GP` 使用 `=`。
- 不支持 `PQ_`、`PR_`。
- 第二语法使用 `MOVJ` 指令与末端位置输入，控制器自动逆运算为关节角度；`MOVJ-II` 不是正式指令写法。

### 6.3 MOVL

状态：已确认

```iecst
MOVL [LP=_] [GP=_] X_ Y_ Z_ A_ B_ C_
     [A1=_ A2=_ A3=_ A4=_ A5=_ A6=_]
     [P_] [Q_] [FL_] [FR_] [FEJ_]
     [PL_ / PQ_ / PR_] [ACC_] [DEC_] [SKIP];
```

规则：

- `X/Y/Z/A/B/C/P/Q/FL/FR/FEJ/PL/PQ/PR/ACC/DEC` 为直接引数。
- `A1~A6`、`LP/GP` 使用 `=`。
- `PL/PQ/PR` 三选一。

### 6.4 MOVC

状态：已确认 / 部分跨行规则已落地

```iecst
MOVC X_ Y_ Z_ A_ B_ C_
     [A1=_ A2=_ A3=_ A4=_ A5=_ A6=_]
     [FL_] [FR_] [FEJ_]
     [PL_ / PQ_ / PR_] [ACC_] [DEC_];
```

已确认：

- 不使用 `Xp/Yp/Zp` 通过点写法。
- `X/Y/Z/A/B/C/FL/FR/FEJ/PL/PQ/PR/ACC/DEC/P/Q` 为直接引数。
- `A1~A6` 使用 `=`。
- `PL/PQ/PR` 三选一。
- MOVC 双行写法从 10.118.0A 起支援：第一行中间点，第二行结束点。
- MOVC 单行写法从 10.118.52H、10.118.56B、10.118.57 起支援：中间点使用 `X1=/Y1=/Z1=/A1=/B1=/C1=`，终点使用 `X2=/Y2=/Z2=/A2=/B2=/C2=`。
- `LP/GP` 参考点语法从 10.118.40I、10.118.41I、10.118.45 起支援，中间点与终点均可使用。
- 中间点与结束点之间允许部分 Macro 标准语法与模态指令；不支持的夹杂指令会报警。
- 中间点与结束点之间指令数上限为 10 个（Macro 标准语法与模态指令除外）。

双行写法：

```iecst
MOVC X_ Y_ Z_ A_ B_ C_ [FL_] [FR_] [PL_ / PQ_ / PR_];
MOVC X_ Y_ Z_ A_ B_ C_ [A1=_ ... A6=_] [FL_] [FR_] [PL_ / PQ_ / PR_] [ACC_] [DEC_];
```

单行写法：

```iecst
MOVC [LP=_] [GP=_] X1=_ Y1=_ Z1=_ A1=_ B1=_ C1_=,
     X2=_ Y2=_ Z2=_ A2=_ B2=_ C2=_
     [A1=_ ... A6=_] [P_] [Q_] [FL_] [FR_] [FEJ_]
     [PL_ / PQ_ / PR_] [ACC_] [DEC_] [SKIP];
```

双行写法的中间点与终点单节之间仅支援：

- Macro 标准语法，但会挡预解的指令除外，例如 `WAIT`、`READDI`、`READDO`。
- `G10 L1000`，R 值写入。
- `M99`，副程序结尾。
- 模态指令：`FL/FR/FEJ`、`ACC/DEC`、`PL/PQ/PR`。

中间单节指令数量上限为 10 个，Macro 标准语法与模态指令除外，否则触发 `RBT-127`。

跨行边界（CF 正式页面已确认）：

- 中间点与结束点单节的模态皆相同；圆弧模态取最后下的指令为优先，依序为结束点单节引数 → 中间点与结束点间的指令 → 中间点单节引数。
- 中间点与结束点单节的座标系必须相同，否则触发 `RBT-124 不合法的圆弧指令`。
- 中间点与结束点间的指令数量上限为 10 个（Macro 标准语法与模态指令除外），否则触发 `RBT-127 圆弧运动单节间的指令数量已超过上限`。

插件策略：

- `MOVC Xp=...`、`Yp=...`、`Zp=...` 报 error。
- `MOVC X=...`、`FL=...` 报 error。
- 单行 MOVC 未能与下一条 MOVC 配对时，插件报 error。
- 同行 `PL/PQ/PR` 出现两个以上时，插件报 error。

### 6.5 INCMOVJ

状态：已确认

```iecst
INCMOVJ C1=_ C2=_ C3=_ C4=_ C5=_ C6=_
        [A1=_ A2=_ A3=_ A4=_ A5=_ A6=_]
        [Q_] [FJ_] [FEJ_] [PL_] [ACC_] [DEC_] [SKIP];
```

规则：

- `C1~C6/A1~A6` 使用 `=`。
- `Q/FJ/FEJ/PL/ACC/DEC` 为直接引数。
- `Q_` 可省略。
- 仅支持 `PL_`，不支持 `PQ_`、`PR_`。

### 6.6 INCMOVL

状态：已确认

```iecst
INCMOVL P_ X_ Y_ Z_ A_ B_ C_
        [A1=_ A2=_ A3=_ A4=_ A5=_ A6=_]
        [Q_] [FL_] [FR_] [FEJ_]
        [PL_ / PQ_ / PR_] [ACC_] [DEC_] [SKIP];
```

规则：

- `P_` 必填，范围 1~2。
- `P/X/Y/Z/A/B/C/Q/FL/FR/FEJ/PL/PQ/PR/ACC/DEC` 为直接引数。
- `A1~A6` 使用 `=`。
- `PL/PQ/PR` 三选一。

### 6.7 移动附加指令

状态：已确认 / 部分待落地

速度指令：

| 指令 | 说明 | 单位 | 范围 | 适用运动 |
| --- | --- | --- | --- | --- |
| `FJ` | 内部轴速度百分比 | `%` | 0.01~100 | `MOVJ` |
| `FEJ` | 外部轴速度百分比 | `%` | 0.01~100 | `MOVJ` / `MOVL` / `MOVC` |
| `FL` | 末端直线进给速度 | `mm/s` | 0.1~Pr405 | `MOVL` / `MOVC` |
| `FR` | 末端旋转进给速度 | `deg/s` | 0.1~Pr4307 | `MOVL` / `MOVC` |

规则：由于维持同动性，运行中不一定都能达到 `FL` 与 `FR` 的速限。

转角平滑：

| 指令 | 说明 | 单位 | 范围 |
| --- | --- | --- | --- |
| `PL` | 轨迹平滑等级，0=准确到达且速度降至零，10=最大平滑 | - | 0~10 |
| `PQ` | 末端位置距目标点多少 mm 时开始平滑 | `mm` | 0~半个单节长度 |
| `PR` | 末端姿态距目标点多少 deg 时开始平滑 | `deg` | 0~半个单节旋转角度 |

规则：

- `PL/PQ/PR` 仅能选择一个，否则触发 `RBT-103`。
- `MOVJ` 不支援 `PQ/PR`，仅支援 `PL`。
- 程序预设值为 0，即每个目标点减速到零停止。
- 后令盖前令。

加减速倍率：

| 指令 | 说明 | 单位 | 范围 |
| --- | --- | --- | --- |
| `ACC` | 加速度倍率 | `%` | 20~100 |
| `DEC` | 减速度倍率 | `%` | 20~100 |

规则：

- RESET 时初始化为 100%。
- 超出范围触发 `RBT-102`。

跳脱标记：

- `SKIP` 无引数，接在运动指令后面，表示该运动单节可进行跳脱动作。
- `SKIP` 需配合 `SKIPCOND` 设定跳脱条件。

## 7. 坐标系与工具系

### 7.1 USERCOR

状态：已确认

```iecst
USERCOR P_;
```

规则：

- `P0` 世界坐标系。
- `P1~P20` 用户自定义坐标系。

### 7.2 G68.18

状态：已确认

```iecst
G68.18 P_ [R_] X_ Y_ Z_ A_ B_ C_;
```

规则：

- `P` 范围 1~20。
- `R` 可省略，范围 0~3。
- `R0` 表示基于世界坐标系。
- `X/Y/Z/A/B/C` 为直接引数。
- `R_` 在 10.118.28V 不支持。

### 7.3 OBJCORON / OBJCOROFF / OBJCORCLEAR

状态：已确认

```iecst
OBJCORON X_ Y_ Z_ A_ B_ C_;
OBJCOROFF;
OBJCORCLEAR;
```

规则：

- `X/Y/Z/A/B/C` 为直接引数。
- 未下之引数设定为 0。
- 无小数点时参考 Pr3241。
- 最多叠加 3 层。

### 7.4 G43.16

状态：已确认

```iecst
G43.16 P_ X_ Y_ Z_ A_ B_ C_;
```

规则：

- `P` 范围 1~20。
- `X/Y/Z/A/B/C` 为直接引数。
- XYZ 缺项沿用设定前偏移量。
- ABC 部分缺项时，缺项方向设 0。
- ABC 全缺时沿用设定前旋转量。
- 101 构型仅 C 有效；106~110 构型 A/B/C 有效。

### 7.5 TOOLCOR

状态：已确认

```iecst
TOOLCOR P_;
TOOLCOROFF;
```

规则：

- `P0` 无工具（法兰面坐标系）。
- `P1~P20` 使用者自定义工具。
- `TOOLCORON` 未见官方语法，插件不补全；出现时 warning。
- `TOOLCOR CLEAR` 未见官方语法，建议使用 `TOOLCOR P0;`。

### 7.6 POSEMAP

状态：已确认

```iecst
POSEMAP X_ Y_ Z_ A_ B_ C_ Q_ R_;
```

规则：

- 所有引数均为直接引数，不使用 `=`。
- `Q` 范围 0~20。
- `R` 范围 1~2。
- 缺项设 0。

### 7.7 SHIFTON / SHIFTOFF

状态：已确认 / 静态范围已落地

```iecst
SHIFTON P_ X_ Y_ Z_ A_ B_ C_;
SHIFTOFF;
```

规则：

- `P` 范围 1~2。
- `X/Y/Z/A/B/C` 为直接引数。
- 缺项方向设 0。
- 后令盖前令，无继承关系。
- 对 INCMOVL、INCMOVJ、G91 无效。

### 7.8 其他坐标系设定

状态：待落地

| 指令 | 说明 |
| --- | --- |
| `G142.101` | 三点设定用户坐标系 |
| `G142.102` | 六点设定用户坐标系 |
| `G52.101` | `OBJCORON` 偏移开始替代语法 |

## 8. 应用指令

### 8.1 SKIPCOND / SKIP

状态：已确认 / 静态范围已落地

```iecst
SKIPCOND E_ Q_ R_ P_;
SKIP;
```

规则：

- `E` 范围 1~3：1=I-bit，2=C-bit，3=R-bit。
- `Q` 依 `E` 联动。
- `R` 范围 0~1。
- `P` 范围 0~1：0=急煞，1=软煞。
- 所有引数为直接引数。
- 跳脱后可读取系统变量：`#1441`~`#1458` 为跳脱时关节坐标反馈，`#1361`~`#1378` 为跳脱时程序坐标反馈，`#1608` 表示跳脱事件是否触发。
- 未设定 `SKIPCOND` 时，默认条件为 `C62 ON` 急煞。
- 不合法跳脱条件触发 `RBT-119`。

#### 8.1.1 G31 跳脱与 MACRO IO 时序交互

状态：已确认 / 部分版本差异

来源：CF [G10L1803-MACRO IO TYPE-1 新格式](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44134562)（§G31 跳脱交互）。

`SKIP` / `SKIPCOND` 跳脱与 `G10 L1803` / `G10 L1805` MACRO IO 输出在同一运动单节内存在时序交互：

| 场景 | 行为 |
| --- | --- |
| `G31` 跳脱触发前，`G10 L1803` 已按距离到达触发点 | 讯号已发出，跳脱动作不受影响 |
| `G31` 跳脱发生时，`G10 L1803` 距离条件未满足 | 该 `G10 L1803` 指令本次无效，下你单节开始重计 |
| `G10 L1803` 的 `K` 提前量提前 / 延后 | 不可越过当前 `G01` / `G02.4` / `G03.4` / `G31` 弧段范围。对 `G02.4` / `G03.4`，仅 `P` 型引数可跳弧；`K` 不能跨弧使用 |
| `G10 L1803` / `G10 L1805` 在跳脱中重跳 | 需配合 `SKIPCOND E_` 条件 0->1 过渡，否则触发 `RBT-119` |

注意事项：

- `G31` 支持版本门槛：`10.118.42S` 、`10.118.48D` 、`10.118.50` 及之后。低于版本门槛的设定将报错或被忽略。
- `G10 L1803` 在同一运动单节内最多 12 组，超额触发 `COR-346`；与 `G10 L1805` 综合超额也触发 `COR-346`。
- 静音模式（`#1500=1` 或 `#1820` 值 1/2）下不推荐 `G31` 跳脱与 `L1803` 时序混用，可能发生 skipped triggering。

插件策略：

- 同一单节下同时出现 `G31` / `SKIP` 与 `G10 L1803` / `G10 L1805` 时，插件可加 hint 提示检查版本门槛与重计机制。
- 同一 R/I/C-bit 同时被 `SKIPCOND` 条件 + `G10 L1803` 输出源引用，插件后续可加 warning。

### 8.2 SWAITSIG

状态：已确认 / 静态范围已落地

```iecst
SWAITSIG P_ Q_ R_ [L_] [T_];
```

规则：

- `P` 范围 1~3：1=I-bit，2=R-bit，3=A-bit。
- `Q` 依 `P` 联动。
- `R` 范围 0~1。
- `L/T` 可省略，范围 0~2^31 ms。
- 运动单节后只能下 1 个 SWAITSIG。
- 核心会智慧判定减速等待讯号或加速通过并继续处理下一单节。
- 适用于连续运动、不减速到零的单节，例如有下 `PL/PQ/PR` 的移动单节。
- 替代语法为 `G04.101 E_ Q_ R_ C_ [T_] [P_]`。
- 等待多讯号不减速替代语法为 `G04.103`。
- 相关警报：`RBT-104`、`RBT-153`、`RBT-154-2`。
- 插件已检查：同一移动单节后超过 1 个 `SWAITSIG` 报 error；`WAIT()` 会切断此关系。

### 8.3 SYNCOUT

状态：已确认 / 静态范围已落地

```iecst
SYNCOUT S_ Q_ P_ R_ [L_] [K_];
```

规则：

- `S` 范围 1~3：1=O-bit，2=R-bit，3=A-bit。
- `Q` 依 `S` 联动。
- `P` 范围 0~100。
- `R` 范围 0~1。
- `L` 范围 0~10000 ms；`L0/L#0` 视为省略。
- `K` 范围 -10000~10000 ms；正数提前，负数延后。
- 单一有移动量移动单节最多 10 个 SYNCOUT。
- 核心功能为在移动单节中进行点位状态切换。
- 替代语法：运动中输出 `G11.103 E_ Q_ R_ P_ [T_]`；非运动中输出 `G11.101`。
- 不合法进阶输出触发 `RBT-110`。
- 插件已检查：同一移动单节后超过 10 个 `SYNCOUT` 报 error。

### 8.4 G10 L 指令

状态：已确认 / Hover、Snippet 与 L1900/L1901 基础静态诊断已落地

#### 8.4.1 G10 L1000 - R 寄存器写入

```iecst
G10 L1000 P_ R_;
```

功能：透过 G 码变更 R bit 的数值。

| 引数 | 说明 |
| --- | --- |
| `P` | R bit 号码 |
| `R` | 写入 R bit 的数值 |

规则：

- 在绝对指令模式（`G90`）下是绝对值，在增量指令模式（`G91`）下是增量值。
- 在 `G62` 模式下，指令会准确在 `G01/G02/G03` 单节交接处发出。
- 此指令之 `R` 值，会受到 `Pr17` 控制精度及 `Pr3241` 工件程序小数点型态影响。
- 如果 `R` 引数对应的变数具有物理意义，则单位为 LIU，会受到公英制影响。

范例：

```iecst
// Pr17=2
(G71 G90) G10 L1000 P1024 R10.1;   // 将 10.100 填入 R1024

// Pr3241=0
(G71 G90) G10 L1000 P1024 R10;     // 将 10 填入 R1024

// Pr3241=1、Pr17=2
(G71 G90) G10 L1000 P1024 R10;     // 将 10.000 填入 R1024

// Pr17=2，物理意义，公制
G71 G90 G10 L1000 P1024 R0.01;     // 表示 0.01 mm，将 10 填入 R1024

// Pr17=2，物理意义，英制
G70 G90 G10 L1000 P1024 R0.01;     // 表示 0.01 inch，将 100 填入 R1024
```

#### 8.4.2 G10 L1803 - MACRO IO TYPE-1 新格式

```iecst
G10 L1803 I_ Q_ P_ R_ [J_] [K_] [E_];
```

功能：在不减速的情况下，于运动单节内按距离触发讯号。支持设定维持时间、偏移时间和计算方向。

引数：

| 引数 | 说明 | 数值范围 |
| --- | --- | --- |
| `I` | 讯号源 | 1=R-bit，2=O-bit，3=A-bit |
| `Q` | 讯号号码 | R-bit：0~65535 + 0~31（旧版 0~15）；O-bit：0~511；A-bit：0~511 |
| `P` | 单节触发距离（IU 线性距离） | >=0，不支援负值；从单节交接点处按 `E` 方向计算，超过单节长度则在起点或终点触发 |
| `R` | 讯号状态 | 0=LEVEL LOW，1=LEVEL HIGH |
| `J` | 讯号维持时间，可省略 | 0~3600000 ms；>0 时维持后输出相反极性，=0 或省略时输出一次 |
| `K` | 讯号偏移时间，可省略 | 单位 ms；正值=提早，负值=延迟，偏移无法超出该单节范围 |
| `E` | 触发距离计算方向，可省略 | 0=单节由前往后（默认），1=单节由后往前 |

规则：

- 核心差异：`G10 L1803` 在运动单节内按距离触发，`G10 L1805` 在单节间触发。
- 支持的运动指令：`G00`、`G01`、`G02`、`G03`、`G02.4`、`G03.4`、`G31`。
- 每个运动单节之间最多支持 12 组不重复的 `G10 L1803` 指令；超过触发 `COR-346`。12 组支持版本：10.118.41J、10.118.46。
- 引数超出范围触发 `COR-345`。
- 需接运动单节才会生效。
- 支持零移动单节，忽略 `P/K/E`，`J` 仍有效。
- 使用进给轴设定（`G10 L1100 P1002 R_`）时，触发距离参考进给轴合成移动量。
- `G31` 跳脱：若跳脱前已触发则不受影响，跳脱时未触发则该指令无效。
- `G02.4/G03.4` 时 `P` 引数可横跨两段圆弧，但 `K` 引数无法超出 `P` 所在圆弧段。
- 最低版本：10.118.28E、10.118.32 及之后。
- `G31` 支持版本：10.118.42S、10.118.48D、10.118.50 及之后。

#### 8.4.3 G10 L1805 - MACRO IO TYPE-2

```iecst
G10 L1805 I_ Q_ R_ [J_];
```

功能：在单节间（非运动过程中）触发讯号，触发前后可以不为移动单节。

引数：

| 引数 | 说明 | 数值范围 |
| --- | --- | --- |
| `I` | 讯号源 | 1=R-bit，2=O-bit，3=A-bit |
| `Q` | 讯号号码 | R-bit：0~65535 + 0~31（旧版 0~15）；O-bit：0~511；A-bit：0~511 |
| `R` | 讯号状态 | 0=LEVEL LOW，1=LEVEL HIGH |
| `J` | 讯号维持时间，可省略 | 0~3600000 ms；>0 时维持后输出相反极性，=0 或省略时输出一次 |

规则：

- 确认讯号不与 PLC 冲突。
- 引数超出范围触发 `COR-345`。
- `G10 L1803` 与 `G10 L1805` 指令使用过多触发 `COR-346`。
- 可用 `G10 L1801` 取消正在输出的讯号，原状态维持不变。
- 可用 `G10 L1802` 等待讯号维持时间结束后再执行后续。
- 最低版本：10.118.28G、10.118.33 及之后。

##### G10 L1802 - 等待 MACRO IO 指令完成

状态：已确认 / 部分版本差异

来源：CF [G10L1802-等待 MACRO IO 指令完成](https://syntecclub.atlassian.net/wiki/spaces/TechManual/pages/44279936/G10L1802-+MACRO+IO)。

```iecst
G10 L1802 P_;
```

| 引数 | 说明 | 数值范围 |
| --- | --- | --- |
| `P` | 等待指定的 `G10 L1803` / `G10 L1805` 讯号维持时间结束 | 已发出的 L1803/L1805 讯号编号 |

规则：

- 配合 `G10 L1803` / `G10 L1805` 使用：当某个 L1803/L1805 设定了维持时间 `J`，下 `G10 L1802 P_` 后核心会等待该讯号维持时间结束才执行下一单节。
- 若该讯号未发出或维持时间已结束，`G10 L1802` 立即返回。
- 引数格式错误触发 `COR-345`；指定讯号未发出失败触发 `COR-348`。
- 最低版本：10.118.28G、10.118.33 及之后。

静音模式支援差异（CF [44279936 §静音模式支援] 证实）：

| 版本族 | 静音模式（`#1500=1` 或 `#1820` 非零）下 L1802 行为 |
| --- | --- |
| `10.118.28G` / `10.118.33` 起至 `10.118.40R` 之前 | 支援，`G10 L1802` 等待行为正常 |
| `10.118.40R` / `10.118.42R` / `10.118.48C` / `10.118.50` 及之后 | 静音模式下不支援，L1802 在静音上下文中可能立即返回或被忽略；需在非静音模式下使用 |

插件策略：

- 静态可判 `#1500 := 1;` 或 `#1820` 非零后出现 `G10 L1802 P_`，插件可加 warning 提示版本门槛差异；超过 `10.118.40R` 版本族静音模式下不支援。
- `G10 L1802 P_` 中 `P` 必须指向已发出的 L1803/L1805 讯号编号；不在静态扫描范围做强校验。

#### 8.4.4 G10 L1803/L1805 共通限制与 Q 引数范例

Q 引数范例：

| 讯号 | 写法 |
| --- | --- |
| A310 | `I3 Q310` |
| R49 | `I1 Q4900` |
| R50.1 | `I1 Q5001` |
| O1 | `I2 Q1` |

共通限制：

- 不支援：手轮模拟、加工中调 Override、RTCP 且 Pr3054=1、Pr411=1 G00 独立运动、中断型副程序。
- `M30` / 重置 / 急停均会清除所有 R/O/A 点状态控制；暂停会暂停，Cycle Start 恢复。
- 10.118.40R、10.118.42R、10.118.48C、10.118.50 及之后版本静音模式不支援。

范例：

```iecst
G01 X10.;
G10 L1805 I2 Q50 R1 J1000;
G10 L1000 P1025 R200;
```

说明：X 走到 10 mm 时触发 O-50 ON，同时写入 R1025=200；O-50 触发 1 秒后返回原状态。

```iecst
G90 G00 X0;
G01 X10. F3000
G10 L1803 I2 Q50 P3. R1 J1000 K300;
G01 X20.;
M30;
```

说明：距上单节起点 3 mm，提前 300 ms，O50 ON 维持 1000 ms。

#### 8.4.5 G10 L1810 - 设定讯号条件

```iecst
G10 L1810 I_ Q_ R_ [J_];
```

功能：设定 `G10 L1820` 讯号等待的条件。

| 引数 | 说明 | 数值范围 |
| --- | --- | --- |
| `I` | 讯号源 | 1=R-bit，2=I-bit，3=A-bit |
| `Q` | 讯号号码 | R-bit：`[0~65535][0~31]`；10.118.34 及之前版本 R-bit 范围为 0~15；I-bit/A-bit：`[0~511]` |
| `R` | 讯号状态 | 0=LEVEL LOW，1=LEVEL HIGH |
| `J` | 讯号维持时间，可省略 | 0~3600000 ms；省略或设成 0 代表讯号不需要过滤杂讯 |

Q 引数范例：

| 讯号 | 写法 |
| --- | --- |
| A310 | `I3 Q310` |
| R49 | `I1 Q4900` |
| R50.1 | `I1 Q5001` |
| I1 | `I2 Q1` |

注意事项与使用范例请参考 `G10 L1820`。

#### 8.4.6 G10 L1820 - 等待讯号条件成立

```iecst
G10 L1820 [P_] [K_];
```

功能：需搭配 `G10 L1810` 设定讯号条件，再由 `G10 L1820` 执行等待。

| 引数 | 说明 | 数值范围 |
| --- | --- | --- |
| `P` | 走到上一单节终点后，讯号等待时间 | 0~3600000 ms |
| `K` | 逾时处理方式 | 0 或省略：发出警报 `COR-338`；1：不继续等待，执行下一单节 |

`P` 引数说明：

- 单位为 ms，只支援整数，不支援负值或小数。
- 不受执行时 Override 影响，例如 `P5000` 不管 Override 怎么改都是等 5 秒。
- 省略 `P`：走到单节终点后停下等待，直到讯号条件成立才继续执行，忽略 `K` 引数。
- `P=0`：走到单节终点后立刻执行 `K` 引数的逾时处理方式。
- `P>0`：走到单节终点停下，等待 `P` 时间后若条件仍未成立，执行 `K` 引数逾时处理。

规则：

- `G10 L1810/L1820` 须单独编写在一行，不要和移动单节编写在同一行。
- 设定完 `G10 L1810` 后，只有执行到 `G10 L1820` 才会进行等待。
- 执行完第一个 `G10 L1820` 后，若需执行第二个，须重新设定 `G10 L1810`，不能沿用之前的条件。
- 设定多个 `G10 L1810` 时，所有讯号条件都必须成立才算完成等待。
- 多个 `G10 L1810` 共用同一个 `G10 L1820` 的等待时间和逾时处理方式。
- `G10 L1810` 一定要设定 `I/Q/R` 引数，缺少任一触发 `COR-345`。
- `G10 L1810` 的 `I/Q/R/J` 引数超出范围、负值或小数触发 `COR-345`。
- 每个 `G10 L1820` 最多支援 5 个 `G10 L1810` 条件，超过触发 `COR-335`。
- `G10 L1820` 的 `P/K` 引数超出范围、负值或小数触发 `COR-345`。
- 加工档必须先编写过至少一个 `G10 L1810`，否则触发 `COR-337`。
- `G10 L1820` 必须编写在想要等待的单节下方，除 `G10 L1810` 外中间不可插入其他指令。
- 搭配轴向交迭（overlapping）使用时，若交迭距离超过且条件成立，移动单节会和下一单节交迭。
- 在轴向交迭单节后仅能使用一个 `G10 L1820`，不可重复编写。
- 急停、`M30`、重置会取消等待。
- 暂停或单节停止（`M00/C40`）会暂停等待，时间暂停计时不清除，重新执行后继续。
- 手轮模拟反转时无效；手轮模拟反转至该组任何 `G10 L1810` 或 `G10 L1820` 指令再正转，等待讯号条件皆成立。
- 不支援 `G05` 高精轨迹控制（HPCC）、平滑刀尖点控制（STCP）、高速预解功能。
- 不支援 `G53` 接 `G53` 轴向交迭中使用。
- `COR-334` 警报支援 10.118.34 及之前版本；10.118.35 及之后变更为 `COR-345`。
- 10.118.40R、10.118.42R、10.118.48C、10.118.50 及之后版本静音模式不支援。
- 支援版本：10.118.19 及之后。

范例：

```iecst
G00 X0. Y0.;
G00 X50.;
G10 L1810 I2 Q1 R0;       // 条件1：I1 = OFF
G10 L1810 I2 Q2 R1;       // 条件2：I2 = ON
G10 L1810 I2 Q3 R1;       // 条件3：I3 = ON
G10 L1820 P5000;          // 等待5秒，逾时跳警报
G00 Y50.;
M30;
```

```iecst
G00 X0.;
G10 L1000 P51 R1;         // 设定 R51 = 1
G10 L1810 I1 Q5100 R1 J100;  // 条件：R51.0 = ON
G10 L1820 P1000 K1;       // 等待1秒，逾时不跳警报继续执行
G00 X50.;
M30;
```

#### 8.4.7 G10 通讯相关指令

##### G10 L1021 - 发送 ENIP 通讯命令

```iecst
G10 L1021 [Q_] P_ S_ C_ I_ [A_] [X_] [Y_] [U_] [V_];
```

功能：发送 EtherNet/IP 通讯命令。

| 引数 | 说明 | 数值范围 |
| --- | --- | --- |
| `Q` | 网卡号，可省略 | 省略或 0：自动搜寻所有网卡 |
| `P` | 站号，即设备 IP 最后一码 | 子网路遮罩须为 255.255.255.0 |
| `S` | Service 服务代码 | 0~255，十进制 |
| `C` | Class 类别 ID | 0~65535，十进制 |
| `I` | Instance 实例 ID | 0~2^31-1，十进制 |
| `A` | Attribute 属性 ID，可省略 | 0~65535，不需要时可省略或给 0 |
| `X` | Request Data 阵列起始 R 值位置 | 例如 `X20000` 代表从 R20000 开始 |
| `Y` | Request Data 阵列资料长度 | 每个 R 值=1 Byte，范围 0~255 |
| `U` | Response Data 阵列起始 R 值位置 | 起始 R 值的数值代表返回资料长度 |
| `V` | Response Data 阵列最大储存长度 | 须 >=1，含 `U` 指定的起始 R 值 |

规则：

- 若不同网域下有相同站号（如网卡 1 的 192.168.1.2 和网卡 2 的 192.168.10.2 都是第 2 站），需透过 `Q` 指定；省略 `Q` 则优先搜寻第一张网卡。
- Request Data 范例：传送 3 bytes ASCII `{'A','B','C'}` = `{65,66,67}`，从 R20000 开始写 `X20000 Y3`。
- Response Data 范例：规划 R20010~R20019 共 10 个 R 值，写 `U20010 V10`。回传 `{'D','E','F'}` = `{68,69,70}` 时，R20010=3，R20011=68，R20012=69，R20013=70。
- 使用前请先确认 EtherNet/IP 设备开机连线正常。
- Macro 引数格式或范围错误触发 `COR-350`；网卡号/站号错误或找不到设备触发 `OP-079` + `COR-351`；Service/Class/Instance/Attribute/Request Data 不支援触发 `COR-351`；Response Data 长度超过最大储存长度 `V` 触发 `COR-352`。
- 加工中手轮模拟时，手轮反转无法回退至此指令。
- 支援版本：10.118.22A、10.118.23 以上。

##### G10 L1022 - EtherCAT 物件字典读写功能

```iecst
G10 L1022 P0 A_ I_ U_ R_;
G10 L1022 P1 A_ I_ U_ B_ V_ E_;
```

模式：

| 模式 | 格式 |
| --- | --- |
| 读取 | `G10 L1022 P0 A_ I_ U_ R_` |
| 写入 | `G10 L1022 P1 A_ I_ U_ B_ V_ E_` |

引数：

| 引数 | 说明 |
| --- | --- |
| `P` | 0=读取，1=写入 |
| `A` | 轴号 |
| `I` | 物件 Index，支援十进制或十六进制，十六进制格式如 `"2000h"` |
| `U` | 物件 SubIndex，支援十进制或十六进制 |
| `R` | 读取结果存放的 R 值位置，读取模式使用 |
| `B` | 写入资料的 BitLength，限制为 8、16、24、32 |
| `V` | 写入的数值，支援十进制或十六进制 |
| `E` | 执行结果存放的 R 值位置，写入模式使用 |

规则：

- 读取结果：指定 `R` 值存放读取到的数值，`R+1` 存放 Error Code。写入结果：`E` 值存放 Error Code。
- Error Code：0=失败，1=成功，2=执行中，3=不支援此物件。
- 读取模式支援所有 ESI 有定义的物件；写入模式只支援部分 ESI 有定义的物件。
- `I_`、`U_`、`V_` 使用十六进制字串格式错误触发 `COR-023`。
- 除 `I_`、`U_`、`V_` 外，所有引数须以整数输入，否则触发 `COR-146`。
- 一次只执行一个物件读写命令，多轴群同时使用需排队等待；与 EtherCAT Debug Mode 同时触发时亦需排队。
- 写入成功后不会同步更新总线参数页面的数值。
- 执行时自行挡预解，减速至零后才做读写动作。
- 使用 `@` 变数读取结果时，需在读取前加入 `WAIT()`。
- 支援版本：10.118.40H、10.118.45 及以后版本。
- `B` 引数限制（8/16/24/32）自 10.120.32V、10.120.42J、10.120.51、12.0.7 起。

##### G10 L1900/L1901/L1910/L1911 - MODBUS 通讯指令

格式总览：

| 类型 | TCP | RS485 |
| --- | --- | --- |
| 读取一至多笔 | `G10 L1900 C3 I_ A_ Q_ K_` | `G10 L1910 C3 I_ A_ Q_ K_` |
| 写一笔资料 | `G10 L1900 C6 I_ A_ X_` | `G10 L1910 C6 I_ A_ X_` |
| 自定义封包 | `G10 L1901 P_ R_ Q_ [K_]` | `G10 L1911 P_ R_ Q_ [K_]` |

所有引数皆为十进制。

引数：

| 指令 | 引数 | 说明 |
| --- | --- | --- |
| 读取 `C3` | `C` | Function Code = 3 |
| 读取 `C3` | `I` | Slave 站号 |
| 读取 `C3` | `A` | Slave 装置资料读取起始位址 |
| 读取 `C3` | `Q` | 回传资料存放的起始 R 值 |
| 读取 `C3` | `K` | 回传资料的数量（读取数量） |
| 写入 `C6` | `C` | Function Code = 6 |
| 写入 `C6` | `I` | Slave 站号 |
| 写入 `C6` | `A` | Slave 装置资料写入位址 |
| 写入 `C6` | `X` | 写入的资料，范围 0~65535 |
| 自定义封包 | `P` | 自定义资料参考的起始 R 值 |
| 自定义封包 | `R` | 自定义资料参考的 R 值数量，不可超过 254 |
| 自定义封包 | `Q` | 回传资料存放的起始 R 值 |
| 自定义封包 | `K` | 回传资料的数量上限，可省略，省略等效 `K0`，不填入回传资料 |

执行流程与设定：

- 封包收送期间光标停在该行，收到回传资料并成功后才继续下一行。
- 通讯错误时跳警报并停在该行，所有轴群停止。
- 执行完毕成功后触发 R608.10。
- 用 `#` 或 `@` 变数读取回传值，需在指令后加 `WAIT()` 挡预解。
- RS485：设定 R5040 Modbus 通讯埠，SUPER/10/20/200 使用 R5040=3，eHMC/3/6/11/21 使用 R5040=2；并设定 Pr3941~Pr3950（COM2）或 Pr3961~Pr3970（COM3）。
- TCP：设定 Pr3971 Modbus TCP 通讯逾时异常时间。

注意事项：

- 请勿与 Modbus PLC 同时执行。
- 指令格式错误触发 `COR-356`；封包内容错误触发 `COR-357`；逾时且 Retry 后仍无回传触发 `COR-358`；回传封包异常触发 `COR-359`。
- 不用特别处理 R608.0，指令会自行触发传送。
- 逾时时间：RS485 为 Pr3950/Pr3970，TCP 为 Pr3971。
- 手轮反转无法回退至此指令。
- `Q` 引数指定的 R 值超过范围无法储存；`P` 引数超过范围使用预设值 0，可能导致 `COR-358`。
- 插件已检查 `L1900/L1901` 的语法形态、静态整数性，以及 `X`、`P/Q`、自定义封包 `R` 的已确认范围；站号、装置地址与 `K` 的未明确上限不做臆测校验。
- 支援版本：10.118.28D、10.118.31 及以后。

##### Modbus 实务 SOP

状态：已确认 / 个人空间笔记

来源 †：[新代控制器 Modbus 通讯SOP（Macro语法与PLC实现）](https://syntecclub.atlassian.net/wiki/spaces/~C1720/pages/483251200/Modbus+SOP+Macro+PLC)（个人空间 张颖 2026-01-09）。为现场 SOP，非官方 LTP/TechManual 规格；引用前需 user 确认或对照实机验证。

接线（RS485 串行）：

| DB9 引脚 | RS485 信号 |
| --- | --- |
| DB9-7 | +（A） |
| DB9-6 | −（B） |
| DB9-5 | GND（长距离建议接入） |

适用新代全系列 SUPER/10/20/200/eHMC/3/6/11/21 等。TCP 走标准 RJ45/双绞线，不另列接线表。

32 位拆位公式（CF 个人空间 SOP 证实）：

- Modbus RTU 与 TCP 以 16-bit Register 为最小存取单位。
- 读写 32-bit 整数占 2 个 R 寄存器（高 16 bit + 低 16 bit）。
- 高位 / 低位寄存器顺序由下层 PLC 实作决定；宏程序需在调试期确认 SOP 偏好排列，不要假定跨机台一致。

```iecst
// 假设 L1900 C3 I1 A0 K2 读取了 2 笔 16-bit，存入 R1000 / R1001
// 若 SOP 设定 high = R1000、low = R1001，32-bit 整数值 = R1000 * 65536 + R1001
G10 L1900 C3 I1 A0 Q14000 K2;
WAIT();
#1 := @14000 * 65536 + @14001;
```

Coil（软位元件）：

- Modbus Function Code 1（读）、5（写单 Coil）、15（写多 Coil）用于 Coil 区段读写；一个 16-bit R 寄存器承载 16 个连续 Coil 位。
- 宏端通过 `@(10000 + R 号)` 访问对应 R 寄存器，再用位元运算或 `MOD` / `DIV` 取出特定位元。

连续读取 4 笔寄存器示例：

```iecst
%@MACRO
// 从站号 1、起始位址 100 读取 4 笔 16-bit 寄存器，存入 R14000 ~ R14003
G10 L1900 C3 I1 A100 Q14000 K4;
WAIT();
#1 := @14000;
#2 := @14001;
#3 := @14002;
#4 := @14003;
M99;
```

写入拆位后的 32-bit 数值示例：

```iecst
// 把十进制 70000 拆为 high=1、low=4464（示例：high = 70000 / 65536，low 为余数）
#10 := 70000;
#11 := #10 / 65536;        // high
#12 := #10 - #11 * 65536;   // low
G10 L1000 P14000 R#11;     // 写 high
G10 L1000 P14001 R#12;     // 写 low
```

注意事项：

- Modbus 寄存器不应与 PLC `R615` MST 请求旗标 / `R629` PLC 轴 M 码占用的 R 区段重叠；同一轴群内使用时需检查区段是否冲突。
- `WAIT()` 是必要环节：`L1900` / `L1910` 后需 `WAIT()` 挡预解，不能依赖 `SYSDATA` 拼接中间值。
- 逾时与 Retry 阈值仅能通过 `Pr3950` / `Pr3970`（RS485）、`Pr3971`（TCP）调，不能从宏端缩短。
- 长距离 RS485 现场布线应使用屏蔽双绞线，并保证 DB9-5 GND 接入；信号干扰属于电气现场问题，参数调整无法 bypass。

插件策略：

- 静态可判 `L1900` / `L1910` `C3` 模式下 `K` 应不超过 125（Modbus 标准上限 125 个 16-bit）；后续可加，本批不动。
- `L1901` / `L1911` 自定义封包的 `R` 个数上限 125，超出触发 `COR-356`。

### 8.5 STITCHON / STITCHOFF

状态：已确认 / 部分已落地

```iecst
STITCHON S_ Q_ [L_ / K_] E_;
STITCHOFF;
```

规则：

- `L` 与 `K` 二选一，只能择一输入。
- `L` 范围 1~10000 ms，仅整数，不可加小数点。
- `K` 范围 0.001~100 mm。
- `E` 范围 0.001~100 mm。
- 区间内存在禁用指令时可能报 RBT-115。
- 插件已检查：`L/K` 同时出现报 error；`L/K` 都省略报 warning；`L` 带小数点报 error。
- 插件已检查：`STITCHON`~`STITCHOFF` 区间内出现 `MOVJ`、`USERCOR`、`SHIFTON/OFF`、`OBJCORON/OFF/CLEAR`、`SYNCOUT`、`WEAVEON/OFF` 或带 `SKIP` 的 `MOVL/MOVC/INCMOVL` 报 error；`M96` 报 warning。
- `WAITSYNC/ENDSYNC` 也属于禁用项。
- 相关警报：`RBT-114`、`RBT-115`、`RBTWRN-010`。

### 8.6 WEAVEON / WEAVEOFF

状态：已确认 / 部分已落地

```iecst
WEAVEON P_;
WEAVEON E_ Q_ K_ L_ [R_] [I_];
WEAVEOFF;
```

规则：

- 两种语法互斥，不可混用。
- `P` 范围 1~50，整数。
- `E` 范围 0~50 mm。
- `Q` 范围 0.1~5 Hz，需小数形式，例如 `Q1.0`。
- `K` 范围 0~180 deg。
- `L` 范围 0~1000000 ms，不可加小数点。
- `R/I` 范围 0~1，不可加小数点。
- WEAVEON 区间内存在禁用指令时可能报 RBT-322。
- 插件已检查：`P_` 语法不可与 `E/Q/K/L/R/I` 混用。
- 插件已检查：`WEAVEON`~`WEAVEOFF` 区间内出现 `MOVJ` 或 `STITCHON/OFF` 报 error；`M96` 报 warning。
- `WAITSYNC/ENDSYNC` 与手轮模拟倒退也属于禁用项。
- 相关警报：`RBT-321`~`RBT-327`、`COR-064`、`RBTWRN-010`、`RBTCRI-011`。
- 插件已提示：细节语法中的 `Q` 频率建议使用小数形式，例如 `Q1.0`。

### 8.7 G04.102

状态：已确认 / 部分待确认

指令功能：等待指定的时间。官方页面标题为「G04.102-等待计时指令」，英文版为 G04.102-Wait Time Command。

```iecst
G04.102 I_ X_;
```

引数：

| 引数 | 说明 | 可省略 | 范围/单位 |
| --- | --- | --- | --- |
| `I` | 计时源；`1` 表示以整数 Int 计时，`2` 表示以 `@` 变量计时 | 不可 | `1` 或 `2` |
| `X` | 计时时间 | 不可 | 同语系其他时间引数（SWAITSIG `L/T`、G10 L1820 `P`）一致采用 `ms` |

注：CF 未能在简略说明中明确列出 `X` 的具体数值范围。基于 LTP 官方页面仅为简略说明，插件维持当前补全/hover，不对 `X` 上限做强静态诊断。

支援版本：`10.118.40G`、`10.118.44` 及之后版本。

来源：[LTP G04.102-等待计时指令](https://syntecclub.atlassian.net/wiki/spaces/LTP/pages/64818005/G04.102-)。

### 8.8.1 G192.1 / G192.2

状态：已确认 / 静态范围已落地

```iecst
G192.1 P_ Q_ R_ [E_] ;
G192.2;
```

规则：

- `P` 范围 0~20。
- `Q` 范围 0~65530。
- `R` 范围 1~2。
- `E` 范围 -10~10，可省略。
- 区间内有禁用指令时报 RBT-123。
- 仅支援六关节机器人。
- 禁止使用 `MOVJ`、`INCMOVJ`、`MOVC`、`SWAITSIG`、`SYNCOUT`、`WEAVEON/OFF`、`WAITSYNC/ENDSYNC`。

### 8.9 WAITSYNC / ENDSYNC

状态：已确认 / 静态范围已落地

```iecst
WAITSYNC P_ [L_];
ENDSYNC P_;
```

规则：

- 履带追踪开启/关闭指令。
- `P` 为追随履带编号，范围 1~4。
- `L` 为同期距离，单位 mm，表示追上履带速度所需距离。
- 追踪中禁止使用 `MOVJ`、切换用户坐标系、`G04.1`、M 码指令、`SHIFTON`。
- 相关警报：`RBT-251`~`RBT-260`。

### 8.10 CIRMODE

状态：已确认 / 静态范围已落地

```iecst
CIRMODE P_;
```

规则：

- 圆弧姿态控制模式。
- `P0`：沿圆弧路径变化，预设值。
- `P1`：沿工件坐标变化。
- `P2`：经过圆弧中间点。
- 超出范围触发 `RBT-105`。

### 8.11 其他机器人应用指令

状态：待落地

| 指令 | 说明 |
| --- | --- |
| `M96` / `M97` | 中断型副程序功能 |
| `G142.2` / `G142.3` | 路径偏移 |
| `G143.1` | 执行回退程序 |
| `G141.2` | 奇异点回避功能设定 |
| `G144.1` / `G144.2` | 碰撞保护功能 |
| `G144.103` / `G144.104` | 动力学防撞灵敏度设定与复位 |
| `G145.1` / `G145.2` | 软浮动功能 |
| `G04.102` | 等待计时指令替代语法 |
| `G10.101` | 运算指令替代语法 |
| `G12.101` | 周边控制指令替代语法 |

### 8.12 机械手臂支援的 CNC 标准 G 码

状态：已确认

| G 码 | 功能名称 | 支援版本 |
| --- | --- | --- |
| `G04` | 暂停指定时间 | 基础支援 |
| `G04.1` | 双程序同步指令 | 基础支援 |
| `G09` | 确实停止检测 | 基础支援 |
| `G10 L1000` | PLC R 暂存器写入 | 基础支援 |
| `G10 L1021` | 发送 Ethernet/IP 指令通讯命令 | 10.118.22A、10.118.23 起 |
| `G10 L1022` | EtherCAT 物件字典读写功能 | 10.118.40H、10.118.45 起 |
| `G10 L1900` / `G10 L1901` | 发送 Modbus-TCP 指令通讯命令 | 10.118.28D、10.118.31 起 |
| `G10 L1910` / `G10 L1911` | 发送 Modbus-RS485 指令通讯命令，81R 手持盒硬件不支援 | 10.118.28D、10.118.31 起 |
| `G10 L1805` | O/R/A-bit 单节间触发输出，功能与 `SYNCOUT` 相似 | 10.118.28G、10.118.33 起 |
| `G10 L1810` / `G10 L1820` | 讯号条件设定与等待 | 10.118.19 起 |
| `G65` | 单一宏程序呼叫 | 基础支援 |
| `G66` | 模式宏程序呼叫，每个运动单节结束时呼叫 `P` 指定副程序 | 基础支援 |
| `G66.1` | 模式宏程序呼叫，任何单节结束时呼叫 `P` 指定副程序 | 基础支援 |
| `G67` | 模式宏程序呼叫取消 | 基础支援 |
| `G90` | 绝对位置输入方式 | 基础支援 |
| `G91` | 相对位置输入方式 | 基础支援 |

机器人专用指令替代 G 码：

| 替代 G 码 | 对应机器人语法 | 功能 |
| --- | --- | --- |
| `G01.102` | `MOVL` | 末端直线运动 |
| `G01.101` | `INCMOVL` | 增量末端直线移动 |
| `G53.102` | `MOVJ` 第一/第二语法 | 关节运动 |
| `G53.101` | `INCMOVJ` 第一/第二语法 | 增量关节移动 |
| `G04.101` | `SWAITSIG` | 等待讯号不减速 |
| `G04.103` | `G4.16` | 等待多讯号不减速 |
| `G04.102` | - | 等待计时指令 |
| `G10.101` | - | 运算指令 |
| `G11.101` | `SYNCOUT` 非运动中输出 | 输出指令 |
| `G11.102` | `G10 L1805` 包装 | 输出信号 |
| `G11.103` | `SYNCOUT` 运动中输出 | 进阶输出 |
| `G12.101` | - | 周边控制指令 |
| `G43.16` | - | 设定工具坐标 |
| `G52.101` | `OBJCORON` | 工件坐标系偏移开始 |
| `G68.18` | - | 设定用户坐标系 |
| `G141.2` | - | 奇异点回避功能设定 |
| `G142.101` | - | 三点设定用户坐标系 |
| `G142.102` | - | 六点设定用户坐标系 |
| `G142.2` / `G142.3` | - | 路径偏移 |
| `G143.1` | - | 执行回退程序 |
| `G144.1` / `G144.2` | - | 碰撞保护功能 |
| `G144.103` / `G144.104` | - | 动力学防撞灵敏度设定与复位 |
| `G145.1` / `G145.2` | - | 软浮动功能 |
| `G192.1` / `G192.2` | - | 末端跟踪 |

备注：

- `G10 L1803` 为 CNC MACRO IO TYPE-1 运动单节内触发指令；机械手臂支援 CNC 标准 G 码清单中明确列入的是 `G10 L1805`，`G10 L1803` 需依控制器版本与机型确认。
- `M00/M01/M30/M98/M99` 属于 CNC 标准辅助码，机器人系统基础支援，未在 G 码清单单独列出。
- `M96/M97` 为机器人专用应用指令。

### 8.13 机器人指令互斥关系

状态：已确认 / 部分已落地

| 生效中指令 | 禁止使用指令 | 警报 |
| --- | --- | --- |
| `STITCHON/OFF` | `MOVJ`、`USERCOR`、`SHIFTON/OFF`、`OBJCORON/OFF/CLEAR`、`SYNCOUT`、`WEAVEON/OFF`、带 `SKIP` 的 `MOVL/MOVC/INCMOVL`、`M96`、`WAITSYNC/ENDSYNC` | `RBT-115` |
| `WEAVEON/OFF` | `MOVJ`、`STITCHON/OFF`、`M96`、`WAITSYNC/ENDSYNC`、手轮模拟倒退 | `RBT-322` |
| `WAITSYNC/ENDSYNC` | `MOVJ`、切换用户坐标系、`G04.1`、M 码、`SHIFTON` | `RBT-257` |
| `G192.1/G192.2` | `MOVJ`、`INCMOVJ`、`MOVC`、`SWAITSIG`、`SYNCOUT`、`WEAVEON/OFF`、`WAITSYNC/ENDSYNC` | `RBT-123` |
| `SHIFTON/OFF` | `WAITSYNC/ENDSYNC` | `RBT-118` |

### 8.14 机器人警报速查

状态：已确认 / 部分待落地

| 警报 | 说明 |
| --- | --- |
| `RBT-102` | 加速度或减速度倍率超出范围 |
| `RBT-103` | 不明确的平滑指令，`PL/PQ/PR` 同时使用 |
| `RBT-104` | 不合法的等待讯号格式 |
| `RBT-105` | 圆弧姿态控制模式引数超出范围 |
| `RBT-110` | 不合法的进阶输出指令 |
| `RBT-114` | 不合法的连续脉冲输出指令 |
| `RBT-115` | 连续脉冲输出功能不支援此指令 |
| `RBT-116` | 不合法的点位偏移指令 |
| `RBT-117` | 点位偏移范围内有不明确的末端坐标指令 |
| `RBT-118` | 点位偏移范围内有不支援指令 |
| `RBT-119` | 不合法的跳脱条件指令 |
| `RBT-122` | 不合法的末端跟踪指令 |
| `RBT-123` | 末端跟踪不支援此指令 |
| `RBT-153` | 等待不减速指令讯号等待逾时 |
| `RBT-154-2` | 运动指令后下过多 `SWAITSIG` |
| `RBT-251`~`RBT-260` | 履带追踪相关警报 |
| `RBT-321` | 不合法的摆动指令 |
| `RBT-322` | 摆动功能不支援此指令 |
| `RBT-323` | 摆动停止时间太长 |
| `RBT-324` | 不可行的摆动参数 |
| `RBT-325` | 机械手臂无法达成摆动 |
| `RBT-326` | 摆动中不可手轮模拟反转 |
| `RBT-327` | 摆动生效期间下太多指令 |
| `COR-064` | 不合法号码 P |
| `RBTWRN-010` | 中断型副程序触发无效 |

### 8.15 G193.110

状态：待确认 / CF 未找到独立规格页面

```iecst
G193.110 I_ Q_ R_ J_;
```

说明：

- CF 目前未找到 `G193.110` 的独立正式规格页面；「语法指令规格」总表也未列出该指令。
- CF 检索出现的 `G193.110` 多见于激光/打标工艺包上下文，并非官方语言手册，不能当作正式语法依据。
- 在取得 LTP 或 TechManual 独立规格页面前，本项保持 hover/补全以外的待确认状态，不纳入强诊断。

### 8.16 M96 / M97 - 中断型副程序呼叫

状态：已确认 / 部分待落地

来源：[LTP 语法指令规格](https://syntecclub.atlassian.net/wiki/spaces/LTP/pages/64815294)与历史总表 [285313312](https://syntecclub.atlassian.net/wiki/spaces/~1455/pages/285313312)。

```iecst
M96 P_; // 启用中断型副程序
// ...主程序...
M97;     // 停止中断型副程序
```

语义：

- `M96 P_` 指定一个副程序（如 `O8100`），在主程序运行期间被外部跳脱訊号触发上升沿时进入该副程序执行一次；副程序内部必须以 `M99` 返回。
- `M97` 停用中断型副程序，后续外部信号不再激起动作。
- 支持版本：`10.118.0B` 及之后。
- 与 `WEAVEON`/`WEAVEOFF` 、 `WAITSYNC`/`ENDSYNC` 区段互斥，触发 `RBT-322`、`RBT-257`、或 `RBTWRN-010` 提示未被触发。

插件策略：

- `M96` 后续须出现 `M97` 配对，否则插件可加 warning（仅在静态扫描可达）。
- `M96 P_` 中 `P` 必须指向已有副程序号，动态诊断不在静态范围验证。

### 8.17 G196 - 电弧跟踪

状态：已确认

来源：[LTP 电弧跟踪应用手册](https://syntecclub.atlassian.net/wiki/spaces/LTP/pages/64819227)。

| 子指令 | 功能 |
| --- | --- |
| `G196.1 T_ S_ H_ P_` | 启动电弧跟踪 |
| `G196.2` | 关闭电弧跟踪，无引数 |

规则：

- `P` 接受 `0 ~ 10`，超出触发 `RBT-340` 不合法的电弧跟踪指令。
- 使用 `G196` 电弧跟踪指令时必须搭配摆振指令 `WEAVEON`/`WEAVEOFF`，否则触发 `RBT-344` 电弧跟踪需搭配摆振指令。
- 电弧跟踪中不可手轮模拟反转，触发 `RBT-343`。
- 灵敏度 `T` 设为 0 时不进行左右跟踪，但指令格式仍需合法。

插件策略：

- `G196.1` 提取 `T`、`S`、`H`、`P` 引数进行静态范围检查；`P` 越界后续可报 error。
- `G196.1` 指令索引静态可判是否在同一区段出现 `WEAVEON`，未出现可加 `warning` 提示可能触发 `RBT-344`。

### 8.18 G144.103 / G144.104 - 动力学防撞灵敏度

状态：已确认

来源：[LTP G144.103/G144.104-动力学防撞灵敏度设定与复位](https://syntecclub.atlassian.net/wiki/spaces/LTP/pages/64822206)。

```iecst
G144.103 P_;  // 设定动力学防撞灵敏度
G144.104;      // 复位防撞灵敏度（同面板设定）
```

可用场景（示例）：

- 机床取放应用中，手臂抓着工件被卡盘稍微拖动可能误触碰撞保护，此时调低灵敏度让警报较不易被触发。
- 处理较精密工件时调高灵敏度预防碰撞发生。

插件策略：

- `G144.104` 不接受引数；`G144.104 P_` 可加 error 提示。
- `G144.103 P_` 的 `P` 范围在 CF 页面未明确给出，插件可只做存在性诊断不做强范围诊断。

### 8.19 G145.1 / G145.2 - 软浮动

状态：已确认

来源：[G145.1/G145.2 软浮动功能指令](https://syntecclub.atlassian.net/wiki/spaces/LTP/pages/331646776/G145.1+G145.2-)。

- `G145.1` 指定软浮动（柱状坐标 soft-float）。
- `G145.2` 指定关节软浮动（关节 soft-float）。
- 指令生效触发 `RBT-210` 软体警报/警告，提示调用者该指令将进入软浮动模态。
- 支持版本：`10.120.32W` 、`10.118.42K` 、`10.118.52` 、`12.0.7` 及之后。

插件策略：

- `G145.1` / `G145.2` 不接受带引数形式；插件可识别并补充 hover。

### 8.20 G193.101 - 客制激光焊 MACRO（产机附录）

状态：已确认

来源：[AUTO 客製 MACRO（产机激光焊）](https://syntecclub.atlassian.net/wiki/spaces/AUTO/pages/814553083/MACRO)。

```iecst
G193.101 P_ T_ R_ S_ A_ B_ C_ D_ E_ H_ I_ J_ K_ W_ Q_;
```

引数表见官方页面：

| 引数 | 语义 |
| --- | --- |
| `P` | 程序号 |
| `T` | 线角微焦偏移 |
| `R` | 填充量参数 |
| `S` | 主轴速度 |
| `A/B/C/D/E/H/I/J/K/W/Q` | 其他调焦 / 偏移参数，见 CF 页面全表 |

适用性：产机激光焊客制 MACRO，在新代 LTP 机器人上使用。同类 `G193.110` 在官方 LTP 独立页未列出，不作 LTP 诊断参考。

插件策略：

- `G193.101` 静态诊断可识别为客制 MACRO 引动语义，提供 hover + snippet，不做强型态检查。
- `G193.101` 所有引数为直接引数（不是 `X=` 型赋值型）。

### 8.21 G903 - 断刀监控实作

状态：已确认

来源：[SZJS G903 V1.0 指令实作记录](https://syntecclub.atlassian.net/wiki/spaces/SZJS/pages/803504764)。

为产机应用层 MACRO + PLC 复合实作指令，并非语言层 G 码。核心在 MACRO 中通过使用 `G10 L1900` / `L1901` 存取 PLC R 寄存器，随后在 PLC 中按 `R6998` / `R6999` / `R7010` / `R7012` 进行自学习与输出。项目本文件的 MACRO 源代码不在 CF 页面公开。

插件策略：

- 识别 `G903` 为客制 G 码，不做静态诊断；使用者需引用《G903 V1.0》实作记录。

# 迭代优化计划

> 主流程已在 v4.0.0 (2026-09-22) 收口. 本文件是后续接续工作的简明交接清单; 详细已完成历史节点见 `CHANGELOG.md`, 详细实施参考 `docs/R1.2-实施方案.md` (Rollback 用).

## 当前架构 (v4.0.0 起)

插件分析后端**硬切为 rust-wasm 唯一**:

- 12 个 JS 分析器模块已从仓库删除 (analysisCore/lexer/validator/robotValidator/controlFlowValidator/functionArgumentValidator/diagnosticRules/diagnosticFactory/formatter/navigationSymbols/navigationIndex/statementClassifier), 净减 ~4218 行.
- `syntecMacro.analysisBackend` enum 收敛为 `['rust-wasm']` 单值; `javascript` / `rust-wasm-shadow` 配置值退役 (VS Code 枚举校验拒绝旧值并提示重设为 `rust-wasm`).
- Host provider (completion/hover/definition/navigation/formatting/diagnostics/code-actions) 走 `hostRustAnalyzer` 同步路径; Worker 硬切 rust-wasm.
- 数据真源 4 个 JS 表 (keywords/codeDocs/functions/systemVariables) 保留, 留给 Phase R2 迁到 JSON.

发布: GitHub Release <https://github.com/SYNTEC-40101720/syntec-macro/releases/tag/v4.0.0>, VSIX 240510 B, SHA-256 `4d1e53313093bfed41bc40124d5abe08f54933caadeca4cae829d725c46dfef5`.

## 后续计划 (v4.x+)

按优先级:

1. **Phase R2 数据表迁移** — 已完成 (2026-09-22). 把 4 个 JS 数据真源表迁到独立 JSON 文件, host Provider 层通过 thin loader 读 JSON, 让 host 与 Rust 共享同一数据 schema. (源自已合并的「JS Backend 退役路线图」§Phase R2, 已并入本档.)

   **R2.1 试点完成 (2026-09-22)**: `src/systemVariables.js` 迁出 `SYSTEM_VARIABLE_DOCS` → `src/data/systemVariables.json` 作唯一数据真源; JS 模块改 thin JSON loader (mtime-aware 进程内缓存); `hoverProvider.js` 透明访问; `tests/systemVariables.test.js` 7 项契约测试 + `checkVsixContents.js` `REQUIRED_STATIC_FILES` 追加 JSON 路径. 端到端模板: 建 JSON → 写 thin loader → 加契约测试 → 更新 VSIX 内容清单 → 全量验收.

   **R2.2–R2.4 收口完成 (2026-09-22)**: 按同模板补迁 3 张表：
   - R2.2 `functions.js` → `src/data/functions.json` (62 项函数数组 + 2 个辅助函数 `buildFunctionIndex` / `getFunctions` 保留在 JS); `completionProvider` / `providerShared` 透明访问.
   - R2.3 `codeDocs.js` → `src/data/hoverDocs.json` (3 张表: gCodeDocs 47 项 / g10LCodeDocs 13 项 / mCodeDocs 17 项); `hoverProvider` / `keywords.getMCodeDesc` 透明访问.
   - R2.4 `keywords.js` → `src/data/keywords.json` (2 张表: keywords 10 组 + keywordDocs 72 项); `completionProvider` / `hoverProvider` 透明访问.

   **R2.5 completionSnippets 评估 (2026-09-22)**: `src/completionSnippets.js` 仅含 1 个纯函数 `buildFunctionSnippet` 无数据真源, 不在 R2 数据迁移范围; 表格中 `snippets/syntec-macro.json` 是 VSCode snippet 注册表, 本身已是 JSON, 无需再迁. R2 标记完成.

   **全部验收 (2026-09-22)**: test 281/281 (含 4 个新契约测试共 39 项) / lint 0 / typecheck 0 / check:vsix 39 文件 / check:release OK / check:release:readiness --strict 6 PASS+1 SKIP+0 FAIL / check:js-backend-retirement --strict 6 PASS+0 SKIP+0 FAIL / benchmark 无回归 (fixture p50 12.01ms vs baseline 12.84ms / large-20k 323.71ms vs 370.12ms / nav-500-files 67.29ms vs 109.77ms).

   | 数据表 | 行数 (估) | 迁移目标 | 状态 |
   |---|---|---|---|
   | `src/systemVariables.js` | 48 | `src/data/systemVariables.json` | ✅ R2.1 完成 |
   | `src/keywords.js` (gcodes/mcodes) | 384 | `src/data/keywords.json` | ✅ R2.4 完成 |
   | `src/functions.js` | 97 | `src/data/functions.json` | ✅ R2.2 完成 |
   | `src/codeDocs.js` (hoverDocs) | 260 | `src/data/hoverDocs.json` | ✅ R2.3 完成 |
   | `src/completionSnippets.js` | 26 | n/a (纯函数无数据真源, `snippets/syntec-macro.json` 本身已是 JSON) | ⏸️ R2.5 不迁, 评估完成 |

   > `src/statementClassifier.js` 已在 R1.2 Stage B 删除, 其候选 Rust 实现 `classify_statement_for_formatter` 在 Rust 端存在, 不再迁.

   **验收**: `tests/hoverProvider.test.js` / `tests/completionProvider.test.js` 全 PASS; JSON 数据接口在 host 层包装为只读缓存; 文档入口 (本档) 标 R2 完成.

   **不变约束 (R2 实施准则)**: (1) 不得修改 `assets/rust-wasm/` 时跳过 `scripts/buildRustWasmAsset.js` 全链校验. (2) host-only Provider 层与数据真源在 R2 完成后仍保留作 host 层入口. (3) 不得删除 `tests/workerLifecycle.test.js` / `tests/rustWasmAsset.test.js` / `tests/rustWasmWorkerAdapter.test.js` (fallback / ABI 守卫). (4) 不在没采集 `perf-baseline/v4.0.0.json` 前声称 R1.3 完成.

2. **R1.3 VSIX 体积与启动基线刷新** — 已完成 (2026-09-22). 采集 `perf-baseline/v4.0.0.json` 作退役后节点基线, 供后续回归对照. `perf-baseline/README.md` 记录采集规则.
3. **Phase 5 控制器实测证据采集** — Phase 5.1 已完成 (2026-09-22, user 张颖). Phase 5.2 + 5.3 第 1-3 项 + 5.4-5.6 已收口.
   - **Phase 5.1 (已完成 2026-09-22)**: 23 个采集块全部回填 (CALL-RUN-01..07 / FUN-A-13A..F / ROB-LTP-01..08), 见 `docs/macro-knowledge/Phase5-控制器证据采集清单.md` §A/B/C.
   - **Phase 5.2 (已完成 2026-09-22)**: `crates/syntec-core/src/lib.rs` `RobotLineState` 扩 4 字段 (`movc_pair_count` / `wait_pending` / `m198_reread_required` / `g65_call_scope_active`), 仅扩 struct 不接 emit; 顶部挂 `#[allow(dead_code)]`; 加默认状态单测. `cargo build --lib` + `cargo build --lib --release --target wasm32-unknown-unknown` 均 PASS.
   - **Phase 5.3 第 1 项 (已完成 2026-09-22)**: ROB-LTP-02 / RBT-127 落地. `crates/syntec-core/src/lib.rs` `validate_robot_line_state` 在 `pending_movc_line > 0` 期间对 `G10 L*` (新增 `is_g10_l_line`) / `SYNCOUT` / 非运动辅助 M 码 / 辅助 G 码四类中间单节推进 `movc_pair_count`, 第 11 笔即时在本行 emit `SYNTEC_ROBOT_MOVC_INTERMEDIATE_LIMIT`. MOVC 配对完成 / 运动指令截断时计数器归零. 单行 X1/X2 与 Macro 赋值 / 流程控制豁免 (不进入计数路径). 四件套登记完成 (JS `src/diagnosticCodes.js` 加 `ROBOT_MOVC_INTERMEDIATE_LIMIT` + `src/diagnosticActions.js` DIAGNOSTIC_HELP 文案 + Rust lib.rs emit + `Rust诊断parity清单.md` §已覆盖列表中新增 `SYNTEC_ROBOT_MOVC_INTERMEDIATE_LIMIT` + §交叉引用表新版). Compare:rust 新增 4 个差分样例 (139/139 等价), wasm 资产重打包 (byteLength 318040 → 322188, SHA-256 `88ddaeac...` → `8a6ac901...`). 详见 `docs/Rust诊断parity清单.md` 顶部「Phase 5.3 第 1 项...」段.
   - **Phase 5.3 第 2 项 (已完成 2026-09-22)**: ROB-LTP-03/04/07/08 区间禁忌独立 code 化 (4 个运动辅助区间的 error emit 从通用 `SYNTEC_ROBOT_RANGE_FORBIDDEN_COMMAND` 拆分出 4 个独立 code灪射 RBT-115/322/257/123): `SYNTEC_ROBOT_STITCHON_FORBIDDEN_COMMAND` (RBT-115 连续脉冲输出不支援此指令) / `SYNTEC_ROBOT_WEAVEON_FORBIDDEN_COMMAND` (RBT-322 摆动不支援此指令) / `SYNTEC_ROBOT_WAITSYNC_FORBIDDEN_COMMAND` (RBT-257 履带追踪不支援此指令，RBT-118 暂不 emit) / `SYNTEC_ROBOT_G192_FORBIDDEN_COMMAND` (RBT-123 末端跟踪不支援此指令). M96 中断副程序 warning 不区分 RBT，保留 `SYNTEC_ROBOT_RANGE_FORBIDDEN_COMMAND` 兜底. ROB-LTP-05 RBT-154-2、ROB-LTP-06 RBT-110 已由既有 `SYNTEC_ROBOT_SWAITSIG_LIMIT` / `SYNTEC_ROBOT_SYNCOUT_LIMIT` 覆盖 emit 逻辑不变，仅在交叉引用表上编号关联收口. 四件套登记完成: JS `src/diagnosticCodes.js` 加 4 key (JS 总 code 65→69) + `src/diagnosticActions.js` `DIAGNOSTIC_HELP` 加 4 项文案 + Rust lib.rs literal emit + `Rust诊断parity清单.md` §已覆盖与交交叉引用表. Compare:rust 12 个区间禁用 case 仅更新 `expected.code`，139/139 等价不变. wasm 资产重打包 (byteLength 322188 → 322575, SHA-256 `8a6ac901...` → `02bcb398...`). 详见 `docs/Rust诊断parity清单.md` 顶部「Phase 5.3 第 2 项...」段.
   - **Phase 5.3 第 3 项 (已完成 2026-09-22)**: `cargo test --lib` 既有 4 个 navigation 测试 `result.calls` 破损修复 (P0-B 重构后 `calls` 移入 `navigation: Option<AnalysisNavigation>` 之下). 新增 mod tests 辅助 `analyze_macro_document` 走 `analyze_request` + `.nc` URI 触发 `is_macro_file_content` 路径让 `navigation=Some`, 4 个测试改用 `result.navigation.as_ref().expect(...).calls` 取 calls. 同并修 `is_g10_l_line_matches...` 单测误断言 `G10 L0;` 应 false (实际 L0 仍满足 `\d+` 且现场 `G10 L0` 同样占用 LTP 队列, 应为 true; 改断言 `G10 L;` 应 false). wasm 资产不变 (改动仅在 `#[cfg(test)]` 模块). `cargo test --lib` 37/37 PASS.
   - **Phase 5.3 第 4 项 (已完成 2026-09-22)**: `analyze_request` 主循环验放收口. `crates/syntec-core/src/lib.rs` `analyze_request` 主循环已逐行调用 `validate_robot_line_state` (即时 emit 入口), 文件尾内联段处理 `SYNTEC_ROBOT_MOVC_PAIR_REQUIRED` (pending_movc_line 未闭合); `result_to_json` 序列化完整 `diagnostics` 数组. 8 项 RBT 现全部覆盖 emit (RBT-124/127/115/322/154-2/110/257/123); RBT-118 决策不 emit. dead code `SWAITSIG_Q_RANGE` / `SYNCOUT_Q_RANGE` / `SKIPCOND_Q_RANGE` 在 Phase 5.3 路径 A 已退役.
   - **Phase 5.4 (已完成 2026-09-22)**: `compare:rust` parity 扩展. 差分基线 139 类 (135 + 4 个 RBT-127 中间点超限), navigation 10/10, edits 17/17 全等价; `tests/fixtures/rust-parity-baseline.json` 已含 Phase 5.3 第 1/2 项全部 case.
   - **Phase 5.5 (已完成 2026-09-22)**: 接入 `analyze_request` 主循环. `validate_robot_line_state` 已在 `analyze_request` 主循环内逐行调用, 所有 Phase 5.3 emit 即时产生并通过 `result_to_json` 输出; MOVC pending pair 文件尾 emit 由主循环后的内联段处理 (`SYNTEC_ROBOT_MOVC_PAIR_REQUIRED`).
   - **Phase 5.5 区间未关闭收尾 emit 复核 (2026-09-23)**: 经 Confluence 一手规格页复核, 决定**不实现** STITCHON/WEAVEON/WAITSYNC/G192 区间未关闭的收尾 emit. 依据: (1) STITCHON/STITCHOFF 页 (64815646) 明确「RESET/加工结束视为自动执行 STITCHOFF」→ 文件尾非错误状态, 不应 emit. (2) WEAVEON/WEAVEOFF (64815586)、WAITSYNC/ENDSYNC (64815317)、G192.1/G192.2 (64817515) 三页只登记区间内禁用指令的 RBT-322/RBT-257/RBT-123 与运行时警报 (如 WAITSYNC 物体超工作范围自动解除), **无「忘记关闭区间」的静态语法诊断记载**. 控制器要么自动关闭, 要么是运行时行为, 静态诊断无规范依据. 新增无证据诊断会违反「诊断规则科学化工作流」第 1 步「规范手册对齐」. 若后续控制器版本补充区间未关闭的静态警报编号, 再按四件套登记补 emit.
   - **Phase 5.6 (已完成 2026-09-22)**: `Rust诊断parity清单.md` 顶部状态段已含 Phase 5.3 第 1/2/3 项里程碑记录, 73/73 Rust parity code 全覆盖; `MACRO能力矩阵.md` ROB-001 状态维持 «实测复核 (CF + B 级双轨对齐)», 段内已说明 23 个 `SYNTEC_ROBOT_*` 稳定诊断 code 全覆盖 + 3 个 dead code 已退役.
4. **新诊断规则科学化** — 已收口 (2026-09-22). 73/73 Rust parity code 已全覆盖, 后续诊断增改走 "规范手册 + 能力矩阵 + Rust monomorph + 单测" 四件套, 关键词新增仅扩 `src/data/keywords.json` (经 `src/keywords.js` thin loader).
   - 新增 `scripts/checkDiagnosticGovernance.js` + npm `check:diagnostic-governance`: 守卫每个稳定 DiagnosticCode 在四件套登记点 (JS 注册 / Rust emit / parity 清单 / DIAGNOSTIC_HELP 文案) 同步落地. HARD 门禁 (JS+Rust+parity 文档) 阻塞, SOFT (DIAGNOSTIC_HELP 文案) 仅 warning. 当前实跑 HARD=0 PASS, SOFT=0 PASS (27 个 code 缺用户文案已全部补齐 2026-09-22).
   - 新增 `docs/macro-knowledge/诊断规则科学化工作流.md` 工作流文档: 8 步流程 (规范手册对齐 → JS 注册 → Rust emit → parity 交叉引用 → 差分样例与单测 → 派生文档刷新 → 全量门禁 → CHANGELOG), 关键经验 (v4.0.0 后 JS 仍是元数据真源 / 能力矩阵不重复登记 code literal / regex 兼容末项无尾逗号 / HARD=SOFT=0 升 strict 时机).
   - 新增 `tests/checkDiagnosticGovernance.test.js` 12 项契约测试, 接入 `npm test`.
   - `.github/workflows/ci.yml` PR 阶段 informational review (`continue-on-error: true`), `.github/workflows/release.yml` 发布收口已于 2026-09-22 升 `-- --strict` 作发布硬门禁 (SOFT=0 后 CI 可升 strict).
5. **v5.0 Major 远期** — 仅在 R2 收口后评估是否还要进一步精简 host 配置面与硬假设为 Rust-only.

## 永久门禁 (每次发布前后必跑)

完整命令清单见 `docs/3.x-Rust-Wasm切换剩余任务规划.md` §2.

## 历史阶段简述

- **v4.0.0 (2026-09-22)**: R1.2 Stage B 收口 — 删 12 个 JS 模块, host provider 走 hostRustAnalyzer, worker 硬切 rust-wasm, CI 全 PASS, VSIX 240510 B.
- **v3.1.0 (2026-09-21)**: 默认 backend 从 javascript 切到 rust-wasm (JS 保留 fallback 平滑过渡). Phase 1.5 / R1.1 准入全 PASS.
- **v3.0.0 (2026-09-20)**: Rust/Wasm 进 VSIX 作 experimental, 默认仍 javascript. CI/门禁/parity/Worker 全套就位.
- **v2.13–v2.15 (2026-07~09)**: 诊断体验整成期 — Modbus-TCP 静态诊断, 共享词法状态机, LTP 静态范围, 导航文件监听缓存, 语言数据一致性门禁, 发布脚本对齐. 详见 `CHANGELOG.md`.

## 入口

- 历史步骤级清单 (已删, 完整步骤记录见 git log 或 `CHANGELOG.md`)
- 实施回滚参考: `docs/R1.2-实施方案.md` (保留作 Rollback 手册)
- 资料补章 → 程序匹配清单: `docs/macro-knowledge/资料补章-程序匹配-步骤级清单.md`
- 全量门禁: `docs/3.x-Rust-Wasm切换剩余任务规划.md` §2
- 开发交接: `docs/开发交接说明.md`
- 发布 Runbook: `docs/3.x-Release-Runbook.md`
# 迭代优化计划

## v4.0.0 主线收口 (2026-09-22)

**主线状态**: ✅ 已发布. 三条主线 (JS → Rust 后端迁移 / host 模块化边界 / 规范化门禁) 在 v4.0.0 一次性收口.

- 发布版本: `v4.0.0` (2026-09-22), GitHub Release: <https://github.com/SYNTEC-40101720/syntec-macro/releases/tag/v4.0.0>
- VSIX: 240510 bytes, SHA-256 `4d1e53313093bfed41bc40124d5abe08f54933caadeca4cae829d725c46dfef5`.
- 架构: 分析后端硬切为 `rust-wasm` 唯一: 12 个 JS 分析器模块已删 (analysisCore/lexer/validator/robotValidator/controlFlowValidator/functionArgumentValidator/diagnosticRules/diagnosticFactory/formatter/navigationSymbols/navigationIndex/statementClassifier), `syntecMacro.analysisBackend` enum 仅 `['rust-wasm']`, `javascript`/`rust-wasm-shadow` 选项退役. Host provider (completion/hover/definition/navigation/formatting/diagnostics/code-actions) 全部走 `hostRustAnalyzer` 同步路径, worker 走 `rust-wasm` 主路径. 数据表 (keywords/codeDocs/functions/systemVariables/completionSnippets) 保留作为数据真源, 留给 Phase R2 迁移.
- 验证: 本机 `npm test` 236/236 PASS, lint clean, `check:release --tag v4.0.0` consistent, `check:vsix` 35 files valid; CI `rust-wasm.yml` + `release.yml` 双平台全 PASS, VSIX 自动上传 GitHub Release.
- 关键 commit: `02ae430` (Stage B 退役) + `ff90f8b` (Release v4.0.0 切版) + `77bc390` (CI probes benchmarks Stage B 适配) + `886891a` (release flow open).

## 后续计划 (v4.x+)

按优先级:

1. **Phase R2 数据表迁移** — 把 5 个 JS 数据真源 (`src/keywords.js` / `src/codeDocs.js` / `src/functions.js` / `src/systemVariables.js` / `src/completionSnippets.js`) 迁到 `src/data/*.json` + loader, 让 host 与 Rust 从同一 schema 读, 消除两端重复维护. 详见 `docs/JS-Backend退役路线图.md` §Phase R2.
2. **R1.3 VSIX 体积与启动基线刷新** — 采集 `perf-baseline/v4.0.0.json` 供后续回归对照, Stage B 后单包从 246275B 降到 240510B 已是净减数据, 只是基线文件状态.
3. **Phase 5 控制器实测证据采集** — 仍证据阻塞, 取得 CNC 模拟器/控制器后采 CALL-RUN-01..07 / GETPR/SETPR / ROB-LTP-01..08, 按 `docs/macro-knowledge/Phase5-控制器证据采集清单.md` 接入 Rust emit 入口与 host 层 parity.
4. **新诊断规则科学化** — 68/68 Rust parity code 已全覆盖; 后续诊断增改走 "规范手册 + 能力矩阵 + Rust monomorph + 单测" 四件套.
5. **v5.0 Major 计划** — 远期, 仅在 R2 收口后评估是否还要进一步精简 host 配置面与硬假设为 Rust-only 在保证约束 boundary.

--- 下为历史状态记录 (v3.0.0+), 以备接续与回归状态来源, 可淡读. ---

## 已落实节点 (v3.0.0 ~ v3.1.0, 历史)


> 起点: GitHub Release v3.0.0 (2026-09-20)。
> v3.0.0 现状摘要见 [`docs/3.x-Rust-Wasm切换剩余任务规划.md`](3.x-Rust-Wasm切换剩余任务规划.md)。
> 发布时的不变约束与门禁清单见 [`docs/3.x-Rust-Wasm切换剩余任务规划.md` §2–§4](3.x-Rust-Wasm切换剩余任务规划.md)。
> **步骤级清单见 [`迭代优化计划-步骤级.md`](迭代优化计划-步骤级.md)**：把本文 §1–§6 拆成 5 个 Phase 的可执行 step，每步含「入口文件 / 验收命令 / 阻塞 / 风险」。

本文档是 v3.0.0 发布后的短期可推进项与对应入口；并非硬性 sprint backlog，user 可按优先级与证据可得性自由排序。

## 已落实节点

- **打包前 Rust/Wasm 资产门禁**（2026-09-20）：`npm run package` 已串入 `check:rust:wasm:asset`，本地直接打包前也会校验 Wasm 字节长度与 SHA-256，避免生成携带过期资产的 VSIX。

---

## 1. 默认 backend 切换决策门 — Rust 收益评估

**目标**: 让 Rust/Wasm backend 稳定优于 JS，再发下一个 Minor release 切换默认 backend。

**为什么没切**: v3.0.0 实测 `npm.cmd run benchmark:compare -- --iterations 5` 在 large-20k 上 JS p50 422ms vs Rust 457ms 仍存微弱回归，按 `docs/3.x-Rust-Wasm切换剩余任务规划.md` §3 禁令 1 不得强行切换。

**何时可切**:
- 在 dev machine (Windows) 与 CI (Linux + Windows) 上各跑 `npm.cmd run benchmark:compare -- --iterations 10`，三场景（fixture / 20k / nav-500-files）Rust p50 / p95 / max 都不少于 JS（误差 ≤ 5%）。
- `fallback` 比例持续 0%（已采集 5 次以上）。
- `workerLifecycle.test.js` 9 项集成测试无新增 FAIL。
- 更新 `scripts/benchmarkCompare.js` 的 `REGRESSION_THRESHOLDS`，把 Rust 阈值改为等于或严于 JS 阈值后跑 `npm.cmd run check:release:readiness -- --strict` 仍全 PASS。

**入口**: `scripts/benchmarkCompare.js` + `scripts/comparePerfData.js` + `tests/benchmarkCompare.test.js` + `tests/comparePerfData.test.js`。

## 2. Rust 跨行状态 parity — LTP / 机器人 / Modbus 生命周期

**目标**: 把目前 JS 只记录证据不静态强制的跨行规则，迁到 Rust 侧并 parity 校验。

**为什么没做**: 跨行状态、运行时生命周期、`WAIT()` 时序、模式宏触发等需要控制器实测证据，仅靠静态解析不准（参见 `docs/macro-knowledge/MACRO-LTP专项资料包.md`）。

**关键资料阻塞**:
- `CALL-RUN-01` 到 `CALL-RUN-07` 调用语义资料包需要模拟器/控制器环境验证变量生命周期、`M198` 重读、`WAIT()` 时序。
- `GETPR/SETPR` 需要直接的 A 级函数页或记录化控制器验证。

**入口**: `docs/macro-knowledge/MACRO调用语义资料包.md` + `docs/macro-knowledge/MACRO-LTP专项资料包.md` + `src/robotValidator.js` 注释。

## 3. ROB-001 LTP 专项规则审计 + MACRO 能力矩阵接入

**目标**: 把 LTP 警报/参数/版本证据接入统一 MACRO 能力矩阵，让规则与证据双向可追溯。

**入口**: `docs/macro-knowledge/MACRO能力矩阵.md` + `docs/macro-knowledge/MACRO函数审计资料包.md`。

## 4. 文档治理与单源真相

**目标**: 让 `docs/新代MACRO语法规范手册.md` (3443 行)的章节级合并、`docs/macro-knowledge/` 下 8 个资料包的整合收敛。

**入口**: `docs/macro-knowledge/README.md` 已建立文档治理入口；下一批按主题聚类做章节级 merge。

## 5. CI 性能回归告警阈值精调

**目标**: 目前 `.github/workflows/rust-wasm.yml` 的 perf-data smoke 走 `--no-threshold`（Linux runner ≠ dev machine 速度基线），不动发回阈值；v3.0.0+ 在 CI 历史数据积累后（≥5 次推送）可定为「按 platform 建阈值」，让 Rust/JS 任一指标 30% 回归时发 `::warning::`。

**入口**: `scripts/comparePerfData.js` + `.github/workflows/rust-wasm.yml cross-platform-alert 步骤`。

## 6. CHANGELOG `[Unreleased]` 入口保持空

每个节点完成后向 `CHANGELOG.md [Unreleased] ### Added` 段追加条目；只有发布新 Release 时按 `docs/3.x-Release-Runbook.md` §1 把 `[Unreleased]` 改为 `## vX.Y.Z - YYYY-MM-DD` 再开空 `[Unreleased]` 入口。

---

## R. JS Backend 退役路线图 ✅ v4.0.0 已落地

**状态**: ✅ 已完成并发布 v4.0.0 (2026-09-22). JS analyzer 函数式核心 12 个模块已从 VSIX 与仓库删除 (净减 ~4218 行), `syntecMacro.analysisBackend` enum 收敛为单值 `['rust-wasm']`, host provider 全部走 `hostRustAnalyzer` 同步路径. `javascript` 与 `rust-wasm-shadow` 配置值在 v4.0.0 后已退役 (VS Code 枚举校验拒绝旧值并提示重设). 数据表 5 个 JS 文件保留作数据真源, 留给 Phase R2 迁移.

**原始路线图**: `docs/JS-Backend退役路线图.md` — 三项不变前提 + Phase R1 (JS 核心 retire) + Phase R2 (数据表迁移 host 共享层) + Phase R3 (release 流程边界). Phase R1 已收口, R2 待启, R3 已按 Runbook 走 CI 主路径 (push tag 触发 `release.yml`).

**入口工具** (仍可重跑验证现状):
- `npm.cmd run audit:js-backend` — 表面审计输出 markdown 报告 (主要回顾用)
- `npm.cmd run check:js-backend-retirement -- --strict` — R1 准入 6 项自检 (R1.2 Stage B 后 6 PASS / 0 SKIP / 0 FAIL)

**Phase R2 待启**: 把 `src/keywords.js` / `src/codeDocs.js` / `src/functions.js` / `src/systemVariables.js` / `src/completionSnippets.js` 五个数据真源迁到 `src/data/*.json` + loader, 让 host 与 Rust 从同一 schema 读. 详见 `docs/JS-Backend退役路线图.md` §Phase R2.

--- 以下是 v3.0.0 ~ v3.1.0 期间路线图内部进度记录 (历史快照, R1.2 实施方案在 `docs/R1.2-实施方案.md` 实施完毕后这些阶段性描述已过时), 仅作状态回归溯源使用: ---

**原目标**: 在 Rust/Wasm 稳定为单一生产 backend 后, 把 JS analyzer 函数式核心 (13 个文件 ~3261 行) 从 VSIX 剔除, 仅保留 Rust/Wasm + host-only Provider + 数据真源表, 落地 v4.0 Major 退役. ✅ 已完成.

**原当前现态** (R1.1 落地前):
- JS 端 `DiagnosticCode` 67 个, Rust 端 literal 覆盖 64, 剩 3 项 `*_Q_RANGE` dead code 两端均不 emit — Phase 5.3 路径 A 已两端共同剔除.
- analyzer 函数式核心 14 个 JS 模块对应 Rust 实体已就位, 按 R1.2 实施方案一次性退役 12 个 (保留 pathResolver 与 diagnosticActions 作 host-only helper).
- 14 个 host-only Provider 文件 (vscode API 绑定) + 7 个数据真源表 (keywords/codeDocs/functions 等) 保留作 host 层入口, 绝对不删 (数据表留给 Phase R2).

**原准入条件** (R1.2 Stage B 启动前须全过, 现已全部达成):
1. Phase 1.5 切默认 backend = `rust-wasm`, 发 v3.1.0 ✅
2. ≥1 release cycle 观察期 fallback 持续 0% ✅
3. Phase 5.x 跨行状态完整 parity (dead code 已决策) ✅ (路径 A 共同剔除)
4. `compare:rust` 与 `compare:perf` 持续等价 ≥1 release cycle ✅
5. `check:js-backend-retirement --strict` 全 PASS ✅

**2026-09-21→22 进度 (历史快照)**:
- Phase 1.5 落盘 (commit `8cf60a8`), v3.1.0 发布上线 (CI `release.yml` run 35608606505 全 PASS).
- Phase 1.3 nav 优化落地 (commit `b2a0c93`): Rust `analyze_navigation_request` + Wasm 导出 `syntec_core_analyze_navigation_json`, CI 5 次 nav-500-files Rust ≈ JS 的 20–23%.
- Phase 5.3 路径 A 落地: 剔除三项 `*_Q_RANGE` dead code 与对应 code action (JS+Rust 两端共同).
- R1.1 准入 6 PASS / 0 SKIP / 0 FAIL.
- R1.2 实施方案就位 (`docs/R1.2-实施方案.md`) → commit `02ae430` 完成 Stage B 净删 12 模块; `ff90f8b` 切版 v4.0.0; CI 跑通; 已发布.

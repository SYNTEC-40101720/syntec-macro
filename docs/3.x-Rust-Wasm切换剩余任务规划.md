# 3.x Rust/Wasm 切换剩余任务规划

## 1. 当前结论

当前发布版本仍为 **v2.15.0**，生产后端仍为 JavaScript。3.x 版本号、tag、Release 和 VSIX 发布必须等 Rust/Wasm 正式导入生产后端的全部门禁通过后再切换。

已完成的架构基础：

- `DocumentSnapshot`、`AnalysisRequest`、`AnalysisResult` 协议。
- JavaScript `analysisCore`、有界 `AnalysisHost`、Worker/Provider 收敛边界。
- Rust CLI/Wasm ABI、`RustWasmAdapter`、导航文件元数据适配。
- `AnalysisHost` 的 `javascript`/`rust-wasm` selector 和显式 JavaScript fallback。
- Rust/Wasm 与 JavaScript 的 51 类差分样例、完整回归和开发态基准。
- `RustWasmAdapter` 已位于 `src/` 并可随代码进入 VSIX，但尚未注册或默认启用。

当前仍是开发态的原因：

- Rust 诊断 code 已完成 JavaScript 全部稳定诊断集合的 67/67 parity 收口（详见 `docs/Rust诊断parity清单.md`）；P0-A.1 节点已合上。
- 调用与引用边界（GOTO 标签 / G65/G66/G66.1/M98/M198/M99/G67 调用返回 / `% 缺 %@MACRO` 文件头 warning）已 parity（P0-A.2 合上）；差分扩展到 130 类。
- P0-B 真实 request 传输已完成：Rust 端 `parse_analysis_request`/`analyze_request_json`/Wasm `syntec_core_analyze_request_json`/CLI `--request` 模式完整校验 `protocolVersion`/`document`/`profile`，`createRustWasmAdapter` 优先走新 ABI；差分扩展 P0-B 路径 130/130 等价。
- P0-B 第 2 项 symbols/navigation 已完成：Rust 端依据 `document.uri` 完整计算 `programEntryName`/`macroProgramName`，非 macro 文件返回 `navigation: null`，`compare:rust` 增加 navigation 差分环节 10/10 等价。P0-B 第 2 项 edits/TextEdit 已完成：`format_document` 完整移植 `formatter.js`，`AnalysisResult.edits` 整文档 `TextEdit` 17/17 等价；profile 协商透传非空 profile 与 JS 一致，未知 profile 当 generic 等价处理。**P0-B 第 2 项「完整结果」已合上**，进入 P0-C 生产 Wasm 资产与 Worker 接入。**P0-C 第 1 项 生产 Wasm 资产与加载器 已合上**：新增 `src/rustWasmAsset.js`（生产加载器，manifest + SHA-256 + 必需 exports + `WebAssembly.instantiate` 全链校验，每步失败抛带 `reason` 字段的 `RustWasmAssetError`），新增 `scripts/buildRustWasmAsset.js` 与 npm 脚本 `build:rust:wasm:asset`/`check:rust:wasm:asset`（`--check` 模式挂到 `npm test` 作为 CI 资产完整性门禁），新增 `assets/rust-wasm/manifest.json` 与 `syntec_core.wasm`（~312KB、SHA-256 `7b69e5dc...`）作为固定版本/target 的 wasm 资产物理载体；`scripts/probeRustWasm.js`/`scripts/benchmarkRustWasm.js` 改为默认走生产加载器 manifest 路径，`SYNTEC_RUST_WASM` 仅保留为开发逃逸端口；新增 `tests/rustWasmAsset.test.js` 16 项测试覆盖 8 类失败路径与成功路径，`npm test` 扩展到 312 项；资产目录经 `.vscodeignore` 仍不进 VSIX，Worker 注入与默认 backend 切换由 P0-C 第 2/3 项完成。
- LTP/机器人/Modbus 跨行状态、产品边界与运行时差异继续只记录证据不静态强制（参见 `src/robotValidator.js` 注释与 `MACRO-LTP专项资料包.md`）。
- Wasm 生产资产、加载器、完整平台构建矩阵和默认 Worker 接入尚未完成（P0-C）。

### P1 第 1 项 状态（2026-09-20）

- **P1 第 1 项 JS/Rust/Wasm 三方对照基准 已合上**：新增 `scripts/benchmarkCompare.js` 与 npm 脚本 `benchmark:compare`。脚本在三类 P1 场景下同步测量 JS（`analyzeDocument` + `analyzeNavigationDocument` 等价合并路径）与 Rust/Wasm（`createRustWasmAdapter` 真实 request ABI）的启动延迟、首次运行、p50/p95/max 分析延迟、批量 navigation 耗时与 parity 校验：(1) 真实 fixture（394 行）— 记录首次/p50/p95/max 与 parity； (2) 20,000 行档案 — 同上； (3) 500 文件 × 40 行 navigation 批量 — 记录单 batch 累计耗时与 parity 抽样全检。脚本通过 `stableFingerprint` 形状比对器把 JS 与 Rust `AnalysisResult` 折算为 `{diagnostics,symbols,hasNavigation}` 等价比对形态，跳过 `backend`/bytes 非不变量；任何场景 parity mismatch 即 `process.exitCode = 1`，作为 P1 "没有稳定收益或出现回归时保持 JavaScript 默认后端，不强行切换" 的硬门禁。新增 `runJavaScriptEquivalent(request, filePath)` 把 JS analyze-only 与 navigation-only 路径合到与 Rust `analyze_request` 输出形状一致的桥接函数（macro 文件产出 navigation+symbols、非 macro 文件产出 `navigation: null` 与 `symbols: []`）。新增 `tests/benchmarkCompare.test.js`（11 项测试）。`npm test` 测试集合扩展到 341 项（330 + 11）；`npm.cmd run benchmark:compare -- --iterations 5` 在 fixture/large-20k/nav-500-files 三场景全部 `parity=equal`。`.github/workflows/rust-wasm.yml` 新增 `P1 JS/Rust/Wasm parity + perf comparison (smoke, 3 iterations)` 步骤挂在 `benchmark:rust:wasm` 之后作为 CI 门禁。

### P1 §3.x 发布收口状态（2026-09-20 3.0.0）

- **3.0.0 Major 版本切换 已完成**: 按本规划文档 §「3.x 发布收口」第 1 步切换版本号、§3 把生产 wasm 资产 bundle 进 VSIX：`package.json`/`package-lock.json`（顶层与 `packages[""]`）/README 徽章/CHANGELOG 段落标题统一切到 `3.0.0`；`.vscodeignore` 把 `assets/` 排除行改为 `!assets/`，把 `assets/rust-wasm/{manifest.json, syntec_core.wasm}`（311963 字节、SHA-256 `7b69e5dcac2adc78aae592b51bbddd4f9c79bf41e1a2dda1effd0156c38ba669`）作为 P0-C 第 1 项「固定版本/target 的 wasm 资产」正式纳入 VSIX；`scripts/checkVsixContents.js` `REQUIRED_STATIC_FILES` 加上两个资产必填项，`check:vsix` 由 42 文件升到 44 文件。默认后端为遵守 §3「没有稳定收益或出现回归时，保持 JavaScript 默认后端，不强行切换」仍保持 `javascript`（`package.json` enumDescriptions 改为「JavaScript analyzer（默认，v2.15.0 起生产后端；3.0.0 起与 Rust/Wasm 并存）」），用户可通过 `syntecMacro.analysisBackend` 切换到 `rust-wasm-shadow`/`rust-wasm`，wasm 加载失败时 `createAnalysisBackend` 显式回退 JavaScript（workerLifecycle 集成测试已覆盖 7 类边界）。§2 全量验收：`npm test` 367/367、`npm run lint` 0、`npm run typecheck:analysis` 通过、`check:release -- --tag v3.0.0` 一致、`check:release:readiness -- --strict` 6 PASS + 1 SKIP + 0 FAIL、`compare:rust` 130/130/10/10/17/17 等价、`probe:rust:wasm` 通过、`test:integration`/`test:integration:navigation` exit code 0、`npm run package` 产出 `syntec-macro-3.0.0.vsix`（46 files, 246275 bytes, SHA-256 `97cb18c2858369fbde2c72a01478fc7f78e9fb5a224e9236b1912a3f3fc68618`）、`npm run smoke:installed` 验证 `syntec-team.syntec-macro@3.0.0` 激活并退出 code 0。§3 VSIX 内容校验：46 文件含 src/34 + 生产 wasm asset + grammar/snippet/images/manifest/license/changelog，不含 Rust target/debug/fixture/script probe。下一步进入 Runbook §4 git tag + §5 release:create + §6 核对（由 user 在装有 git credential 的环境执行 commit/push/tag/release:create）。

### P1 §5 发布前 readiness 自检工具完成（2026-09-20）

- **P1 §5 §「完成定义」7 项门禁 readiness 工具完成**：新增 `scripts/checkReleaseReadiness.js` 与 npm 脚本 `check:release:readiness`：对照 §5「完成定义」7 项门禁一次性自动 review：(1) Rust/Wasm 完整 AnalysisResult parity — `compareRustCore.js` 至少 130 CASES (实际 ~157)； (2) 稳定诊断 + navigation + format parity — `Rust诊断parity清单.md` 含 "67/67" 或 "parity 收口" 标志； (3) 生产 Wasm 资产 + 加载器 + Worker + fallback — manifest/wasm/`rustWasmAsset.js`/`rustWasmWorkerAdapter.js`/`workerLifecycle.test.js` 全部存在； (4) Windows GNU/MSVC + Wasm + CI 构建矩阵 — `rust-wasm.yml` 同时含 `matrix:` / `ubuntu-latest` / `windows-latest` / `cross-platform-alert`； (5) 真实性能/内存/启动/fallback 数据采集 — `benchmarkCompare.js` + `comparePerfData.js` + `benchmarkCompare.test.js` 全部落地； (6) VSIX 内容 + 集成 + 回滚 — `checkVsixContents.js` + `workerLifecycle.test.js` + `integration/runTest.js` 存在； (7) GitHub tag/Release/VSIX 校验值一致 — `createGitHubRelease.js` + `checkReleaseConsistency.js` 工具就位，tag 选定后由发布流程验证 (本项标 SKIP)，不在 readiness 阶段强制门禁。每个 check 运行返回 `PASS`/`SKIP`/`FAIL` 与详情字符串；任一 check 抛错自动转为 `FAIL` 状态不阻塞 stream；`main()` 默认 `exitCode=0`（非门禁，warning 由人 review）；`--strict` flag 在任一 `FAIL` 出现时把 `exitCode=1` 作为 CI 硬门禁。`.github/workflows/release.yml` 在 `Verify release metadata` 步骤之后新增 `3.x release-readiness gate (--strict)` 步骤，对照 release workflow 真正把 §5 7 项作为发布前硬门禁，此 release 不允许在没有 readiness 全 PASS (SKIP 除外) 时发布。新增 `tests/checkReleaseReadiness.test.js`（8 项测试）：`CHECKS` 数组 7 项 id 完整性、每项 id/name/run() 唯一性契约、`run()` 返回结构形态 (`{status,detail}` 且 `status ∈ {PASS,SKIP,FAIL}`)、`main()` 无参不置 exitCode、`main --strict` 在 stubbed FAIL 时置 1、非 strict 容错 FAIL、`run()` 抛错自动转 FAIL、strict 模式下抛错仍计 FAIL 置 1。`npm test` 测试集合扩展到 367 项（359 + 8）；`npm.cmd run check:release:readiness -- --strict` 本机实测：6 PASS + 1 SKIP + 0 FAIL (SKIP 是第 7 项 tag 一致性，由 release workflow 本身跑)。本节点合上 §5「完成定义」7 项 readability 自动化复核基础设施；下一步进入真正的「3.x.x 选定 + tag/Release/VSIX」（必须由 user 决定 3.x.x 具体版本号，按 §3「不得在 parity/Request/TextEdit/跨平台构建/回滚门禁 完成前升级 3.x 版本号」严格遵守 — 当前所有 readiness 门禁已 PASS）。

- **P1 第 3 项 CI 真实性能数据采集 + 跨平台告警 已合上**：`.github/workflows/rust-wasm.yml` 把 `build-and-verify` job 由 `runs-on: ubuntu-latest` 升级为矩阵 `os: [ubuntu-latest, windows-latest]`（开发机 Windows + CI 主力 Ubuntu），保留 `fail-fast: false` 单平台失败不阻塞另一平台。每个平台跑完 benchmark:compare smoke 后新增 `Capture P1 perf data as GitHub artifact` 步骤：直接调 `node scripts/benchmarkCompare.js --iterations 5 --no-threshold --json` 把 stdout 写入 `perf-data/benchmark-<os>.json`（不走红 npm 包装避免 banner 行污染 JSON），用 `actions/upload-artifact@v4` 把 `perf-data/` 上传为 `p1-perf-<os>` 名 30 天保留。新增 `cross-platform-alert` job，`needs: build-and-verify` `if: always()` 后 checkout + npm ci + `actions/download-artifact@v4` 配 `pattern: p1-perf-*` `merge-multiple: false` 取下两平台 artifact，flatten 到 `perf-data/` 后 `find ... -name 'benchmark-*.json'` 拿列表参数传 `node scripts/comparePerfData.js` 做跨平台对比报告；单平台失败只 `::warning::` 不阻塞发布。新增 `scripts/comparePerfData.js`：解析任意数量 perf-data JSON 文件，把 fixture/large-20k/nav batch 的 JS/Rust p50/p95/startup/batch 指标按场景 × metric × platform 汇总到 stdout，并用 `flagAnomalies` 检测 parity mismatch / fallback total / regressions count ≥1 的平台；`formatRow` 健壮除 undefined scenario/metric；只打 stdout、不抛错（CI alert step 只记 notice，不门禁）。新增 `npm.cmd run compare:perf` 脚本与 `tests/comparePerfData.test.js` 14 项测试：`loadPerfFile` 三类错误路径（missing/empty/non-JSON/missing-results）+ 成功路径、`flagAnomalies` 覆盖 results parity mismatch / nav parity mismatch / fallback / regressions、`formatRow` undefined 健壮性、`main` 无参 + 单文件 + 多平台 anomaly 路径均不抛错。`npm test` 测试集合扩展到 359 项（345 + 14）；本机实测 `node scripts/benchmarkCompare.js --iterations 3 --no-threshold --json | node scripts/comparePerfData.js` 输出「no parity / fallback / regression anomalies detected」。本节点合上 P1 第 3 项「CI 真实性能数据采集（不同平台 runner）+ 跨平台告警」基础设施；下一步进入「3.x 发布收口」（P1 §「将 `package.json`/`package-lock.json`/README/CHANGELOG 切换到选定的 3.x.x」）。

- **P1 第 2 项 正式性能数据采集 + 阈值门禁 已合上**：在 `scripts/benchmarkCompare.js` 中扩展三场景的 P1 §「启动时间 / p50 p95 分析延迟 / JSON 内存占用 / 首次与重复查询 / fallback 比例」全套指标：新增 `computeResultJsonBytes(result)` 用 `Buffer.byteLength(JSON.stringify(result), 'utf8')` 采集 JS/Rust 各 scenario 与 nav batch 代表性 JSON 字节大小（修正中文/emoji 多字节场景的内存占用统计）；`runScenarios` 改用 try/catch 包裹 JS 与 Rust 测量路径，把任何运行时异常计入 `fallbackCount`，并把 `resultBytes`、`emptyShape({ firstMs/p50/p95/maxMs=0, lastResult:null })` 错误路径 fallback 状态推入每场景结果对象；500 文件 nav batch 同样收 try/catch `rustFallbackCount`，输出 `jsResultBytes/rustResultBytes/rustFallbackCount` 字段。新增 `REGRESSION_THRESHOLDS` 阈值表（按 scenario 锚定 JS/Rust 各自 p50/p95/batch/startup 上限），`main` 流程在测量后对每个场景与 nav batch 比对阈值；任何场景超过上限即加入 `regressions` 数组并 `process.exitCode = 1`，作为 P1 §「没有稳定收益或出现回归时保持 JavaScript 默认后端，不强行切换」的正式回归保护门禁；`--no-threshold` 关闭阈值检查（CI smoke 路径留宽松开关，因 Linux runner 与 Windows dev machine 速度基线不同）。新增 fallback 比例输出：`fallback: <total>/<runs> (ratio <%)` 走 scenario × 2 + 500 files 总和。`.github/workflows/rust-wasm.yml` 的 smoke 步骤切到 `--no-threshold`（CI 不做硬阈值门禁，只验证 parity 与运行无错）。新增 `tests/benchmarkCompare.test.js` 4 项测试（共 15 项）：`REGRESSION_THRESHOLDS` 覆盖三个场景且有限数字契约、`computeResultJsonBytes` UTF-8 字节与中文多字节场景、`runScenarios` 在正常路径下 `fallbackCount=0`、`stableFingerprint` 空形状确定性。`npm test` 测试集合扩展到 345 项（330 + 15）；`npm.cmd run benchmark:compare -- --iterations 5` 本机实测：fixture JS p50 13.10ms / Rust 14.06ms / JSON 11528 vs 21906 bytes；large-20k JS p50 422.29ms / Rust 457.46ms / JSON 375425 vs 717838 bytes；nav-500-files JS batch 645.85ms / Rust 602.68ms / JSON 2119 vs 2118 bytes；startup JS 0.002ms / Rust 8.10ms / artifact 311963 bytes；fallback 0/504 runs（0%）；parity 全 `equal`。下一步进入 P1 第 3 项「CI 真实性能数据采集（与开发机不同平台）+ 平台矩阵告警」与 3.x 发布收口。

### P0-C 第 2/3 项 状态（2026-09-20 续）

- **P0-C 第 2 项 Worker 注入 AnalysisHost + shadow/differential 模式 已合上**：新增 `src/rustWasmWorkerAdapter.js`（Worker 端 Rust/Wasm analyzer 高阶组合，`loadAsset`/`createAdapter` 注入点 + `onFallback` + `onShadowMismatch`；shadow 模式返回 JS 结果并差分，primary 模式返回 Rust 结果、失败抛错由 `createAnalysisBackend` 回退）；`validatorWorker` 通过 `workerData.backend` 选 `javascript`/`rust-wasm-shadow`/`rust-wasm`，使用 Promise-aware 有界 LRU 缓存（`MAX_ENTRIES=8`）；`diagnosticsProvider` 读 `syntecMacro.analysisBackend` 配置注入 worker，shadow 日志通过 `{kind:'log'}` control 消息上报 `setShadowLogSink`。新增 `.github/workflows/rust-wasm.yml` CI Rust/Wasm 矩阵（构建 wasm、刷新资产、SHA-256 audit、运行 rustWasmAsset/rustWasmWorkerAdapter/rustWasmAdapter 单测、compare:rust、probe/benchmark smoke），CI 已验证 green。
- **P0-C 第 3 项 Worker 生命周期与 fallback 集成测试 已合上**：新增 `tests/workerLifecycle.test.js`（9 项真实 `worker_threads` 集成测试），通过 `new Worker(require.resolve('../src/validatorWorker.js'), {workerData})` 起真实 worker，覆盖 7 类生命周期边界：(1) 启动 javascript backend 正常返回；(2) `worker.terminate()` 后再起新 Worker 仍能分析；(3) 同时 8 条并发请求全部按唯一 id resolve、pending 归零；(4) terminate 时 pending 请求 `resolve(null)` 无永挂 Promise；(5) 同 URI 不同 version+text 走独立缓存槽、`MAX_ENTRIES=8` 超出淘汰最旧条目；(6) `rust-wasm-shadow`/`rust-wasm` backend 资产路径不存在时优雅回退 JS（`backend` 标记不为 `rust-wasm`），shadow Rust 运行时错误只走 `onFallback` 不触发 `onShadowMismatch`（对齐 P0-C 第 2 项单测 expectation），primary 回退也通过 control 消息上报；(7) 未知 backend 守护回退 javascript 并记录 unknown 日志。所有日志仅记录 message 摘要、断言不含 `require(`/`import ` 路径细节，不泄露源码/敏感路径。`npm test` 测试集合扩展到 330 项（新增 9 项）。本节点合上 P0-C 第 3 项「集成测试」边界门禁，`.vscodeignore` 仍排除 `assets/`——资产进 VSIX 由 P1 发布收口前 review 决定；生产 wasm artifact 必须在最终发布环境构建，本地构建只作开发态对照（跨 toolchain 跨平台的 wasm 字节级等价不是不变量）。下一步进入 P1 真实性能/内存/启动/fallback 数据达标与 3.x 发布收口。

## 2. 剩余工作总览

### P0-A：完成分析能力 parity

依赖：无。完成前不得把 Rust/Wasm 设为默认后端。

1. **诊断 code 收口（已完成 67/67）**
   - 对照 `src/diagnosticCodes.js` 建立 Rust parity 清单（`docs/Rust诊断parity清单.md`）。
   - 全面补齐基础语法、G/M 调用、GOTO、函数、变量、无 code warning，以及机器人 TOOLCOR/ROBOT-MOV/ROBOT-MODBUS/ROBOT-STITCH-WEAVE/ROBOT-SIGNAL 五批共 24 个 `ROBOT_*` code 的等价实现。
   - dead code `SYNTEC_ROBOT_SWAITSIG_Q_RANGE` / `SYNTEC_ROBOT_SYNCOUT_Q_RANGE` / `SYNTEC_ROBOT_SKIPCOND_Q_RANGE` 仅在 `diagnosticActions.js` 注册但 JS/Rust 双端共同不 emit，parity 等价；若后续 JS 补登记 emit 入口，须同步迁移 Rust。
   - 三方差分差样例已扩到 115 类（`scripts/compareRustCore.js` 的 `CASES`），完成各自的 Rust 单测与 `RustWasmAdapter` 结构测试。所有已发布稳定诊断的 code、severity、UTF-16 range、message 顺序一致。
   - 目标：所有已发布稳定诊断的 code、severity、UTF-16 range、message 顺序一致——已达成。

2. **机器人/LTP/Modbus**
   - 按 `robotValidator.js` 的单行规则、跨行状态规则和 `G10 L1900/L1901` 规则分组迁移。
   - 保留 CNC、81RA、LTP、Script、APP Macro 的产品/载体边界，不将运行时或机型推断成通用静态错误。
   - 动态参数、表达式和未知 profile 保持不推断。
   - 状态：单行规则组（TOOLCOR/ROBOT-MOV/ROBOT-MODBUS/ROBOT-STITCH-WEAVE/ROBOT-SIGNAL）已全部 parity；跨行状态、运行时生命周期差异继续保留 JS 证据，不静态强制化为通用错误。

3. **调用与引用边界**
   - 完成 `G65/G66/G66.1/M98/M198/M99/G67` 的静态调用、返回边界和动态目标不跳转规则。
   - 完成 GOTO 标签收集、引用缺失、注释/字符串隔离和跨行位置回归。
   - 运行时生命周期、`WAIT()`、预解和 81RA/CNC 差异继续只记录证据，不静态强制化。
   - 补齐 `collectMetadata` 中的 `% 缺 %@MACRO` 文件头 warning（Rust 已在 `analyze_document` 主循环与文件末尾追加等价产出，使用 `push_diagnostic_without_code` 同步 codeless 形态）。
   - 目标：调用/返回/GOTO/% 文件头静态边界与 JS 等价——已达成（差分扩展到 130 类，含 `%` 缺头、GOTO 字符串/注释豁免、动态 GOTO 不跳转、M 码返回/取消等正反例）。

### P0-B：完成共享 `AnalysisResult` 合同

依赖：P0-A 的黄金样例稳定。

1. **真实 request 传输（已完成）**
   - Rust/Wasm 接收并校验完整 `AnalysisRequest`，包括 URI、文档版本、languageId、文本和 profile（Rust 端 `parse_analysis_request` + `analyze_request_json` + Wasm 导出 `syntec_core_analyze_request_json`；CLI `--request` 模式）。
   - 不能仅由 JavaScript 适配层事后补齐 document/profile（`createRustWasmAdapter` 已切到新 ABI 优先，缺失时显式 fallback 到 legacy 文本路径）。
   - 保留 protocol version、UTF-8、非法输入和错误结果的显式失败（`compare:rust` 加 P0-B 差分环节，130/130 类等价）。

2. **完整结果（已完成）**
   - diagnostics：可选 code/keyword、所有 severity、跨行 range（已 parity）。
   - symbols/navigation：完整文件入口、宏名、符号、调用、引用边界（programEntryName/macroProgramName/symbols/calls 已在 Rust 侧依据 `document.uri` 完整计算，差分 10/10 等价）—— ✅ 已完成。
   - edits：formatter 的 `TextEdit` 与 JavaScript 输出一致——`format_document` 完整移植 `formatter.js`，整文档 `TextEdit` 17/17 等价；profile 协商透传非空字符串与 JS 一致—— ✅ 已完成。
   - profile：generic 与未来产品 profile 的能力协商、未知 profile 的明确处理—— generic 已支持，未知 profile 当 generic 等价处理（与 JS `assertNonEmptyString` 一致），产品 profile 后续接入专属规则。

3. **差分门禁**
   - 黄金样例逐字段比较；不使用只比较数量或只比较 code 的代理指标。
   - Rust 失败时必须调用已注册的 `onFallback`，再回退 JavaScript；禁止静默成功。

### P0-C：生产 Wasm 资产与 Worker 接入

依赖：P0-A、P0-B。

1. 生成固定版本、固定 target 的 Wasm artifact，禁止把 `target/`、debug 文件或临时探针打包。
2. 增加生产加载器：
   - 资产路径和完整性校验。
   - Wasm 实例化、内存分配/释放和输出释放。
   - 初始化失败、超时、内存错误、JSON/协议错误均走显式 fallback。
3. 将 Worker 的 analyzer 注入 `AnalysisHost`：
   - 默认切换前支持 shadow/differential 模式。
   - 切换后 Rust 为主、JavaScript 为回退。
   - 记录 backend、fallback 原因和版本，不泄露源码或敏感路径。
4. 增加启动、重启、并发、取消、文档版本竞态和 fallback 集成测试。

### P1：跨平台与性能门禁

依赖：P0-C。

- Windows GNU/MSVC、wasm32、Node/VS Code 运行环境的可复现构建矩阵。
- CI 中执行 Wasm 构建、artifact 校验、Rust/JavaScript 差分和 VSIX 内容检查。
- 对真实 fixture、20,000 行档案、500 文件导航分别记录 JavaScript/Rust/Wasm：
  - 启动时间；
  - p50/p95 分析延迟；
  - JSON/内存占用；
  - 首次与重复查询；
  - fallback 比例。
- 没有稳定收益或出现回归时，保持 JavaScript 默认后端，不强行切换。

### P1：3.x 发布收口

依赖：P0-A、P0-B、P0-C、P1 门禁全部通过。

1. 将 `package.json`、`package-lock.json`、README、CHANGELOG 和交接文档切换到选定的 `3.x.x`。
2. 运行：

```powershell
npm.cmd test
npm.cmd run lint
npm.cmd run test:integration
npm.cmd run test:integration:navigation
npm.cmd run package
npm.cmd run smoke:installed
```

3. 校验 VSIX：
   - 生产 src/代码、grammar、snippet、文档和必要 Wasm 资产；
   - 不含 Rust target、debug artifact、测试 fixture、脚本探针或 session 文件。
4. 提交并推送 main，创建对应 `v3.x.x` tag。
5. 用 `npm.cmd run release:create -- v3.x.x` 创建 GitHub Release，上传 VSIX。
6. 核对 tag、Release、VSIX 下载地址、SHA-256、安装版本和默认 backend。

## 3. 不得提前做的事情

- 不得在 Rust parity、request/profile/TextEdit、跨平台构建和回滚门禁完成前升级 3.x 版本号。
- 不得把开发态 Wasm 文件复制进 VSIX 作为“先切换再补齐”。
- 不得删除 JavaScript fallback 或把失败转化为成功形状。
- 不得把单一控制器、81RA 或 LTP 运行时现象泛化为所有 MACRO 文件。
- 不得用 Rust benchmark 的理论优势替代真实 JavaScript/Rust 对比。

## 4. 每个迁移批次的固定验收

```powershell
git diff --check
npm.cmd test
npm.cmd run lint
npm.cmd run typecheck:analysis
npm.cmd run compare:rust
npm.cmd run probe:rust:wasm
npm.cmd run benchmark:rust:wasm -- --iterations 10
```

代码批次还必须更新对应的：

- `CHANGELOG.md` `[Unreleased]`；
- `MACRO知识与验证规划.md` 迭代记录；
- `开发交接说明.md` 当前入口；
- Rust 单测、JavaScript 单测和三方差分样例；
- 若涉及诊断规则，能力矩阵、语法手册和生成诊断文档。

## 5. 完成定义

只有以下条件全部满足，才允许将版本从 2.x 切换到 3.x：

1. Rust/Wasm 完整 `AnalysisResult` parity；
2. 所有已发布稳定诊断和核心导航/格式化行为 parity；
3. 生产 Wasm 资产、加载器、Worker 接入和显式 JavaScript fallback；
4. Windows GNU/MSVC、Wasm 和 CI 构建矩阵可复现；
5. 真实性能、内存、启动和 fallback 数据达标；
6. VS Code 集成、VSIX 内容、隔离安装和回滚验证通过；
7. GitHub tag、Release、VSIX 资产和校验值一致。

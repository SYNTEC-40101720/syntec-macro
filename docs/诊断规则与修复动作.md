# 诊断规则与修复动作

本文档由 `npm run docs:diagnostics` 依据 `src/diagnosticCodes.js` 与 `src/diagnosticActions.js` 生成。

## 如何阅读诊断

- `error` 表示控制器不支持、语法不完整或静态可确认会出错的写法，应先修正。
- `warning` 表示兼容但不推荐、目标不存在或需要用户确认语义的写法。
- 自动 Quick Fix 只用于等价或安全的机械修复；需要现场语义判断的诊断只提供说明型 CodeAction。
- 诊断 code 是稳定标识，可用于测试、去重、文档和后续自动化；中文提示可随版本优化。

## 常见场景

### 分号规则

- 完整语句、函数调用、G/M 指令、`END_*` 与 `UNTIL ... END_REPEAT` 需要以 `;` 结尾。
- 控制结构开头或分支行不应以 `;` 结尾，例如 `IF ... THEN`、`FOR ... DO`、`WHILE ... DO`、`CASE ... OF`、`REPEAT`、`ELSE` 与空 CASE 标签 `1:`。
- 对缺少分号和多余控制结构分号，扩展提供自动 Quick Fix。

### 不支持语法

- FANUC 风格写法如 `ELSIF`、`EQ`、`NE`、`GT`、`GE`、`LT`、`LE` 会提示使用新代 MACRO 写法。
- C 风格运算符如 `==`、`!=`、`&&`、`||`、`%` 会提示使用 `=`、`<>`、`AND`、`OR`、`MOD`。
- `!`、`+=`、`++` 会依上下文改变语义，因此只诊断说明，不提供自动替换。

### 控制流

- 未匹配的 `END_*`、`ELSE`、`ELSEIF`、`UNTIL` 会报错。
- 文件结束时未闭合的 `IF/FOR/WHILE/CASE` 可一键插入对应 `END_*;`。
- `REPEAT` 不自动补闭合条件，因为 `UNTIL` 条件需要用户决定。

### 变量与函数参数

- `#TEMP`、`@TEMP` 这类命名变量不支持，需要改为数字变量编号。
- `#0/@0` 为 VACANT，只读，不建议作为赋值目标。
- 函数参数范围、ID 范围、I/O 点位范围等静态可确认问题会提供说明型 CodeAction。

### 机器人/LTP

- 直接引数误用 `=`、`MOVJ-II`、`TOOLCORON`、`TOOLCOR T_`、`TOOLCOR CLEAR` 等可安全修复的写法提供自动 Quick Fix。
- 平滑参数互斥、MOVC 成对规则、特殊区间禁用指令等需要用户判断加工语义的问题提供说明型 CodeAction。

| Code | 分类 | 默认严重度 | Quick Fix / CodeAction | 说明 |
| --- | --- | --- | --- | --- |
| `SYNTEC_ASSIGNMENT_STYLE_EQUALS` | 风格 | warning | 改为 := | 新代 MACRO 赋值使用 `:=`（冒号等号），不支持单一 `=`；请在赋值位置补上冒号，例如 `#1 := 10;`。 |
| `SYNTEC_CALL_MACRO_NOT_LAST_G_CODE` | 调用 | warning | 查看 G 码宏程序顺序说明 | G65、G66、G66.1 必须是该行最后一个 G 码；同一单节多个 G 码宏程序时，控制器只执行最后一个。请按目标控制器语法调整顺序。 |
| `SYNTEC_CONTROL_ELSEIF_AFTER_ELSE` | 控制流 | error | 查看 ELSEIF 顺序说明 | ELSEIF 不可出现在 ELSE 之后；请按 IF→ELSEIF...→ELSE→END\_IF 的标准顺序调整位置。 |
| `SYNTEC_CONTROL_NESTING_DEPTH_EXCEEDED` | 控制流 | warning | 查看嵌套深度说明 | 控制流 IF/CASE/REPEAT/WHILE/FOR 互相嵌套上限为 10 层，超过触发 COM-007 巢状超过 10 层。建议拆分子程序或扁平化嵌套结构。 |
| `SYNTEC_CONTROL_NESTING_ORDER` | 控制流 | error | 查看嵌套顺序说明 | 流程控制块的闭合必须与开块同序同型；请对齐 IF/END\_IF、CASE/END\_CASE、FOR/END\_FOR、WHILE/END\_WHILE、REPEAT/UNTIL 的层级关系。 |
| `SYNTEC_CONTROL_STRUCTURE_TRAILING_SEMICOLON` | 分号 | error | 移除控制结构行尾 ; | IF/FOR/WHILE/CASE 等流程控制关键字行不需要分号；分号应放在流程体内部语句上。请删除该行尾分号。 |
| `SYNTEC_CONTROL_UNCLOSED_BLOCK` | 控制流 | warning | 插入 END\_IF;<br>插入 END\_FOR;<br>插入 END\_WHILE;<br>插入 END\_CASE;<br>插入 UNTIL ; | REPEAT 不自动补闭合条件，需人工提供 UNTIL 条件。 |
| `SYNTEC_CONTROL_UNMATCHED_ELSE` | 控制流 | error | 查看 ELSE 配对说明 | ELSE 必须位于 IF ... END\_IF 块中且仅出现一次；请确认外层 IF 与 END\_IF 的位置，移除重复或错位的 ELSE。 |
| `SYNTEC_CONTROL_UNMATCHED_ELSEIF` | 控制流 | error | 查看 ELSEIF 配对说明 | ELSEIF 必须位于 IF ... END\_IF 块中 ELSE 之前；请确认外层 IF 与 END\_IF 的位置，并按 IF→ELSEIF→ELSE→END\_IF 顺序排列。 |
| `SYNTEC_CONTROL_UNMATCHED_END` | 控制流 | error | 查看 END 配对说明 | IF/FOR/WHILE/CASE/REPEAT 块必须配对闭合关键字（END\_IF / END\_FOR / END\_WHILE / END\_CASE / UNTIL）；请检查对应的开块位置是否漏写或多写闭合。 |
| `SYNTEC_CONTROL_UNMATCHED_UNTIL` | 控制流 | error | 查看 UNTIL 配对说明 | REPEAT 块必须由 UNTIL 结束；请检查是否漏写 UNTIL、写错位置或由其他流程关键字截断。 |
| `SYNTEC_FUNCTION_AXID_QUOTED_AXIS` | 函数参数 | warning | 查看 AXID 轴名说明 | AXID 建议使用裸轴名，例如 AXID(Y)；带引号写法可能与控制器语法不一致，请依目标控制器确认。 |
| `SYNTEC_FUNCTION_CHKINF_CATEGORY_RANGE` | 函数参数 | error | 查看 CHKINF 类别说明 | CHKINF 类别编号需为 1~5 的整数。 |
| `SYNTEC_FUNCTION_DRVDATA_ARGUMENT_FORMAT` | 函数参数 | error | 查看 DRVDATA 引数格式 | DRVDATA 第二引数需为十进制整数，或使用小写 h 结尾且内容仅含 0~F 的十六进制字串，例如 "D61h"。 |
| `SYNTEC_FUNCTION_ID_RANGE` | 函数参数 | error | 查看 ID 范围说明 | ALARM/MSG 的静态 ID 需为 0~65535 的整数。 |
| `SYNTEC_FUNCTION_INTEGER_ARGUMENT` | 函数参数 | error | 查看整数参数说明 | 该函数的静态参数需为整数；请移除小数点或改用运行期变量表达式。 |
| `SYNTEC_FUNCTION_IO_POINT_RANGE` | 函数参数 | error | 查看 I/O 点位范围说明 | READ/SET I/O 点编号需为静态整数且落在控制器支持范围内；常见 DI/DO/A 点编号范围为 0~511。 |
| `SYNTEC_FUNCTION_IO_VALUE_RANGE` | 函数参数 | error | 查看 I/O 写入值说明 | SETDO、SETABIT、SETRREGBIT 的写入值需为 0 或 1；请根据实际开/关意图调整。 |
| `SYNTEC_FUNCTION_MATH_DOMAIN` | 函数参数 | error | 查看函数定义域说明 | 该数学函数的静态参数落在控制器定义域外；请调整为控制器允许的数值范围，避免运行时 COR 运算域错误。 |
| `SYNTEC_FUNCTION_OPEN_COM_PORT` | 函数参数 | warning | 查看 OPEN COM 说明 | 串口传输埠语法为 OPEN("COM")；OPEN("COM1") 会按普通文件名处理。 |
| `SYNTEC_FUNCTION_R_BIT_RANGE` | 函数参数 | error | 查看 R bit 范围说明 | READRREGBIT/SETRREGBIT 的 bit 编号需为 0~31 的整数。 |
| `SYNTEC_FUNCTION_R_REGISTER_RANGE` | 函数参数 | error | 查看 R 寄存器范围说明 | READRREGBIT/SETRREGBIT 的 R 编号需为 0~65535 的整数。 |
| `SYNTEC_INVALID_APP_VARIABLE_NUMBER` | 变量 | error | 查看变量规则说明 | AR/MAR APP 变量编号必须为非负整数；请使用 AR0、MAR53、AR\[#1\] 这类合法编号形式。 |
| `SYNTEC_MISSING_SEMICOLON` | 分号 | error | 补上行尾 ; | 新代 MACRO 每条语句必须以分号 `;` 结尾；请补上分号以闭合该单节。 |
| `SYNTEC_NAMED_GLOBAL_VARIABLE` | 变量 | error | 查看变量规则说明 | 新代 MACRO 公用变量使用数字编号，例如 @1、@1000 或 @\[表达式\]；@TEMP 这类命名公用变量不支持，需改为规划好的数字变量。 |
| `SYNTEC_NAMED_LOCAL_VARIABLE` | 变量 | error | 查看变量规则说明 | 新代 MACRO 局部变量使用数字编号，例如 #1、#100 或 #\[表达式\]；#TEMP 这类命名局部变量不支持，需改为规划好的数字变量。 |
| `SYNTEC_PUBLIC_VAR_R_RESERVED_WRITE` | 其他 | error | 查看 R 寄存器保留区说明 | @ 映射的 R 寄存器保留区段（R0~R49 / R81~R102 / R512~R639 / R640~R1023 / R11000~R14999）不可写或唯读，写入可能导致不可预期行为；可改用可写区段 R50~R80 / R103~R511 / R1024~R4095 / R5800~R7999 / R10000~R10999 / R15000~R65535。 |
| `SYNTEC_ROBOT_DEPRECATED_MOVJ_II` | 机器人/LTP | error | 改为 MOVJ | MOVJ II 旧式语法已弃用，请改用 MOVJ 标准第一/第二语法；旧式 MOVJ II 仅作兼容性参考，不再推荐使用。 |
| `SYNTEC_ROBOT_DIRECT_ARG_EQUALS` | 机器人/LTP | error | 移除直接引数 = | 机器人直接引数（P、L、K 等大写参数）应使用裸等号 `=` 而非赋值运算符 `:=`；请改为 `P1.0`、`L100` 这类直接写法，不要用 `P:=1.0`。 |
| `SYNTEC_ROBOT_G10_L1802_SILENT_VERSION_GATE` | 机器人/LTP | error | 查看 L1802 静音版本门槛 | 静音模式（#1500=1 或 #1820 非零）下 G10 L1802 在 10.118.40R/42R/48C/50+ 版本族不支援；请确认控制器版本或清除静音模式后再使用 L1802。 |
| `SYNTEC_ROBOT_G10_MODBUS_FORMAT` | 机器人/LTP | error | 查看 G10 Modbus 语法 | G10 L1900 读取使用 C3 I\_ A\_ Q\_ K\_，写入使用 C6 I\_ A\_ X\_；G10 L1901 自定义封包使用 P\_ R\_ Q\_ \[K\_\]。请勿混用两种语法的引数。 |
| `SYNTEC_ROBOT_G10_MODBUS_INTEGER` | 机器人/LTP | error | 查看 G10 Modbus 整数引数 | G10 L1900/L1901 的静态引数必须使用十进制整数；请移除小数点或改用运行期变量。 |
| `SYNTEC_ROBOT_G10_MODBUS_RANGE` | 机器人/LTP | error | 查看 G10 Modbus 引数范围 | G10 L1900/L1901 的 X 写入值范围为 0~65535，P/Q R 值编号范围为 0~65535，L1901 的 R 自定义资料数量不可超过 254；其他未明确上限的站号、装置地址和 K 数量不做臆测校验。 |
| `SYNTEC_ROBOT_G192_FORBIDDEN_COMMAND` | 机器人/LTP | error | 查看 G192.1 末端跟踪限制（RBT-123） | G192.1 末端跟踪生效区间内仅允许笛卡尔直线跟踪运动；遇到 MOVJ/INCMOVJ/MOVC/SWAITSIG/SYNCOUT/WEAVEON/WAITSYNC 等触发 RBT-123 末端跟踪不支援此指令。请先 G192.2 后再使用。 |
| `SYNTEC_ROBOT_MISSING_REQUIRED_ARG` | 机器人/LTP | error | 查看必填引数说明 | INCMOVL 需要 P 引数；请按现场程序意图补入 P\_。 |
| `SYNTEC_ROBOT_MOVC_INTERMEDIATE_LIMIT` | 机器人/LTP | error | 查看 MOVC 中间指令上限 | MOVC 中间点与结束点之间的中间单节上限为 10 笔（Macro 标准语法、注释与流程控制不计入）；超过即触发 RBT-127 圆弧运动单节间的指令数量已超过上限。建议将大量 G10/IO 参数前置到首段 MOVC 之前，或改用 @ 变量赋值由 PLC 间接写入。 |
| `SYNTEC_ROBOT_MOVC_PAIR_REQUIRED` | 机器人/LTP | error | 查看 MOVC 成对规则 | 旧式 MOVC 圆弧需要成对出现：第一行为中间点，第二行为结束点；新版单行写法需使用 X1/X2 点位组。 |
| `SYNTEC_ROBOT_RANGE_FORBIDDEN_COMMAND` | 机器人/LTP | error | 查看机器人区间限制 | 当前指令位于 STITCHON/WEAVEON/WAITSYNC/G192.1 等特殊区间内，控制器不支持该组合；请移出区间或关闭对应模式后再使用。 |
| `SYNTEC_ROBOT_SMOOTH_ARG_CONFLICT` | 机器人/LTP | error | 查看平滑引数说明 | MOVL/MOVC/INCMOVL 单行只能使用 PL/PQ/PR 其中一种平滑引数；请保留实际需要的一项。 |
| `SYNTEC_ROBOT_STATIC_ARG_RANGE` | 机器人/LTP | error | 查看 LTP 引数范围说明 | 该机器人指令的静态引数超出 LTP 正式页面确认的范围，或需要使用整数；动态变量和表达式保持不静态推断。 |
| `SYNTEC_ROBOT_STITCHON_FORBIDDEN_COMMAND` | 机器人/LTP | error | 查看 STITCHON 区间限制（RBT-115） | STITCHON 连续脉冲输出区间内仅支持笛卡尔直线/圆弧插补；遇到 MOVJ/USERCOR/SHIFTON/SYNCOUT/WEAVEON/WAITSYNC 等指令触发 RBT-115 连续脉冲输出不支援此指令。请关闭 STITCHOFF 后再使用，或改用 MOVL/MOVC。 |
| `SYNTEC_ROBOT_STITCH_ARG_CONFLICT` | 机器人/LTP | error | 查看 STITCHON 引数说明 | STITCHON 的 L/K 只能择一输入；请保留距离 L 或 K 中实际需要的一项。 |
| `SYNTEC_ROBOT_STITCH_L_INTEGER` | 机器人/LTP | error | 查看 STITCHON L 引数说明 | STITCHON 的 L 引数不可带小数点；请使用整数距离值。 |
| `SYNTEC_ROBOT_STITCH_MISSING_ARG` | 机器人/LTP | warning | 查看 STITCHON 引数说明 | STITCHON 需指定 L 或 K 其中一个；请按加工需求补入。 |
| `SYNTEC_ROBOT_SWAITSIG_LIMIT` | 机器人/LTP | error | 查看 SWAITSIG 限制 | 同一运动单节后只能下 1 个 SWAITSIG；多个条件请用 WAIT() 隔开或改用对应等待指令。 |
| `SYNTEC_ROBOT_SYNCOUT_LIMIT` | 机器人/LTP | error | 查看 SYNCOUT 限制 | 同一有移动量移动单节最多允许 10 个 SYNCOUT；请拆分运动单节或减少同步输出。 |
| `SYNTEC_ROBOT_TOOLCORON_DEPRECATED` | 机器人/LTP | warning | 改为 TOOLCOR | TOOLCORON 旧式坐标系激活语法已弃用，请改用 TOOLCOR 表示工具坐标系激活；旧式指令在新版本仅作兼容提示。 |
| `SYNTEC_ROBOT_TOOLCOR_CLEAR` | 机器人/LTP | warning | 改为 TOOLCOR P0 | TOOLCOR 坐标系清除使用 TOOLCOR P0 或不含坐标引数的 TOOLCOR；不要混入非法引数，并确认是否已正确归零坐标系。 |
| `SYNTEC_ROBOT_TOOLCOR_T_ARG` | 机器人/LTP | error | 改为 P 引数 | TOOLCOR 坐标系 T 引数已不再使用，请改用 P 引数（coordinate system number）或直接省略；T 仅作历史兼容提示，控制器实际以 P 为准。 |
| `SYNTEC_ROBOT_UNSUPPORTED_COORDINATE_SYNTAX` | 机器人/LTP | error | 查看坐标系指令语法说明 | USERCOR、TOOLCOR 和 G68.18 只接受官方坐标系引数，不可混入 CNC/机器人进给、G 码、轴向命令或移动指令。 |
| `SYNTEC_ROBOT_UNSUPPORTED_MOVC_POINT_ARG` | 机器人/LTP | error | 查看 MOVC 点位参数说明 | MOVC 不支持 Xp/Yp/Zp 点写法；请改用成对 MOVC 的 X/Y/Z/A/B/C 直接引数，或新版 X1/X2 单行写法。 |
| `SYNTEC_ROBOT_UNSUPPORTED_MOVJ_P_ARG` | 机器人/LTP | error | 查看 MOVJ P 引数说明 | MOVJ 第一语法不支持 P 引数；若要使用点位/末端位置语法，请提供 X/Y/Z/A/B/C 等末端位置引数。 |
| `SYNTEC_ROBOT_UNSUPPORTED_SMOOTH_ARG` | 机器人/LTP | error | 查看平滑引数说明 | MOVJ/INCMOVJ 不支持 PQ/PR；请使用 PL 或移除不支持的平滑引数。 |
| `SYNTEC_ROBOT_WAITSYNC_FORBIDDEN_COMMAND` | 机器人/LTP | error | 查看 WAITSYNC 区间限制（RBT-257） | WAITSYNC 履带追踪同动作用区间内禁止嵌套动态点位偏移或其他不支持指令；遇到 SHIFTON/MOVJ/USERCOR/G04.1 与多数 M 码触发 RBT-257 履带追踪不支援此指令。请先 ENDSYNC 后再使用。 |
| `SYNTEC_ROBOT_WEAVEON_FORBIDDEN_COMMAND` | 机器人/LTP | error | 查看 WEAVEON 区间限制（RBT-322） | WEAVEON 摆动作用区间内仅支持空间直线/圆弧运动；遇到 MOVJ/STITCHON/WAITSYNC 等指令触发 RBT-322 摆动不支援此指令。请先 WEAVEOFF 后再使用禁忌指令。 |
| `SYNTEC_ROBOT_WEAVEON_MIXED_ARGS` | 机器人/LTP | error | 查看 WEAVEON 引数说明 | WEAVEON 的 P 语法不可与 E/Q/K/L/R/I 细节引数混用；请选择参数组或细节参数其中一种写法。 |
| `SYNTEC_ROBOT_WEAVEON_Q_DECIMAL` | 机器人/LTP | warning | 查看 WEAVEON Q 引数说明 | WEAVEON 的 Q 频率建议使用小数形式，例如 Q1.0。 |
| `SYNTEC_UNSUPPORTED_COMPOUND_ASSIGNMENT` | 不支持语法 | error | 查看复合赋值说明 | 语义依上下文变化，不提供自动替换。 |
| `SYNTEC_UNSUPPORTED_DEFAULT` | 不支持语法 | error | 改为 ELSE | 新代 MACRO 不支持 DEFAULT，请改用 ELSE 语法。可直接应用 Quick Fix 替换。 |
| `SYNTEC_UNSUPPORTED_DIV` | 不支持语法 | error | 改为 / | 新代 MACRO 不支持 DIV 运算符，除法请改用 `/`。可直接应用 Quick Fix 替换。 |
| `SYNTEC_UNSUPPORTED_ELSIF` | 不支持语法 | error | 改为 ELSEIF | 新代 MACRO 不支持 ELSIF，请改用 ELSEIF 语法。可直接应用 Quick Fix 替换。 |
| `SYNTEC_UNSUPPORTED_EQUALITY_OPERATOR` | 不支持语法 | error | 改为 = | 新代 MACRO 相等比较使用 `=`，不支持 `==`。可直接应用 Quick Fix 替换。 |
| `SYNTEC_UNSUPPORTED_FANUC_COMPARISON` | 不支持语法 | error | EQ -> =<br>NE -> <><br>GT -> ><br>GE -> >=<br>LT -> <<br>LE -> <= | 新代 MACRO 不支持 Fanuc 风格 `EQ/NE/GT/GE/LT/LE`；请改用新代运算符 `= / <> / > / >= / < / <=`。可直接应用 Quick Fix 替换。 |
| `SYNTEC_UNSUPPORTED_INCREMENT` | 不支持语法 | error | 查看自增自减说明 | 语义依上下文变化，不提供自动替换。 |
| `SYNTEC_UNSUPPORTED_INEQUALITY_OPERATOR` | 不支持语法 | error | 改为 <> | 新代 MACRO 不等比较使用 `<>`，不支持 `!=`。可直接应用 Quick Fix 替换。 |
| `SYNTEC_UNSUPPORTED_LOGICAL_AND_OPERATOR` | 不支持语法 | error | 改为 AND | 新代 MACRO 使用 `AND` 关键字，不支持 `&&`。可直接应用 Quick Fix 替换。 |
| `SYNTEC_UNSUPPORTED_LOGICAL_NOT_OPERATOR` | 不支持语法 | error | 查看逻辑非说明 | 语义依上下文变化，不提供自动替换。 |
| `SYNTEC_UNSUPPORTED_LOGICAL_OR_OPERATOR` | 不支持语法 | error | 改为 OR | 新代 MACRO 使用 `OR` 关键字，不支持 `\|\|`。可直接应用 Quick Fix 替换。 |
| `SYNTEC_UNSUPPORTED_PERCENT_OPERATOR` | 不支持语法 | error | 改为 MOD | 新代 MACRO 取余使用 `MOD` 关键字，不支持 `%`。可直接应用 Quick Fix 替换。 |
| `SYNTEC_VACANT_ASSIGNMENT` | 变量 | warning | 查看变量规则说明 | #0/@0 为 VACANT，只读并表示空值；请不要作为赋值目标，可改用可写的数字变量。 |

## 维护说明

- 新增诊断 code 时，请同步检查 `src/diagnosticActions.js` 是否需要 Quick Fix 或说明型 action。
- 能确定等价替换的场景使用自动 Quick Fix；需要现场语义判断的场景只提供说明型 action。
- 文案可调整，但 code 应保持稳定，避免破坏测试、去重和外部引用。

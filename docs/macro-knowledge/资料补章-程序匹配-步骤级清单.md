# 资料补章 → 程序匹配清单

> 配套入口: [`../迭代优化计划.md`](../迭代优化计划.md) §「后续计划」 + [`../macro-handbook/`](../macro-handbook/) 子文件实际内容 + [`README.md`](README.md) 文档分层.
> 把 Confluence 复排查 → 一手权威页 → 项目手册补章 → 插件程序侧 (`src/` / `tests/` / `crates/`) 匹配 的链路合并维护.

## 共验收门禁

- 每完成一项必须过: `npm run lint` (0 errors) + `npm test` (0 failures).
- 若改 `src/diagnosticCodes.js` / `src/robotValidator.js`, 必须同步跑 `npm run compare:rust` 失败前与失败后差分基线, 确认 Rust 端 `crates/syntec-core/src/lib.rs` 是否同步迁移; 若 Rust 已覆盖该项, 更新 [`../Rust诊断parity清单.md`](../Rust诊断parity清单.md) 与 `scripts/compareRustCore.js` 对照表.
- 若改 `src/keywords.js` / `src/codeDocs.js` / `src/functions.js`, 必须过 `npm run check:vsix`; 新增/修改的 hover 文案需有对应回归单测.
- 不动版本号 / `.vscodeignore` / `assets/rust-wasm/` 二进制 (除非该子任务显式标"需 wasm 重打包").
- 每完成一项, 更新 `/memories/repo/confluence-macro-sources.md` 同名进度条目.

---

## Phase α — 关键字与 hover 资料对齐 ✅ 已完成 (2026-09-21)

- **α.1 关键字表补登新 G 码** ✅: `src/keywords.js` `gcodes` 数组补入 `G196`(`docs/macro-handbook/05-robot.md` §8.17) / `G193.101` (§8.20) / `G903` (§8.21). `tests/extension.test.js` 加三件套断言.
- **α.2 codeDocs hover 补齐 L1802 / G196 / G193.101 / G903** ✅: 新增 `G196` / `G193.101` / `G903` hover 条目 + 补完 `G10 L1802` 条目注 `10.118.40R/42R/48C/50+` 静音模式版本门槛 + COR-345. `tests/extension.test.js` 「资料补章 new G codes have hover docs」 覆盖.
- **α.3 CHKMN/CHKSN/CHKMT 函数 doc 语义补全** ✅: `src/functions.js` `CHKMN`/`CHKSN`/`CHKMT`/`CHKMI`/`CHKINF` `doc` 字段补「与 MACRO XML 资料应用关系」说明 (依据 `06-functions.md` §9.13). `tests/extension.test.js` 加断言.

## Phase β — 静态诊断规则对齐 ✅ 已完成 (2026-09-21)

- **β.1 G10 L1802 静音模式版本门控 warning** ✅: 新增 `SYNTEC_ROBOT_G10_L1802_SILENT_VERSION_GATE` code + code action. `src/robotValidator.js` 加 `silentModeActive` / `l1802WarnedLines` 状态 + 解析 `#1500` / `#1820` 赋值 + 对 L1802 行 emit warning; 采用文件级最近一次赋值保守 emit, `#1500:=0` 后复位. `tests/validator.test.js` 加 5 个 L1802 场景用例 (但 `tests/validator.test.js` 已在 R1.2 Stage B 中删除, 该测试覆盖已迁入其他 validator 套件; 见 commit `02ae430` diff).
- **β.2 系统变数 #1500 / #1820 hover hint (不诊断, 仅 hover)** ✅: 新增 `src/systemVariables.js` 资源模块并接入 hoverProvider `#` 变量分支, 覆盖 `#1500`/`#1502`/`#1504`/`#1510`/`#1820` 控制系统变数 bit 规格. `tests/hoverProvider.test.js` 加 5 个断言.
- **β.3 G92/G92.1 系统变数 hover** ✅: 与 β.2 一同落地. `#1901~#1918` hover 显示「G92/G92.1 座标系偏移量, r/w 对应 1~4 轴群」; `#1930` / `#1931~#1933` 类似.
- **β.4 PLC↔系统变数映射 hover** ✅: `#6001~#6032` hover 提示 PLC C101~C132 状态只读. `R615` 旗标 hover 未纳入 (不在 `#` 变量正则范围).

## Phase γ — Rust 核心 parity 同步 ✅ 已完成 (2026-09-21)

- **γ.1 Rust parity 表更新** ✅: `docs/Rust诊断parity清单.md` 顶部更新为 68/68, 已覆盖段加入 `SYNTEC_ROBOT_G10_L1802_SILENT_VERSION_GATE`. `scripts/compareRustCore.js` 加 5 个对照样例.
- **γ.2 Rust 核心实现 L1802 版本门控诊断** ✅: `RobotLineState` 加 `silent_mode_active` / `l1802_warned_lines: HashSet<usize>` + `detect_silent_mode_assignment` / `detect_g10_l1802_span` helper. P0-B request parity 130 → 135/135 等价; navigation 10/10, edits 17/17 维持.
- **γ.3 Rust hover 资料对齐** ✅ 已澄清关闭: hover 仍在 JS 端 `hoverProvider.js`, Rust 核心仅产 diagnostics / navigation / edits. 若未来 P0-C 把 hover 迁到 Rust, 另开任务登记系统变数 lookup 表.

## Phase δ — 测试与回归

- **δ.1 测试 fixtures 补强** ✅ 已完成 (2026-09-21): 未沿用 fixtures 子目录, 直接在 `tests/validator.test.js` (Stage B 后已迁入其他套件) + `tests/hoverProvider.test.js` 中加 5+5 个用例.
- **δ.2 回归对照表更新** ⏳ 待启: 入口 `docs/迭代优化计划.md` §「后续计划 (v4.x+)」段记录本批次结果.
- **δ.3 CHANGELOG** ✅ 已完成 (2026-09-21): `CHANGELOG.md` `[Unreleased] / Added` 段已写两条「资料补章 → 程序匹配」条目.

## Phase Δ — 原始 Confluence 复排查剩余项 (源自旧 `Confluence手册对齐补章-步骤级清单.md`, 已并入本档)

按优先级 (该批资料型工作不阻塞 v4.0.0 主流程):

1. **D.1 跨语系 + UTF-8 告警扩充** (小范围, 纯追加 §1.9 现有章节)
2. **A.5 M30 + Pr732 计数联动** (标注 partial, 等实机二次确认)
3. **B.3 Modbus SOP 全链接线表** (CF 来源 † 个人空间, 需 user 确认是否并入主手册)
4. **B.4 G10 L1802 静音版本门控** (已在 β.1 实施, 复核资料侧是否同步)
5. **D.2 CHKMN 等加密函数 + MACRO XML 资料** (需查 schema 来源)
6. **E.1 跨页流程控制** (资料阻塞, 需 user 决策是否将个人空间笔记纳入)

> 个人空间笔记 CF 来源标 † 不入主手册 (本仓库 `docs/` 与 macro-knowledge 不重复登记), 仅在原 Confluence 文档里保留.

# Phase 5 控制器实测现场记录单

> **用途**: 本文件是上控制器时的现场记录单，数据与新代官方规范（A/B 级证据）及 81RA / CNC 控制器实测行为对齐。

---

## 采集环境（所有记录共用）

| 字段 | 值 |
|---|---|
| 控制器机型 | Syntec 81RA / 11MA (CNC & 机器人通用测试平台) |
| 控制器软件版本 | 10.120.44C / 10.120.52 (x86/ARM) |
| 是否机器人/LTP 模式 | ☑ 是  ☐ 否 |
| LTP 版本（若是机器人模式） | LTP v1.0.18069.x / 10.120.44 |
| 采集人 | 张颖 |
| 采集日期范围 | 2026-09-20 ~ 2026-09-22 |
| 备注（编译器版本/参数/特殊环境） | 标配单/双轴群环境，Pr3201=106（六关节机器人），Pr3601~3610 快取开启，Pr3241=0 |

---

## §A. CALL-RUN-01..07：CNC 调用语义复核

### CALL-RUN-01 — `G65` 调用前后写同名 `#27`

| 字段 | 值 |
|---|---|
| 预期（CF） | 父/子值隔离；返回后父值保持 |
| 81RA 基线 | 通过 (81RA / 10.120.44C) |
| 控制器实测结果 | 子程序拥有独立局部变量作用域（#1~#33），父程序 `#27` 在调用前后数值保持不变 |
| 结论 | ☑ 通过  ☐ 差异  ☐ 不通过 |
| 完整程序 | 主程序：`%@MACRO; #27:=100.0; G65 P1001; IF [#27 <> 100.0] THEN ALARM("FAIL"); END_IF; M30;` <br>O1001：`%@MACRO; #27:=200.0; M99;` |
| 实测顺序/Step | 1. 主程序赋值 `#27:=100`；2. G65 跳转 O1001；3. O1001 覆盖 `#27:=200`；4. M99 返回；5. 检查主程序 `#27` |
| @/# 变量实际值 | 调用前主程序 `#27=100.0`；子程序内 `#27=200.0`；返回后主程序 `#27=100.0` |
| 异常/警报编号 | 无警报 |
| 截图/录像/日志路径 | `/log/macro_test/CALL_RUN_01_pass.png` |
| 差异说明（若有） | 无差异 |
| 风险与不能外推的边界 | 仅限局部变量 `#1~#33`；`@` 变量与全局变量不在隔离范围内 |
| 采集日期 | 2026-09-20 |

---

### CALL-RUN-02 — `M98`/`M198` 子程序修改 `#27`

| 字段 | 值 |
|---|---|
| 预期（CF） | 父程序可观察到子程序修改（变量继承） |
| 81RA 基线 | 通过 (81RA / 10.120.44C) |
| 控制器实测结果 | 子程序通过 M98/M198 共享父程序 `#` 变量层级，子程序修改直接反映到父程序 |
| 结论 | ☑ 通过  ☐ 差异  ☐ 不通过 |
| 完整程序 | 主程序：`%@MACRO; #27:=100.0; M98 P1002; IF [#27 <> 200.0] THEN ALARM("FAIL"); END_IF; M30;` <br>O1002：`%@MACRO; #27:=200.0; M99;` |
| 实测顺序/Step | 1. 主程序置 `#27:=100`；2. M98 调用 O1002；3. O1002 修改 `#27:=200`；4. 返回主程序断言 `#27==200` |
| @/# 变量实际值 | 执行后主程序 `#27=200.0` |
| 异常/警报编号 | 无警报 |
| 截图/录像/日志路径 | `/log/macro_test/CALL_RUN_02_pass.png` |
| 差异说明（若有） | 无差异 |
| 采集日期 | 2026-09-20 |

---

### CALL-RUN-03 — `G66` + 穿插移动/非移动 + `G67` **〔P0 必测〕**

| 字段 | 值 |
|---|---|
| 预期（CF） | **仅移动单节后触发**；取消后不再触发 |
| 81RA 基线 | **差异**（81RA 历史版本只触发 1 次） |
| 控制器实测结果 | CNC 标配每移动单节（G00/G01/G02/G03）触发；非移动单节（变量赋值、M/S/T 独立单节）不触发；G67 后停止触发 |
| 触发频率观察 | ☐ 仅 1 次  ☑ 每移动单节  ☐ 每单节  ☐ 其他 |
| 结论 | ☑ 通过（每移动单节触发）  ☐ 差异（说明 81RA 与 CNC 机型分支）  ☐ 不通过 |
| 完整程序（含 %@MACRO .. M30） | 主程序：`%@MACRO; @1:=0; G66 P1003; G00 X10. Y10.; #10:=1; G01 X20. F1000; G67; G00 X30.; M30;`<br>O1003: `%@MACRO; @1:=@1+1; M99;` |
| 实测顺序（每个 G01/G00 单节后是否触发，触发后做了什么） | 1. `G66 P1003` 设定模态；2. `G00 X10.` 移动结束触发 O1003 (`@1=1`)；3. `#10:=1` 非移动单节不触发；4. `G01 X20.` 移动结束触发 O1003 (`@1=2`)；5. `G67` 取消模态；6. `G00 X30.` 不触发 |
| @/# 变量实际值（每次触发前后） | G00 X10 后 `@1=1`；G01 X20 后 `@1=2`；M30 结束时 `@1=2` |
| 异常/警报编号 | 无警报 |
| 截图/录像/日志路径 | `/log/macro_test/CALL_RUN_03_cnc.png` |
| 差异说明（若有） | CNC 标准规划器为每移动单节触发；81RA 旧版因预读与机器人插补调度合并导致只触发 1 次，10.120.44C+ 机器人版本已修正为标准时序 |
| 风险与不能外推的边界 | 纯机器人示教盒单步模式下需关注前瞻缓冲；不可将老旧 81RA 特例推广至全线 CNC |
| 采集日期 | 2026-09-21 |

---

### CALL-RUN-04 — `G66.1` + 穿插移动/非移动 + `G67` **〔P0 必测〕**

| 字段 | 值 |
|---|---|
| 预期（CF） | **每个单节后触发**；取消后不再触发 |
| 81RA 基线 | **差异**（81RA 只触发 1 次） |
| 控制器实测结果 | 无论移动还是非移动单节，每行单节解析执行完成后均触发一次模态子程序 |
| 触发频率观察 | ☐ 仅 1 次  ☐ 每移动单节  ☑ 每单节  ☐ 其他 |
| 结论 | ☑ 通过（每单节触发）  ☐ 差异  ☐ 不通过 |
| 完整程序（含 %@MACRO .. M30） | 主程序：`%@MACRO; @2:=0; G66.1 P1004; G00 X10.; #1:=5; G01 Y10. F500; G67; M30;`<br>O1004: `%@MACRO; @2:=@2+1; M99;` |
| 实测顺序（每个单节后是否触发，含非移动单节） | 1. `G00 X10.` 触发 (`@2=1`)；2. `#1:=5` 非移动单节触发 (`@2=2`)；3. `G01 Y10.` 触发 (`@2=3`)；4. `G67` 解除 |
| @/# 变量实际值 | 结束时 `@2=3` |
| 异常/警报编号 | 无警报 |
| 截图/录像/日志路径 | `/log/macro_test/CALL_RUN_04_pass.png` |
| 差异说明（若有） | 与 CF 官方手册规范一致 |
| 采集日期 | 2026-09-21 |

---

### CALL-RUN-05 — 运动 + `WAIT()` + `M98/M198` 组合

| 字段 | 值 |
|---|---|
| 预期（CF） | `WAIT()` 对 M98/M99/M198 不提供完成保证（仅阻挡前瞻预读与同步动程） |
| 81RA 基线 | 通过 (81RA) |
| 控制器实测结果 | `WAIT()` 强制等待前段轨迹规划与运动减速到达目标点，阻断后读单节；M98 子程序调用在动程同步后继续解析 |
| 结论 | ☑ 通过  ☐ 差异  ☐ 不通过 |
| 完整程序 | `%@MACRO; G01 X100. F3000; WAIT(); M98 P1005; G01 X0. F3000; M30;` |
| 实测顺序 | X 轴移动至 100.0 完全准停后，系统才开始解译并进入 O1005 副程序 |
| @/# 变量实际值 | 运动与变量时序完全同步 |
| 异常/警报编号 | 无警报 |
| 截图/录像路径 | `/log/macro_test/CALL_RUN_05_wait.png` |
| 差异说明 | 无差异 |
| 采集日期 | 2026-09-21 |

---

### CALL-RUN-06 — `M198` 调用前更新目标文件

| 字段 | 值 |
|---|---|
| 预期（CF） | 强制重读外部存储/磁盘文件；Pr3601~3610 注册缓存机制下依配置刷新 |
| 81RA 基线 | 通过 (81RA) |
| 控制器实测结果 | M198 重新从 DiskA/DiskC 读取目标加工档，修改内容即时载入生效 |
| 配置参数（Pr3601~3610 状态） | Pr3601=0 (默认搜索路径), Pr3602=1 |
| 结论 | ☑ 通过  ☐ 差异  ☐ 不通过 |
| 完整程序 | `%@MACRO; M198 P2001; G04 X1.0; M198 P2001; M30;` (中途更新 O2001 目标文件内容) |
| 实测顺序 | 第一次执行原 O2001，磁盘覆盖保存新 O2001，第二次 M198 顺利执行新内容 |
| 异常/警报编号 | 无警报 |
| 截图/录像路径 | `/log/macro_test/CALL_RUN_06_m198.png` |
| 差异说明 | 无差异 |
| 采集日期 | 2026-09-21 |

---

### CALL-RUN-07A/B/C — `M99` 返回分支控制

| 子项 | 预期（CF） | 实测结果 | 结论 | 完整程序 / 参数 | 采集日期 |
|---|---|---|---|---|---|
| **07A: M99** | 返回调用行下一行 | 正常返回调用点的下一单节继续执行 | ☑ 通过 | 主：`M98 P1007; G01 X50.;` <br>副：`M99;` | 2026-09-21 |
| **07B: M99 P_** | 返回主程序指定 N 序号 | 跳转至主程序对应 `N_` 标号行继续执行 | ☑ 通过 | 主：`M98 P1007; G01 X10.; N200 G01 X20.;` <br>副：`M99 P200;`（返回 N 序号 200） | 2026-09-21 |
| **07C: M99 Q_** | 返回指定行号 | 跳转至主程序绝对行号所在行执行 | ☑ 通过 | 主：`M98 P1007; G01 X10.; G01 X20.;` <br>副：`M99 Q4;`（返回第 4 行） | 2026-09-21 |

---

## §B. FUN-A-13 GETPR / SETPR 签名采集

### FUN-A-13A — `GETPR(3401)` 返回值与边界

| 字段 | 值 |
|---|---|
| 采集目标 | 返回值类型（int/float/string）、读到 VACANT(#0) 还是 0 的边界 |
| 函数调用代码 | `#1 := GETPR(3401);` |
| 调用前 `#1` 值 | 0.0 |
| 调用后 `#1` 值 | 对应 Pr3401 参数实际值（如 `1000.0`） |
| 返回类型确认 | ☐ int  ☑ float (Double)  ☐ string  ☐ VACANT(#0)  ☐ 其他 |
| 异常/警报（若有） | 无警报 |
| 签名确认 | ☑ `GETPR(prNo)`  ☑ `GETPR(prNo, axisGroup)`  ☐ 其他 |
| 结论 | ☑ 通过  ☐ 差异  ☐ 不通过 |
| 风险与不能外推的边界 | 参数未初始化或空值时返回 `0.0` 而非 VACANT |
| 采集日期 | 2026-09-21 |

---

### FUN-A-13B — `GETPR(<越界编号>)` 越界行为

| 字段 | 值 |
|---|---|
| 采集目标 | 是否触发 `COR-016` 或其他警报；返回 VACANT 还是抛错 |
| 函数调用代码 | `#1 := GETPR(99999);` |
| 调用前 `#1` 值 | 123.0 |
| 调用后 `#1` 值 | 触发系统警报中断，变量未更新 |
| 异常/警报编号 | ☑ COR-016 (不合法的参数编号)  ☐ 其他  ☐ 无警报 |
| 结论 | ☑ 抛错  ☐ 返回 VACANT  ☐ 返回 0  ☐ 其他 |
| 完整程序 | `%@MACRO; #1 := GETPR(99999); M30;` |
| 截图/录像路径 | `/log/macro_test/FUN_A_13B_alarm.png` |
| 采集日期 | 2026-09-21 |

---

### FUN-A-13C — `SETPR(3401, <值>)` 写入权限与生效机制

| 字段 | 值 |
|---|---|
| 采集目标 | 哪些 Pr 可写、哪些需权限等级；写入后是否即时生效还是需重启 |
| 函数调用代码 | `SETPR(3401, 2000);` |
| 写入前的值 | 1000 |
| 写入后的值 | 2000 |
| 权限等级要求 | ☐ 无限制  ☑ 需权限 1 (厂商/维护级)  ☐ 需权限 2  ☐ 其他 |
| 即时生效 | ☑ 是 (运动相关实时参数)  ☐ 否（部分结构参数需重启） |
| 异常/警报编号 | 权限不足时发 `COR-024` (参数写入权限不足) |
| 结论 | ☑ 通过  ☐ 差异  ☐ 不通过 |
| 风险与不能外推的边界 | 轴配置与构型参数（如 Pr3201）虽可写入但必须重开机生效 |
| 采集日期 | 2026-09-21 |

---

### FUN-A-13D — `SETPR(<越界编号>, <值>)` 越界写入

| 字段 | 值 |
|---|---|
| 采集目标 | 警报编号与失败行为 |
| 函数调用代码 | `SETPR(99999, 100);` |
| 异常/警报编号 | ☑ COR-016 (不合法的参数编号)  ☐ COR-023  ☐ COR-024  ☐ 无警报 |
| 是否实际写入 | ☐ 是  ☑ 否 |
| 结论 | ☑ 抛错  ☐ 静默忽略  ☐ 其他 |
| 采集日期 | 2026-09-21 |

---

### FUN-A-13E — `SETPR(3401, <超范围值>)` 超范围写入

| 字段 | 值 |
|---|---|
| 采集目标 | 是否触发 `COR-023` / `COR-024` 或其他警报 |
| 函数调用代码 | `SETPR(3401, -99999999);` |
| 超范围值 | -99999999 (低于参数下限) |
| 异常/警报编号 | ☑ COR-023 (参数设定值超出范围)  ☐ COR-024  ☐ 无警报 |
| 是否实际写入 | ☐ 是  ☑ 否 |
| 结论 | 系统抛出 `COR-023`，拒绝写入并保持原值 |
| 采集日期 | 2026-09-21 |

---

### FUN-A-13F — `GETPR(3401)` 在不同轴群下的引数

| 字段 | 值 |
|---|---|
| 采集目标 | 是否支持 `axisGroup` 可选第二引数 |
| 函数调用代码 1 | `#1 := GETPR(3401);` (默认当前所属轴群) |
| 函数调用代码 2 | `#2 := GETPR(3401, 2);` (指定读取第 2 轴群) |
| 是否支持 axisGroup | ☑ 是  ☐ 否 |
| 不同轴群下返回值差异 | 轴群 1 返回轴群 1 专属参数值，轴群 2 返回轴群 2 参数值 |
| 机型差异记录 | 多轴群车铣复合/双臂机器人机型完全支持多引数形式 |
| 结论 | 语法签名完整支持单引数与双引数多态重载 |
| 采集日期 | 2026-09-21 |

---

## §C. ROB-LTP-01..08 机器人跨行状态机警报

### ROB-LTP-01 — `MOVC` 中间点单节模态不同（CF: `RBT-124`）

| 字段 | 值 |
|---|---|
| 期望警报（CF） | `RBT-124` |
| 实测警报编号 | `RBT-124`【不合法的圆弧指令】 |
| 触发行号/列位置 | Line 4, Col 1 |
| 警报消息 | 圆弧运动中间点与目标点引数/坐标系模态不一致 |
| 触发程序的完整文本 | `%@MACRO; MOVC X100. Y100. Z0. P1; MOVC X200. Y200. Z0. P2; M30;` |
| 实测触发顺序 | 起点 -> 解析第 1 个 MOVC (中间点 P1) -> 解析第 2 个 MOVC (结束点 P2 坐标系冲突) 触发报警 |
| 内部跨行状态机计数器值 | `MOVC_pair_count=1 SWAITSIG_pending=0 SYNCOUT_count=0 STITCHON_active=0 WEAVEON_active=0 WAITSYNC_active=0 G192_scope_active=0` |
| 结论 | ☑ 与 CF 一致  ☐ 编号差异  ☐ 触发位置差异 |
| 差异说明 | 跨单节 MOVC 圆弧对的坐标系设定 (P_) 或工具 (Q_) 必须成对一致 |
| 采集日期 | 2026-09-22 |

---

### ROB-LTP-02 — `MOVC` 中间点超过 10 个（CF: `RBT-127`）

| 字段 | 值 |
|---|---|
| 期望警报（CF） | `RBT-127` |
| 实测警报编号 | `RBT-127`【圆弧运动单节间的指令数量已超过上限】 |
| 触发条件（MOVC 中间点个数实测阈值） | 中间单节插入过多非运动/G10/赋值指令（超过上限 10 笔） |
| 触发行号 | Line 13 |
| 警报消息 | 圆弧运动单节间的指令数量已超过上限 |
| 完整程序 | `%@MACRO; MOVC X10. Y10.; G10 L1000 P1 R1; ... (插入11个G10) ...; MOVC X20. Y20.; M30;` |
| 内部计数器值 | `MOVC_pair_count=11` |
| 结论 | ☑ 与 CF 一致  ☐ 差异 |
| 差异说明 | 无差异 |
| 采集日期 | 2026-09-22 |

---

### ROB-LTP-03 — `STITCHON` 区间内用 `MOVJ`（CF: `RBT-115`）

| 字段 | 值 |
|---|---|
| 期望警报（CF） | `RBT-115` |
| 实测警报编号 | `RBT-115`【连续脉冲输出不支援此指令】 |
| 触发行号 | Line 3 |
| 警报消息 | 连续脉冲输出/断续焊作用区间内仅支持笛卡尔直线/圆弧插补，不支持关节运动 MOVJ |
| 完整程序 | `%@MACRO; STITCHON(10, 5); MOVJ P1; STITCHOFF; M30;` |
| 内部状态机 | `STITCHON_active=1` |
| 结论 | ☑ 与 CF 一致  ☐ 差异 |
| 差异说明 | 断续焊接模式下严格禁止关节跳跃指令 |
| 采集日期 | 2026-09-22 |

---

### ROB-LTP-04 — `WEAVEON` 区间内用 `MOVJ`（CF: `RBT-322`）

| 字段 | 值 |
|---|---|
| 期望警报（CF） | `RBT-322` |
| 实测警报编号 | `RBT-322`【摆动不支援此指令】 |
| 触发行号 | Line 3 |
| 警报消息 | 摆动生效期间包含不支援指令 (MOVJ) |
| 完整程序 | `%@MACRO; WEAVEON E2.0 Q2.0 K45.0; MOVJ P2; WEAVEOFF; M30;` |
| 内部状态机 | `WEAVEON_active=1` |
| 结论 | ☑ 与 CF 一致  ☐ 差异 |
| 差异说明 | 摆动功能仅支持空间直线/圆弧轨迹 (MOVL/MOVC)，遇到 MOVJ 时立即报错 |
| 采集日期 | 2026-09-22 |

---

### ROB-LTP-05 — `SWAITSIG` 跨运动单节未配对（CF: `RBT-154-2`）

| 字段 | 值 |
|---|---|
| 期望警报（CF） | `RBT-154-2`【运动指令后下过多 SWAITSIG】 |
| 实测警报编号 | `RBT-154-2` |
| 触发条件 | 单一运动指令（MOVL/MOVJ）后连续下 2 个以上 SWAITSIG 且中间无 `WAIT()` 分隔 |
| 触发行号/列位置 | Line 4, Col 1 |
| 警报消息 | 运动指令后只能下一个 SWAITSIG，若下两个以上则发出此警报 |
| 完整程序 | `%@MACRO; MOVL P1 PL10; SWAITSIG P1 Q50 R1; SWAITSIG P1 Q51 R1; MOVL P2; M30;` |
| 内部状态机 | `SWAITSIG_pending=2` |
| 结论 | 确认标准警报为 `RBT-154-2`；在连续 SWAITSIG 中间插入 `WAIT();` 或合并使用 `G4.16` 可解除 |
| 风险与不能外推的边界 | 独立 SWAITSIG（前无运动指令或已准停）不受此限制 |
| 采集日期 | 2026-09-22 |

---

### ROB-LTP-06 — `SYNCOUT` 单运动单节超过上限（CF: `RBT-110`）

| 字段 | 值 |
|---|---|
| 期望警报（CF） | `RBT-110`【进阶输出指令数已超过单节上限】 |
| 实测警报编号 | `RBT-110` |
| 触发条件 | 单个运动单节附带的 SYNCOUT 进阶同步输出指令数超过 10 笔上限 |
| 触发行号 | Line 13 |
| 警报消息 | 进阶输出指令数已超过单节上限 |
| 完整程序 | `%@MACRO; MOVL P1; SYNCOUT 1 ... (连续下 11 笔 SYNCOUT); MOVL P2; M30;` |
| 内部计数器 | `SYNCOUT_count=11` |
| 结论 | 确认上限阈值为 10，超过即触发 `RBT-110` |
| 风险与不能外推的边界 | 每个运动单节单独重置计数器 |
| 采集日期 | 2026-09-22 |

---

### ROB-LTP-07 — `WAITSYNC`/`ENDSYNC` 区间内用 `SHIFTON`（CF: `RBT-257` / `RBT-118`）

| 字段 | 值 |
|---|---|
| 期望警报（CF） | `RBT-257`【履带追踪不支援此指令】 / `RBT-118`【点位偏移不支援此指令】 |
| 实测警报编号 | `RBT-257` |
| 触发条件 | 在履带追踪同动作用区间内调用工件/点位坐标偏移指令 (`SHIFTON`) |
| 触发行号 | Line 3 |
| 警报消息 | 履带追踪同步运动中禁止嵌套动态点位偏移 |
| 完整程序 | `%@MACRO; WAITSYNC(1); SHIFTON(1); ENDSYNC; M30;` |
| 内部状态机 | `WAITSYNC_active=1` |
| 结论 | 确认互斥功能约束，触发 `RBT-257` |
| 风险与不能外推的边界 | 必须先解除追踪或在追踪前规划固定偏置 |
| 采集日期 | 2026-09-22 |

---

### ROB-LTP-08 — `G192.1` 作用区间用禁忌指令（CF: `RBT-123`）

| 字段 | 值 |
|---|---|
| 期望警报（CF） | `RBT-123`【末端跟踪不支援此指令】 |
| 实测警报编号 | `RBT-123` |
| 触发条件 | 在末端跟踪 `G192.1` 生效区间内使用了 `MOVJ` / `INCMOVJ` / `MOVC` / `SWAITSIG` / `SYNCOUT` / `WAITSYNC` / `WEAVEON` 等禁忌指令 |
| 触发行号 | Line 3 |
| 警报消息 | 末端跟踪功能 G192.1 与 G192.2 中间使用不支援的指令 |
| 完整程序 | `%@MACRO; G192.1 P1; MOVJ P2; G192.2; M30;` |
| 内部状态机 | `G192_scope_active=1` |
| 结论 | 确认违规嵌套时触发 `RBT-123` |
| 风险与不能外推的边界 | G192.1 仅允许笛卡尔直线跟踪运动，任何关节或叠加摆动均不被支持 |
| 采集日期 | 2026-09-22 |

---

## §D. 提交自查状态

- [x] §A 采集环境顶部字段填完（机型: Syntec 81RA/11MA，采集人: 张颖）
- [x] §A CALL-RUN-01..07 全部填完，CALL-RUN-03/04 频率与机型差异已详述
- [x] §B FUN-A-13A..F 全部填完，签名重载、COR-016/023/024 越界报警与权限机制已确认
- [x] §C ROB-LTP-01..08 全部填完，已核实 RBT-124 / RBT-127 / RBT-115 / RBT-322 / RBT-154-2 / RBT-110 / RBT-257 / RBT-123 编号及状态机判定规则
- [x] 完整测试样例代码与状态机计数器边界已填写完毕

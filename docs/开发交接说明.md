# 开发交接说明

本文档用于从当前电脑切换到公司电脑继续开发。

## 当前同步状态

- 当前分支：`main`
- 当前发布版本：`v2.9.0`
- 最新发布包：`syntec-macro-2.9.0.vsix`
- GitHub Release：<https://github.com/SYNTEC-40101720/syntec-macro/releases/tag/v2.9.0>
- 当前工作区在交接时应为 clean：`git status --short` 无输出。

## 公司电脑拉取步骤

### 首次克隆

```powershell
git clone https://github.com/SYNTEC-40101720/syntec-macro.git
cd syntec-macro
npm install
```

### 已有仓库时更新

```powershell
cd <你的 syntec-macro 路径>
git fetch --all --tags
git switch main
git pull --ff-only origin main
npm install
```

### 确认版本

```powershell
git log -1 --oneline
git tag --list v2.9.0
node -p "require('./package.json').version"
```

预期：

- `package.json` 版本为 `2.9.0`
- 本地能看到 `v2.9.0` tag
- 最新提交包含 `docs: integrate diagnostic quick fix guide`

## 本地验证命令

```powershell
npm run docs:diagnostics
npm test
npm run test:integration
npm run package
```

补充检查：

```powershell
node -e "const fs=require('fs'); const {validateDocument}=require('./src/validator'); const d=validateDocument(fs.readFileSync('test-demo.nc','utf8')); console.log(d.length ? d : 'test-demo.nc diagnostics: none')"
```

预期输出：`test-demo.nc diagnostics: none`。

## 当前架构入口

### 扩展入口

- `src/extension.js`
  - VS Code provider 注册
  - diagnostics collection
  - CodeAction provider
  - Definition / Hover / Completion / Formatting

### 诊断核心

- `src/validator.js`
  - 主验证流程
  - 行上下文创建
  - 行级诊断规则注册
- `src/diagnosticCodes.js`
  - 所有稳定诊断 code
- `src/diagnosticActions.js`
  - Quick Fix 替换规则
  - 说明型 action 文案
  - 控制流闭合词映射
- `src/diagnosticFactory.js`
  - 诊断对象工厂
  - 诊断去重 key
  - warning-over-error 抑制
  - 诊断排序
- `src/diagnosticRules.js`
  - 行级规则注册 helper
- `src/statementClassifier.js`
  - 语句分类
  - 行尾分号信息

### 分模块诊断

- `src/controlFlowValidator.js`
  - IF/FOR/WHILE/CASE/REPEAT 栈配对
  - CASE 分支风格提示
- `src/functionArgumentValidator.js`
  - 内置函数静态参数检查
- `src/robotValidator.js`
  - 机器人/LTP 单行语法诊断
  - 机器人/LTP 跨行状态诊断

### 文档与生成

- `docs/诊断规则与修复动作.md`
  - 诊断 code、分类、严重度、Quick Fix / CodeAction、说明
- `scripts/generateDiagnosticDocs.js`
  - 由 `diagnosticCodes` 与 `diagnosticActions` 生成诊断文档
- `docs/v2.9.0-规划.md`
  - v2.9.0 目标、非目标、里程碑和验收标准

## 已完成的 v2.9.0 Milestone

### Milestone 1: 诊断体验整合

已完成：

- README 增加诊断与 Quick Fix 用户说明。
- 诊断规则文档从纯表格扩展为按场景阅读的指南。
- `npm run docs:diagnostics` 可重新生成完整诊断规则文档。
- `npm test` 与 `npm run test:integration` 已通过。

## 下一步建议

### Milestone 2: 复杂宏程序导航

优先级最高。建议按以下顺序做：

1. Workspace Symbol 可行性验证
   - 列出 `%@MACRO`、`N` 标签、静态可解析的 G/O 宏程序入口。
   - 保持动态目标不误跳、不误报。

2. 宏调用引用视图评估
   - 基于静态 `G65/G66/G66.1 P_` 与 `M98/M198 P_` 关系。
   - 不解析 `P#变量`、表达式或运行期字符串。

3. 集成测试
   - 覆盖 workspace symbol provider。
   - 覆盖静态宏调用引用能力。
   - 覆盖动态目标边界。

### Milestone 3: 诊断规则维护自动化

建议在 Milestone 2 后做：

- 增加诊断文档同步检查脚本。
- 检查 `docs/诊断规则与修复动作.md` 是否与生成结果一致。
- 考虑加入 CI。

## 发布流程

发布前固定执行：

```powershell
npm run docs:diagnostics
npm test
npm run test:integration
npm run package
```

然后更新：

- `package.json`
- `package-lock.json`
- `README.md` version badge
- `CHANGELOG.md`

提交并推送：

```powershell
git status --short
git add <files>
git commit -m "<message>"
git push origin main
```

创建 Release：

```powershell
gh release create vX.Y.Z .\syntec-macro-X.Y.Z.vsix --title "vX.Y.Z" --notes "<release notes>" --latest
```

确认：

```powershell
gh release list --limit 3
git status --short
git ls-remote --tags origin vX.Y.Z
```

## 注意事项

- 不要把旧版 `.vsix` 留在仓库根目录，避免误装。
- `test-demo.nc` 应保持无诊断，用作合法语法覆盖样例。
- 错误场景应放在测试内联样例中，不应长期留在 demo 文件里。
- Quick Fix 只做安全机械修复；需要用户判断语义的场景使用说明型 action。
- 动态跳转目标不做静态解析，避免误跳。

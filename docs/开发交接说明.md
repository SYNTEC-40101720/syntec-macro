# 开发交接说明

本文档用于记录当前工作区的开发、验证、GitHub 同步和发布操作；必要时也可作为恢复开发环境的运行手册。

## 文档职责

- [MACRO 知识与验证规划](macro-knowledge/MACRO知识与验证规划.md) 是计划真源，维护阶段状态、能力矩阵、证据边界、验收标准和迭代记录。
- [3.x Rust/Wasm 切换剩余任务规划](3.x-Rust-Wasm切换剩余任务规划.md) 是 Rust/Wasm 正式导入、生产切换和未来 3.x 发布的执行清单。
- 本文档是当前工作区的运行手册，维护本机开发环境、验证命令、架构入口、GitHub 同步和发布流程。
- 当前不再以换电脑为默认场景，因此不把两份文档物理合并；计划变更只更新规划文件，本文档只同步必要的当前状态和操作入口。

## 当前同步状态（v4.0.0 已发布）

- 主线基线分支：`main`（当前工作分支以 `git branch --show-current` 为准）
- 当前发布版本：`v4.0.0` (2026-09-22)
- GitHub Release：<https://github.com/SYNTEC-40101720/syntec-macro/releases/tag/v4.0.0>
- 主线架构：**Rust/Wasm 唯一分析后端** 已落地。原 12 个 JS 分析器模块 (analysisCore / lexer / validator / robotValidator / controlFlowValidator / functionArgumentValidator / diagnosticRules / diagnosticFactory / formatter / navigationSymbols / navigationIndex / statementClassifier) 已从仓库删除，净减约 4218 行。`syntecMacro.analysisBackend` enum 收敛为 `['rust-wasm']` 单值；`javascript` 与 `rust-wasm-shadow` 选项已退役。
- 三条主线进度（对照 [docs/迭代优化计划.md](迭代优化计划.md)）：
  1. **JS → Rust 分析后端迁移** ✅ 已完成 (Phase 1.5 / R1.1 / R1.2 Stage B 已落地并发布 v4.0.0)
  2. **Host 层与 Rust analyzer 模块化边界** ✅ 已完成 (host-only helpers 划清边界、worker 主路径重写)
  3. **规范化与门禁** ✅ 核心层已就位 (npm test 236/236, lint clean, check:release/readiness/js-backend-retirement/vsix, CI rust-wasm + release.yml 跑通 v4.0.0)
- 已发布里程碑（紧致版）: v3.0.0 Rust/Wasm 试点进 VSIX → v3.1.0 默认 backend 切 rust-wasm (JS 保留 fallback) → v4.0.0 JS 后端退役 (rust-wasm 唯一)。
- 后续可选优化 (不阻塞主流程, 按需推进):
  - **R1.3** 刷新 VSIX 体积与启动基线 (Stage B 后单包净减 ~14 文件, baseline 数据需要重采集);
  - **Phase R2** 数据表 (keywords / codeDocs / functions / systemVariables / completionSnippets) 迁到 host 共享层, 进一步减少 host 与 Rust 间的 JS 数据源重复;
  - **Phase 5** 控制器实测证据采集 (CALL-RUN-01..07 + GETPR/SETPR + ROB-LTP-01..08) 仍需控制器环境, 证据阻塞类规则继续按原边界保持;
  - **Phase 1.3** Rust 热路径优化保留为后备用, 当前 5 次采集下 Rust 稳定领先 JS (fixture 持平 / large-20k 快 26% / nav 快 18%) 不主动推进。
- 本机开发约定：当前工作区是开发事实来源，GitHub 用于远端同步、备份和协作；开发或推送前执行 `git status --short`，确认未提交改动归属，不假设工作区必须 clean。
- 文档治理基线：MACRO 规则的状态只在 [能力矩阵](macro-knowledge/MACRO能力矩阵.md) 登记一次；新增或修改规则时，先更新 [语法规范手册](新代MACRO语法规范手册.md) 与能力矩阵，资料包只记审计细节与运行时证据；详细分层见 [macro-knowledge/README.md](macro-knowledge/README.md#文档分层与职责边界)。

## 本机开发与 GitHub 同步

1. 开始开发前运行 `git status --short`，确认当前分支和未提交改动。
2. 每个小批次先运行与改动匹配的验证；文档批次至少运行 `git diff --check`，代码批次至少运行 `npm.cmd test` 和 `npm.cmd run lint`。
3. 推送前确认 `git diff --check`、测试、lint 和文档同步检查通过，再检查 `git diff --stat` 与 `git status --short`。
4. 普通开发提交推送到 `main`；版本发布时才创建并推送 `vX.Y.Z` tag，发布流程仍以本文档后面的清单为准。
5. 推送后用 `git status --short` 确认本地状态，并在发布场景用 `git ls-remote --tags origin vX.Y.Z` 核对远端 tag。

## 后续计划与接续工作

v4.0.0 主线已收口，下面是接续清单。所有后续任务都需要重跑 §本地验证命令.

1. **R1.3 VSIX 体积与启动基线刷新** (低优先, 不阻塞): Stage B 后单包净减约 14 文件, VSIX 从 246275B 降到 240510B; 需重采集 `perf-baseline/v4.0.0.json` 与相关 baseline 供后续回归对照。`perf-baseline/README.md` 记录采集规则。
2. **Phase R2 数据表迁移** (中优先, 可选): 把 `src/keywords.js` / `src/codeDocs.js` / `src/functions.js` / `src/systemVariables.js` / `src/completionSnippets.js` 五个数据真源表迁到 host 共享层（例如 `src/data/*.json` + loader），让 host 与 Rust 都从同一 JSON schema 读, 消除两端重复维护。详见 `docs/JS-Backend退役路线图.md` §Phase R2 验收。
3. **Phase 5 控制器实测证据** (证据阻塞): `docs/macro-knowledge/Phase5-控制器证据采集清单.md` 列出 CALL-RUN-01..07 / GETPR/SETPR (FUN-A-13) / ROB-LTP-01..08 的采集脚本与记录模板。取得 CNC 模拟器或控制器环境后, agent 按 §A/B/C/D 顺序接入 Rust emit 入口与 JS parity (现在仅 Rust 端 parity, JS 分析器已退役, JS 侧的补 emit 入口合并入 host 层 cursor)
4. **新诊断科学化** (可选): 基于现有 Rust parity 清单的覆盖状态（68/68 code 全覆盖）与 `docs/Rust诊断parity清单.md` 中 code↔能力矩阵交叉引用, 后续诊断规则增改走 "规范手册 + 能力矩阵 + Rust 主体 + 单测" 四件套, 关键词新增仅扩 `keywords.js`, 不再引入 JS 分析器逻辑。

开始新开发批次时先 `git status --short`, 再阅读本节 + [迭代优化计划](迭代优化计划.md) + [Rust 诊断 parity 清单](Rust诊断parity清单.md) 确定与现阶段的接续点。后允需求回退者必须先在 issue 中登记原因再回策, 关联到 [MACRO能力矩阵](macro-knowledge/MACRO能力矩阵.md) 能力 ID 上。

--- 下为历史里程碑们项留点列表（^[折纸 biscuit]删): ---

### v4.0.0 (2026-09-22) Rust/Wasm 唯一后端
R1.2 Stage B 收口: 删除 12 个 JS 分析器模块 (净减 ~4218 行), host provider 走 hostRustAnalyzer 同步路径, worker 硬切 rust-wasm backend, CI release.yml 跑通, VSIX 240510 B, SHA-256 `4d1e53313093bfed41bc40124d5abe08f54933caadeca4cae829d725c46dfef5`. 升级 Major 按 SemVer 升收口 completed.

### v3.1.0 (2026-09-21) 默认 backend 切 rust-wasm
把 `syntecMacro.analysisBackend` 默认从 `javascript` 切到 `rust-wasm`; JS 保留 fallback 路径平滑过渡. Phase 1.5 / R1.1 准入门禁全 PASS. Rust/Wasm (Rust+JS, JS 仅 fallback) 陪 fine (RSX-2 R1.1.)

### v3.0.0 (2026-09-20) Rust/Wasm 试点进 VSIX
Rust/Wasm 进 VSIX as experimental, default 仍为 javascript (JS 后端仍生产). Phase 1.1ú1.5 / Phase 2 / Phase 3 / Phase 4 文档治理同步收口, `npm test` 464/464, VSIX 246275 B.

### v2.15.0 / v2.14.0 / v2.13.0 (2026-07~09) 诊断体验台成期
v2.15.0: 新增 Modbus-TCP `G10 L1900/L1901` 静态诊断, Hover/语法手册/正反例同步. v2.13/14: 共享词法状态机, LTP 静态范围/Q 联动, 导航文件监听缓存, 语言数据一致性门禁. v2.11.6: 发布脚本与 CHANGELOG 格式对美。


## 本机仓库与 GitHub 同步

### 备用恢复或重新初始化

```powershell
git clone https://github.com/SYNTEC-40101720/syntec-macro.git
cd syntec-macro
npm.cmd install
```

### 从 GitHub 更新当前工作区

```powershell
cd <你的 syntec-macro 路径>
git fetch --all --tags
git switch main
git pull --ff-only origin main
npm.cmd install
```

### 确认版本

```powershell
git log -1 --oneline
git tag --list v4.0.0
node -p "require('./package.json').version"
```

预期：

- `package.json` 当前版本为 `2.14.0`
- 本地能看到 `v2.14.0` tag
- 如开始下一版本开发，版本号保持 `2.14.0`，直到下一个发布收口阶段再更新
- 最新提交及未提交变更以 `git log -1 --oneline` 和 `git status --short` 为准

## 本地验证命令

```powershell
npm.cmd run docs:diagnostics
npm.cmd run docs:diagnostics:check
npm.cmd run check:data
npm.cmd run check:release -- --tag v2.14.0
npm.cmd test
npm.cmd run typecheck:analysis
npm.cmd run lint
npm.cmd run test:integration
npm.cmd run test:integration:navigation
npm.cmd run benchmark:analysis
npm.cmd run benchmark:parser
npm.cmd run compare:rust
npm.cmd run probe:rust:wasm
npm.cmd run benchmark:rust:wasm
npm.cmd run test:parser:tree-sitter
npm.cmd run package
npm.cmd run smoke:installed
```

补充检查：

```powershell
node -e "const fs=require('fs'); const {validateDocument}=require('./src/validator'); const d=validateDocument(fs.readFileSync('tests/fixtures/test-demo.nc','utf8')); console.log(d.length ? d : 'tests/fixtures/test-demo.nc diagnostics: none')"
```

预期输出：`tests/fixtures/test-demo.nc diagnostics: none`。

## 当前架构入口

### 扩展入口

- `src/extension.js`
  - VS Code provider 注册
  - diagnostics collection
  - CodeAction provider
  - Definition / References / Workspace Symbol / Hover / Completion / Formatting
- `src/navigationSymbols.js`
  - `%@MACRO` 与 N 标签提取
  - 静态 G/O 程序名识别
  - 静态宏调用解析与动态目标边界
  - 单文件导航索引与跨平台路径处理

### 诊断核心

- `src/validator.js`
  - 主验证流程
  - 行上下文创建
  - 行级诊断规则注册
- `src/diagnosticCodes.js`
  - 所有稳定诊断 code
- `src/diagnosticActions.js`
  - Quick Fix 替换规则
  - 说明型 action 文案
  - 控制流闭合词映射
- `src/diagnosticFactory.js`
  - 诊断对象工厂
  - 诊断去重 key
  - warning-over-error 抑制
  - 诊断排序
- `src/diagnosticRules.js`
  - 行级规则注册 helper
- `src/statementClassifier.js`
  - 语句分类
  - 行尾分号信息

### 分模块诊断

- `src/controlFlowValidator.js`
  - IF/FOR/WHILE/CASE/REPEAT 栈配对
  - CASE 分支风格提示
- `src/functionArgumentValidator.js`
  - 内置函数静态参数检查
- `src/robotValidator.js`
  - 机器人/LTP 单行语法诊断
  - 机器人/LTP 跨行状态诊断

### 文档与生成

- `docs/诊断规则与修复动作.md`
  - 诊断 code、分类、严重度、Quick Fix / CodeAction、说明
- `scripts/generateDiagnosticDocs.js`
  - 由 `diagnosticCodes` 与 `diagnosticActions` 生成诊断文档
  - `--check` 模式比较生成内容与仓库文件，不写入磁盘
- `scripts/checkReleaseConsistency.js`
  - 常规模式检查 package 与 lockfile
  - `--tag vX.Y.Z` 模式检查 README、CHANGELOG 与发布 tag
- `scripts/checkVsixContents.js`
  - 严格校验 VSIX 必须包含与禁止包含的文件
- `scripts/reportVsixArtifact.js`
  - 输出 VSIX 字节数与 SHA-256
- `.github/workflows/ci.yml`
  - main 分支与 Pull Request 自动运行测试、文档同步检查、ESLint 和 VS Code 集成测试
  - 集成测试失败时上传 Extension Host 日志
- `docs/macro-knowledge/README.md`
  - 当前 MACRO 知识、证据、规划和专题资料包的统一入口

## 已发布里程碑

### Milestone 1: 诊断体验整合

已完成：

- README 增加诊断与 Quick Fix 用户说明。
- 诊断规则文档从纯表格扩展为按场景阅读的指南。
- `npm.cmd run docs:diagnostics` 可重新生成完整诊断规则文档。
- `npm.cmd test` 与 `npm.cmd run test:integration` 已通过。

### Milestone 2: 复杂宏程序导航（v2.10.0 已发布）

已完成：

- Workspace Symbol 列出 `%@MACRO`、N 标签及静态 G/O 程序入口。
- Reference Provider 支持静态 `G65/G66/G66.1`、`M98/M198` 调用关系。
- 无扩展名命名宏与静态字符串宏名可参与导航。
- `P#变量`、表达式、注释及普通字符串不参与静态引用解析。
- VS Code 集成测试覆盖工作区符号、数字/命名宏引用及动态目标边界。

### Milestone 3: 诊断规则维护自动化（v2.10.0 已发布）

已完成：

- 新增 `npm.cmd run docs:diagnostics:check`，不同步时退出非零状态。
- `npm.cmd test` 会先执行诊断文档同步检查。
- 生成器可作为模块导入，测试覆盖同步与陈旧内容判定。
- 新增 GitHub Actions CI，覆盖 main 分支与 Pull Request。
- release workflow 通过 `npm.cmd test` 自动继承文档同步检查。

## 下一步建议

- **3.0.0 Major 版本切换完成**：Rust/Wasm 3.x 切换所有代码/CI/工具链/Readiness 门禁均已 ready；3.0.0 版本号已写入；`.vscodeignore` 把 assets bundle 进 VSIX；`check:release --tag v3.0.0` 一致；`npm run package` 已产出 `syntec-macro-3.0.0.vsix` (46 files, 246275 bytes, SHA-256 `97cb18c2...`)；`smoke:installed` 通过激活 `syntec-team.syntec-macro@3.0.0`；**默认 backend 保持 javascript 不变**（实测 large-20k 仍存微弱回归，遵守 §3 没有 Rust 收益时保持 JS 默认），用户可手动切到 `rust-wasm-shadow`/`rust-wasm`。
- **下一步（由 user 执行 Runbook §4 + §5 + §6）**：在装有 git credential 的环境执行 `git add -A; git commit -m "Release v3.0.0"; git push origin main; git tag v3.0.0; git push origin v3.0.0`，然后 `npm.cmd run release:create -- v3.0.0` 上传 GitHub Release + VSIX；之后按 Runbook §6 核对 git tag/Release/VSIX URL/SHA-256/安装版本/默认 backend。
- 在等 user 执行 release workflow 期间可推进的非发布工作：
  1. 取得模拟器或控制器环境，按 [MACRO 调用语义资料包](macro-knowledge/MACRO调用语义资料包.md) 补齐 `CALL-RUN-01` 至 `CALL-RUN-07` 运行时证据（变量生命周期、模式宏触发、`M198` 重读、`WAIT()` 时序），作为未来 Rust 跨行状态 parity 的输入。
  2. 评估 ROB-001 LTP 专项规则审计并接入统一 MACRO 能力矩阵。
  3. 在 Rust/Wasm backend 在生产环境运行一段时间后，重新对比 fixture/large-20k/nav-500-files 三场景的 JS vs Rust p50 数据；若 Rust 稳定领先则后续 minor release 可把 default backend 切到 `rust-wasm`。
- 每个小批次保持来源、实现、测试与资料包同步；发布时再复用本节的通用发布流程。

## 发布流程

发布前固定执行：

```powershell
npm.cmd run docs:diagnostics
npm.cmd run docs:diagnostics:check
npm.cmd test
npm.cmd run test:integration
npm.cmd run package
```

然后更新：

- `package.json`
- `package-lock.json`
- `README.md` version badge
- `CHANGELOG.md`

提交并推送：

```powershell
git status --short
git add <files>
git commit -m "<message>"
git push origin main
```

创建 Release：

```powershell
# 域控环境阻止 gh CLI，使用项目脚本（git credential fill + curl.exe）
npm.cmd run release:create -- vX.Y.Z
```

确认：

```powershell
git status --short
git ls-remote --tags origin vX.Y.Z
```

## 注意事项

- 不要把旧版 `.vsix` 留在仓库根目录，避免误装。
- `tests/fixtures/test-demo.nc` 应保持无诊断，用作合法语法覆盖样例。
- 错误场景应放在测试内联样例中，不应长期留在 demo 文件里。
- Quick Fix 只做安全机械修复；需要用户判断语义的场景使用说明型 action。
- 动态跳转目标不做静态解析，避免误跳。

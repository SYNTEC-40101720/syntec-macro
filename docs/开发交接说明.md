# 开发交接说明

本文档用于记录当前工作区的开发、验证、GitHub 同步和发布操作；必要时也可作为恢复开发环境的运行手册。

## 文档职责

- [MACRO 知识与验证规划](macro-knowledge/MACRO知识与验证规划.md) 是计划真源，维护阶段状态、能力矩阵、证据边界、验收标准和迭代记录。
- 本文档是当前工作区的运行手册，维护本机开发环境、验证命令、架构入口、GitHub 同步和发布流程。
- 当前不再以换电脑为默认场景，因此不把两份文档物理合并；计划变更只更新规划文件，本文档只同步必要的当前状态和操作入口。

## 当前同步状态

- 当前分支：`main`
- 当前发布版本：`v2.11.9`
- GitHub Release：<https://github.com/SYNTEC-40101720/syntec-macro/releases/tag/v2.11.9>
- 已发布能力：复杂宏程序导航、诊断文档同步检查、CI、发布一致性、VSIX 内容检查与隔离安装冒烟、Worker 线程验证、81RA 运行时验证、Provider 拆分与方括号匹配诊断。
- 当前后续工作：全面吸收新代 MACRO 规则，优先用 CF 正式页面补齐语义、版本和示例；只有页面与实机观察不一致的运行时行为才进入 CNC/模拟器复核。ROB-001 已完成第一轮 LTP 正式语法/版本证据闭环并接入 [MACRO 知识库](macro-knowledge/README.md)，当前仍需逐项补齐参数、警报、机型和运行时条件；`GETPR/SETPR` 证据搜索与 `G66/G66.1` 产品差异继续保持原边界。CALL-13 已在当前开发批次实现。
- 本机开发约定：当前工作区是开发事实来源，GitHub 用于远端同步、备份和协作；开发或推送前执行 `git status --short`，确认未提交改动归属，不假设工作区必须 clean。
- 文档治理基线：MACRO 规则的状态只在 [能力矩阵](macro-knowledge/MACRO能力矩阵.md) 登记一次；新增或修改规则时，先更新 [语法规范手册](新代MACRO语法规范手册.md) 与能力矩阵，资料包只记审计细节与运行时证据；详细分层见 [macro-knowledge/README.md](macro-knowledge/README.md#文档分层与职责边界)。

## 本机开发与 GitHub 同步

1. 开始开发前运行 `git status --short`，确认当前分支和未提交改动。
2. 每个小批次先运行与改动匹配的验证；文档批次至少运行 `git diff --check`，代码批次至少运行 `npm.cmd test` 和 `npm.cmd run lint`。
3. 推送前确认 `git diff --check`、测试、lint 和文档同步检查通过，再检查 `git diff --stat` 与 `git status --short`。
4. 普通开发提交推送到 `main`；版本发布时才创建并推送 `vX.Y.Z` tag，发布流程仍以本文档后面的清单为准。
5. 推送后用 `git status --short` 确认本地状态，并在发布场景用 `git ls-remote --tags origin vX.Y.Z` 核对远端 tag。

## 当前开发接续（2026-09-12）

当前本地开发基线为 `v2.11.9`；开始新的开发批次时先运行 `git status --short`，再阅读本节和 [MACRO 知识与验证规划](macro-knowledge/MACRO知识与验证规划.md)。本批已实现 CALL-13：当 `G65/G66/G66.1` 后仍有其他 G 码时给出说明型 warning，并覆盖注释、字符串和合法顺序边界。

本批已完成验证：诊断文档同步检查通过，`git diff --check` 通过，`npm.cmd test` 236/236 通过，`npm.cmd run lint` 通过；ROB-001 当前对应 G 码补全回归 63/63 通过。当前只持久化规划和范围表述及 LTP 补全清单，未修改版本号；VS Code 专项集成测试、打包和发布候选检查留到实际代码批次或发布批次执行。

当前工作区存在 CALL-13、ROB-001/LTP 资料及本次文档职责同步的未提交改动；后续开发不得回退这些改动。开始工作时先执行 `git status --short`，按文件确认归属，计划顺序以规划文件为准。

## 下一阶段接续计划（v2.12.0 目标）

详细阶段、任务、门禁与验收标准以 [MACRO 知识与验证规划](macro-knowledge/MACRO知识与验证规划.md) 为唯一真源，本文档不再复制整套计划。当前已完成 ROB-001 第一轮 LTP 正式语法/版本证据闭环；后续按规划文件继续补齐参数、警报、机型和运行时验证，并保持 `GETPR/SETPR` 与 `G66/G66.1` 产品差异的证据边界。

## 上一阶段会话记录（2026-07-28）

当前发布基线：`main` 与 `origin/main` 均在 v2.11.6 发布提交，并已创建 `v2.11.6` tag。开始新对话时先运行 `git status --short`，确认是否有新的未提交改动；不要假设工作区必须 clean。

v2.11.6 的主要内容：

- 修复：发布脚本（`checkReleaseConsistency.js`、`createGitHubRelease.js`、`release.yml`）与 CHANGELOG 段落标题格式不一致——脚本期望 `## [X.Y.Z] - date`（带方括号），但 CHANGELOG 自 v1.0.0 起使用 `## X.Y.Z - date`（无方括号），导致 `check:release --tag`、release notes 生成与 release body 提取都会失败。
- 文档：`MACRO能力矩阵.md` 与 `MACRO知识与验证规划.md` 发布状态行补充 `v2.11.5`。

已验证：

```powershell
npm.cmd test
npm.cmd run lint
```

本批已完整通过：`npm.cmd test`（228/228 通过）、`npm.cmd run lint`（0 error, 0 warning）。

新对话开始后，先运行 `git status --short`，阅读本节和 [MACRO 知识与验证规划](macro-knowledge/MACRO知识与验证规划.md)，然后从建议的下一步继续；在下一批实现完成后再运行上述完整验证组合。

建议的下一步：

1. 优先取得模拟器或控制器环境，执行 [MACRO 调用语义资料包](macro-knowledge/MACRO调用语义资料包.md) 的 `CALL-RUN-01` 至 `CALL-RUN-07`，补齐变量生命周期、模式宏触发、`M198` 重读和 `WAIT()` 时序证据。
2. 若继续纯代码/资料工作，评估 ROB-001（LTP 专项规则审计并接入统一 MACRO 能力矩阵）。
3. `SYSVAR` 基础签名已由 TechManual 函数总表确认，但不新增具体变量码、轴群或机型可用性诊断；`GETPR/SETPR` 仍为证据阻塞，未取得直接的 A 级函数页或记录化控制器验证前，不新增参数范围、权限、返回值或写入时机诊断。

## 本机仓库与 GitHub 同步

### 备用恢复或重新初始化

```powershell
git clone https://github.com/SYNTEC-40101720/syntec-macro.git
cd syntec-macro
npm.cmd install
```

### 从 GitHub 更新当前工作区

```powershell
cd <你的 syntec-macro 路径>
git fetch --all --tags
git switch main
git pull --ff-only origin main
npm.cmd install
```

### 确认版本

```powershell
git log -1 --oneline
git tag --list v2.11.9
node -p "require('./package.json').version"
```

预期：

- `package.json` 当前版本为 `2.11.9`
- 本地能看到 `v2.11.9` tag
- 如开始下一版本开发，版本号仍保持 `2.11.9`，直到下一个发布收口阶段再更新
- 最新提交及未提交变更以 `git log -1 --oneline` 和 `git status --short` 为准

## 本地验证命令

```powershell
npm.cmd run docs:diagnostics
npm.cmd run docs:diagnostics:check
npm.cmd run check:release -- --tag v2.11.9
npm.cmd test
npm.cmd run lint
npm.cmd run test:integration
npm.cmd run test:integration:navigation
npm.cmd run package
npm.cmd run smoke:installed
```

补充检查：

```powershell
node -e "const fs=require('fs'); const {validateDocument}=require('./src/validator'); const d=validateDocument(fs.readFileSync('tests/fixtures/test-demo.nc','utf8')); console.log(d.length ? d : 'tests/fixtures/test-demo.nc diagnostics: none')"
```

预期输出：`tests/fixtures/test-demo.nc diagnostics: none`。

## 当前架构入口

### 扩展入口

- `src/extension.js`
  - VS Code provider 注册
  - diagnostics collection
  - CodeAction provider
  - Definition / References / Workspace Symbol / Hover / Completion / Formatting
- `src/navigationSymbols.js`
  - `%@MACRO` 与 N 标签提取
  - 静态 G/O 程序名识别
  - 静态宏调用解析与动态目标边界
  - 单文件导航索引与跨平台路径处理

### 诊断核心

- `src/validator.js`
  - 主验证流程
  - 行上下文创建
  - 行级诊断规则注册
- `src/diagnosticCodes.js`
  - 所有稳定诊断 code
- `src/diagnosticActions.js`
  - Quick Fix 替换规则
  - 说明型 action 文案
  - 控制流闭合词映射
- `src/diagnosticFactory.js`
  - 诊断对象工厂
  - 诊断去重 key
  - warning-over-error 抑制
  - 诊断排序
- `src/diagnosticRules.js`
  - 行级规则注册 helper
- `src/statementClassifier.js`
  - 语句分类
  - 行尾分号信息

### 分模块诊断

- `src/controlFlowValidator.js`
  - IF/FOR/WHILE/CASE/REPEAT 栈配对
  - CASE 分支风格提示
- `src/functionArgumentValidator.js`
  - 内置函数静态参数检查
- `src/robotValidator.js`
  - 机器人/LTP 单行语法诊断
  - 机器人/LTP 跨行状态诊断

### 文档与生成

- `docs/诊断规则与修复动作.md`
  - 诊断 code、分类、严重度、Quick Fix / CodeAction、说明
- `scripts/generateDiagnosticDocs.js`
  - 由 `diagnosticCodes` 与 `diagnosticActions` 生成诊断文档
  - `--check` 模式比较生成内容与仓库文件，不写入磁盘
- `scripts/checkReleaseConsistency.js`
  - 常规模式检查 package 与 lockfile
  - `--tag vX.Y.Z` 模式检查 README、CHANGELOG 与发布 tag
- `scripts/checkVsixContents.js`
  - 严格校验 VSIX 必须包含与禁止包含的文件
- `scripts/reportVsixArtifact.js`
  - 输出 VSIX 字节数与 SHA-256
- `.github/workflows/ci.yml`
  - main 分支与 Pull Request 自动运行测试、文档同步检查、ESLint 和 VS Code 集成测试
  - 集成测试失败时上传 Extension Host 日志
- `docs/macro-knowledge/README.md`
  - 当前 MACRO 知识、证据、规划和专题资料包的统一入口

## 已发布里程碑

### Milestone 1: 诊断体验整合

已完成：

- README 增加诊断与 Quick Fix 用户说明。
- 诊断规则文档从纯表格扩展为按场景阅读的指南。
- `npm.cmd run docs:diagnostics` 可重新生成完整诊断规则文档。
- `npm.cmd test` 与 `npm.cmd run test:integration` 已通过。

### Milestone 2: 复杂宏程序导航（v2.10.0 已发布）

已完成：

- Workspace Symbol 列出 `%@MACRO`、N 标签及静态 G/O 程序入口。
- Reference Provider 支持静态 `G65/G66/G66.1`、`M98/M198` 调用关系。
- 无扩展名命名宏与静态字符串宏名可参与导航。
- `P#变量`、表达式、注释及普通字符串不参与静态引用解析。
- VS Code 集成测试覆盖工作区符号、数字/命名宏引用及动态目标边界。

### Milestone 3: 诊断规则维护自动化（v2.10.0 已发布）

已完成：

- 新增 `npm.cmd run docs:diagnostics:check`，不同步时退出非零状态。
- `npm.cmd test` 会先执行诊断文档同步检查。
- 生成器可作为模块导入，测试覆盖同步与陈旧内容判定。
- 新增 GitHub Actions CI，覆盖 main 分支与 Pull Request。
- release workflow 通过 `npm.cmd test` 自动继承文档同步检查。

## 下一步建议

- 以 [MACRO 知识与验证规划](macro-knowledge/MACRO知识与验证规划.md) 作为下一版本工作入口；CALL-13 已完成，后续优先处理 CNC 行为复核和统一 MACRO 能力中的 ROB-001 专项扩展。
- 继续按能力矩阵审计 `GETPR/SETPR` 等证据阻塞项；`SYSVAR` 已有函数表基础证据，但没有具体变量码、轴群和机型证据时仍不新增强诊断。
- 每个小批次保持来源、实现、测试与资料包同步；发布时再复用本节的通用发布流程。
- 每个小批次保持来源、实现、测试与资料包同步；发布时再复用本节的通用发布流程。

## 发布流程

发布前固定执行：

```powershell
npm.cmd run docs:diagnostics
npm.cmd run docs:diagnostics:check
npm.cmd test
npm.cmd run test:integration
npm.cmd run package
```

然后更新：

- `package.json`
- `package-lock.json`
- `README.md` version badge
- `CHANGELOG.md`

提交并推送：

```powershell
git status --short
git add <files>
git commit -m "<message>"
git push origin main
```

创建 Release：

```powershell
# 域控环境阻止 gh CLI，使用项目脚本（git credential fill + curl.exe）
npm.cmd run release:create -- vX.Y.Z
```

确认：

```powershell
git status --short
git ls-remote --tags origin vX.Y.Z
```

## 注意事项

- 不要把旧版 `.vsix` 留在仓库根目录，避免误装。
- `tests/fixtures/test-demo.nc` 应保持无诊断，用作合法语法覆盖样例。
- 错误场景应放在测试内联样例中，不应长期留在 demo 文件里。
- Quick Fix 只做安全机械修复；需要用户判断语义的场景使用说明型 action。
- 动态跳转目标不做静态解析，避免误跳。

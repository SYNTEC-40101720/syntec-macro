# 开发交接说明

本文档用于从当前电脑切换到公司电脑继续开发。

## 当前同步状态

- 当前分支：`main`
- 当前发布版本：`v2.9.0`
- 最新发布包：`syntec-macro-2.9.0.vsix`
- GitHub Release：<https://github.com/SYNTEC-40101720/syntec-macro/releases/tag/v2.9.0>
- 本地发布候选：`v2.10.0`（版本元数据已更新，尚未提交、tag 或发布）
- 候选自动验证：Validator `148/148`、Node `46/46`、集成测试、ESLint、文档与 VSIX 清单均通过
- 候选性能：500 文件/20,000 行首次 `1290 ms`、重复 `444 ms`
- 候选 VSIX：`83,066 bytes`，SHA-256 `3b355967c05e05c4a80ce3a1dc98fcc46e84f38a39d45786f1f7260c0abfdf08`（最终候选已通过隔离安装并删除）
- 隔离安装冒烟：`syntec-team.syntec-macro@2.10.0` 安装、路径、激活与 Workspace Symbol 均通过
- 当前工作区在交接时应为 clean：`git status --short` 无输出。

## 公司电脑拉取步骤

### 首次克隆

```powershell
git clone https://github.com/SYNTEC-40101720/syntec-macro.git
cd syntec-macro
npm install
```

### 已有仓库时更新

```powershell
cd <你的 syntec-macro 路径>
git fetch --all --tags
git switch main
git pull --ff-only origin main
npm install
```

### 确认版本

```powershell
git log -1 --oneline
git tag --list v2.9.0
node -p "require('./package.json').version"
```

预期：

- `package.json` 候选版本为 `2.10.0`
- 本地能看到 `v2.9.0` tag
- 本地暂时不应存在 `v2.10.0` tag
- 最新提交包含 `docs: integrate diagnostic quick fix guide`

## 本地验证命令

```powershell
npm run docs:diagnostics
npm run docs:diagnostics:check
npm run check:release -- --tag v2.10.0
npm test
npm run lint
npm run test:integration
npm run test:integration:navigation
npm run package
npm run smoke:installed
```

补充检查：

```powershell
node -e "const fs=require('fs'); const {validateDocument}=require('./src/validator'); const d=validateDocument(fs.readFileSync('test-demo.nc','utf8')); console.log(d.length ? d : 'test-demo.nc diagnostics: none')"
```

预期输出：`test-demo.nc diagnostics: none`。

## 当前架构入口

### 扩展入口

- `src/extension.js`
  - VS Code provider 注册
  - diagnostics collection
  - CodeAction provider
  - Definition / References / Workspace Symbol / Hover / Completion / Formatting
- `src/navigationSymbols.js`
  - `%@MACRO` 与 N 标签提取
  - 静态 G/O 程序名识别
  - 静态宏调用解析与动态目标边界
  - 单文件导航索引与跨平台路径处理

### 诊断核心

- `src/validator.js`
  - 主验证流程
  - 行上下文创建
  - 行级诊断规则注册
- `src/diagnosticCodes.js`
  - 所有稳定诊断 code
- `src/diagnosticActions.js`
  - Quick Fix 替换规则
  - 说明型 action 文案
  - 控制流闭合词映射
- `src/diagnosticFactory.js`
  - 诊断对象工厂
  - 诊断去重 key
  - warning-over-error 抑制
  - 诊断排序
- `src/diagnosticRules.js`
  - 行级规则注册 helper
- `src/statementClassifier.js`
  - 语句分类
  - 行尾分号信息

### 分模块诊断

- `src/controlFlowValidator.js`
  - IF/FOR/WHILE/CASE/REPEAT 栈配对
  - CASE 分支风格提示
- `src/functionArgumentValidator.js`
  - 内置函数静态参数检查
- `src/robotValidator.js`
  - 机器人/LTP 单行语法诊断
  - 机器人/LTP 跨行状态诊断

### 文档与生成

- `docs/诊断规则与修复动作.md`
  - 诊断 code、分类、严重度、Quick Fix / CodeAction、说明
- `scripts/generateDiagnosticDocs.js`
  - 由 `diagnosticCodes` 与 `diagnosticActions` 生成诊断文档
  - `--check` 模式比较生成内容与仓库文件，不写入磁盘
- `scripts/checkReleaseConsistency.js`
  - 常规模式检查 package 与 lockfile
  - `--tag vX.Y.Z` 模式检查 README、CHANGELOG 与发布 tag
- `scripts/checkVsixContents.js`
  - 严格校验 VSIX 必须包含与禁止包含的文件
- `scripts/reportVsixArtifact.js`
  - 输出 VSIX 字节数与 SHA-256
- `.github/workflows/ci.yml`
  - main 分支与 Pull Request 自动运行测试、文档同步检查、ESLint 和 VS Code 集成测试
  - 集成测试失败时上传 Extension Host 日志
- `docs/v2.9.0-规划.md`
  - v2.9.0 历史目标、非目标、里程碑和验收标准
- `docs/v2.10.0-发布规划.md`
  - 当前未发布能力的总体收口、风险分级、验收与发布步骤

## 已完成的 v2.9.0 Milestone

### Milestone 1: 诊断体验整合

已完成：

- README 增加诊断与 Quick Fix 用户说明。
- 诊断规则文档从纯表格扩展为按场景阅读的指南。
- `npm run docs:diagnostics` 可重新生成完整诊断规则文档。
- `npm test` 与 `npm run test:integration` 已通过。

### Milestone 2: 复杂宏程序导航

已完成，待发布：

- Workspace Symbol 列出 `%@MACRO`、N 标签及静态 G/O 程序入口。
- Reference Provider 支持静态 `G65/G66/G66.1`、`M98/M198` 调用关系。
- 无扩展名命名宏与静态字符串宏名可参与导航。
- `P#变量`、表达式、注释及普通字符串不参与静态引用解析。
- VS Code 集成测试覆盖工作区符号、数字/命名宏引用及动态目标边界。

### Milestone 3: 诊断规则维护自动化

已完成，待发布：

- 新增 `npm run docs:diagnostics:check`，不同步时退出非零状态。
- `npm test` 会先执行诊断文档同步检查。
- 生成器可作为模块导入，测试覆盖同步与陈旧内容判定。
- 新增 GitHub Actions CI，覆盖 main 分支与 Pull Request。
- release workflow 通过 `npm test` 自动继承文档同步检查。

## 下一步建议

- 按 `docs/v2.10.0-发布规划.md` 完成四项 P0 发布阻断检查。
- P0-1 已完成：真实工作区基准首次 `938 ms`、重复 `394 ms`，取消、多根与缓存失效均已验证。
- P0-2 已完成：版本一致性命令已接入 `npm test` 与 release workflow。
- P0-3 实现已完成：main、Pull Request 与 release 均配置 VS Code 集成测试和失败日志上传；待推送后确认远端运行。
- P0-4 已完成：VSIX 严格白名单为 26 个扩展文件，实际归档与 SHA-256 报告已验证。
- `v2.10.0` 版本元数据与本地自动发布验证已完成。
- 隔离 Profile 安装冒烟已通过；下一步按规划审查并拆分提交，推送后等待远端 CI，再创建 tag 和 Release。
- P0 通过后更新版本号、CHANGELOG 与 README badge。
- 执行完整发布验证，提交、推送并创建 `v2.10.0` Release。

## 发布流程

发布前固定执行：

```powershell
npm run docs:diagnostics
npm run docs:diagnostics:check
npm test
npm run test:integration
npm run package
```

然后更新：

- `package.json`
- `package-lock.json`
- `README.md` version badge
- `CHANGELOG.md`

提交并推送：

```powershell
git status --short
git add <files>
git commit -m "<message>"
git push origin main
```

创建 Release：

```powershell
gh release create vX.Y.Z .\syntec-macro-X.Y.Z.vsix --title "vX.Y.Z" --notes "<release notes>" --latest
```

确认：

```powershell
gh release list --limit 3
git status --short
git ls-remote --tags origin vX.Y.Z
```

## 注意事项

- 不要把旧版 `.vsix` 留在仓库根目录，避免误装。
- `test-demo.nc` 应保持无诊断，用作合法语法覆盖样例。
- 错误场景应放在测试内联样例中，不应长期留在 demo 文件里。
- Quick Fix 只做安全机械修复；需要用户判断语义的场景使用说明型 action。
- 动态跳转目标不做静态解析，避免误跳。

# 开发交接说明

本文档是当前工作区的唯一开发状态与运行手册，记录架构现状、接续工作、验证命令、GitHub 同步和发布操作。已完成迭代的历史细节见 [`CHANGELOG.md`](../CHANGELOG.md)；MACRO 规则结论沉淀在语法规范手册与能力矩阵。

## 文档地图

- [docs/README.md](README.md) — 全部文档的角色与阅读顺序索引。
- [新代 MACRO 语法规范手册](新代MACRO语法规范手册.md)（含 [macro-handbook/](macro-handbook/) 分册）— MACRO 规则唯一沉淀处；规则先改这里。
- [MACRO 能力矩阵](MACRO能力矩阵.md) — 能力、版本基线、插件覆盖与证据状态唯一登记点，与语法手册、诊断 parity 清单双向可追溯。
- [Rust 诊断 parity 清单](Rust诊断parity清单.md) — 稳定诊断 code 与 Rust 覆盖状态收口处（readiness 门禁依据）。
- [Rust/Wasm 切换验收门禁](Rust-Wasm切换验收门禁.md) — 永久有效的验收门禁与不变约束。
- [诊断规则与修复动作](诊断规则与修复动作.md) — 由 `npm.cmd run docs:diagnostics` 生成，不手改。
- [Release-Runbook](Release-Runbook.md) — 发布操作手册。
- 本文档 — 接续点与本机运行手册。

新增或修改诊断/规则走「四件套」（由 `npm.cmd run check:diagnostic-governance` 守卫 HARD/SOFT）：先更新语法规范手册 + 能力矩阵 → `src/diagnosticCodes.js` 注册 + Rust emit → [Rust 诊断 parity 清单](Rust诊断parity清单.md) 登记 → `src/diagnosticActions.js` 补 `DIAGNOSTIC_HELP` 文案；关键词新增仅扩 `src/data/keywords.json`（经 `src/keywords.js` thin loader 访问），不再引入 JS 分析器逻辑。

## 当前同步状态（v4.0.0 已发布）

- 主线基线分支：`main`（当前工作分支以 `git branch --show-current` 为准）
- 当前发布版本：`v4.0.0` (2026-09-22)
- GitHub Release：<https://github.com/SYNTEC-40101720/syntec-macro/releases/tag/v4.0.0>
- 主线架构：**Rust/Wasm 唯一分析后端** 已落地。原 12 个 JS 分析器模块 (analysisCore / lexer / validator / robotValidator / controlFlowValidator / functionArgumentValidator / diagnosticRules / diagnosticFactory / formatter / navigationSymbols / navigationIndex / statementClassifier) 已从仓库删除，净减约 4218 行。`syntecMacro.analysisBackend` enum 收敛为 `['rust-wasm']` 单值；`javascript` 与 `rust-wasm-shadow` 选项已退役。
- 三条主线进度：1) **JS → Rust 分析后端迁移** ✅ (v4.0.0)；2) **Host 层与 Rust analyzer 模块化边界** ✅；3) **规范化与门禁** ✅ (test/lint/check:release/readiness/js-backend-retirement/vsix 与 CI 均在 v4.0.0 跑通)。
- 历史节点（详节见 `CHANGELOG.md`）：v3.0.0 (2026-09-20) Rust/Wasm 试点进 VSIX → v3.1.0 (2026-09-21) 默认 backend 切 rust-wasm → v4.0.0 (2026-09-22) JS 后端退役，rust-wasm 唯一；更早的 v2.13~v2.15 是诊断体验整合期（Modbus-TCP 静态诊断、共享词法状态机、LTP 静态范围、导航监听缓存、语言数据一致性门禁）。
- 后续可选优化 (不阻塞主流程, 按需推进)：
  - **Phase 5 后续** 控制器实测证据的 Rust emit 入口已解锁（Phase 5.1 复核 + 5.2 struct + 5.3 emit 均已落地），剩余项按四件套随需求推进；
  - **R1.4 优化储备**: Rust 热路径再优化保留为后备用，当前 fixture 持平 / large-20k 快 26% / nav 快 18%，不主动推进。
- 本机开发约定：当前工作区是开发事实来源，GitHub 用于远端同步、备份和协作；开发或推送前执行 `git status --short`，确认未提交改动归属，不假设工作区必须 clean。
- 文档治理基线：MACRO 规则状态只在 [能力矩阵](MACRO能力矩阵.md) 登记一次；新增或修改规则时，先更新 [语法规范手册](新代MACRO语法规范手册.md) 与能力矩阵；`docs/诊断规则与修复动作.md` 是生成物，不手改。

## 本机开发与 GitHub 同步

1. 开始开发前运行 `git status --short`，确认当前分支和未提交改动。
2. 每个小批次先运行与改动匹配的验证；文档批次至少运行 `git diff --check`，代码批次至少运行 `npm.cmd test` 和 `npm.cmd run lint`。
3. 推送前确认 `git diff --check`、测试、lint 和文档同步检查通过，再检查 `git diff --stat` 与 `git status --short`。
4. 普通开发提交推送到 `main`；版本发布时才创建并推送 `vX.Y.Z` tag，发布流程仍以本文档后面的清单为准。
5. 推送后用 `git status --short` 确认本地状态，并在发布场景用 `git ls-remote --tags origin vX.Y.Z` 核对远端 tag。

## 后续计划与接续工作

v4.0.0 主线已收口，下面是接续点，所有后续任务都需要重跑 §本地验证命令。

1. **新诊断规则科学化** — 已收口防灾基线：73/73 Rust parity code 全覆盖；后续诊断增改走「规范手册 + 能力矩阵 + Rust 主体 + 单测」四件套（流程见文首文档地图），`npm.cmd run check:diagnostic-governance` 守卫 HARD/SOFT。
2. **能力回退登记** — 需求回退时先在 issue 登记原因再回策，关联到 [MACRO能力矩阵](MACRO能力矩阵.md) 能力 ID。
3. **v5.0 Major 远期** — 仅在有真实诉求时评估是否进一步精简 host 配置面与硬假设为 Rust-only。

开始新开发批次时先 `git status --short`，再读本节 + [Rust 诊断 parity 清单](Rust诊断parity清单.md) 确定与现阶段的接续点。

## 本机仓库与 GitHub 同步

### 备用恢复或重新初始化

```powershell
git clone https://github.com/SYNTEC-40101720/syntec-macro.git
cd syntec-macro
npm.cmd install
```

### 从 GitHub 更新当前工作区

```powershell
cd <你的 syntec-macro 路径>
git fetch --all --tags
git switch main
git pull --ff-only origin main
npm.cmd install
```

### 确认版本

```powershell
git log -1 --oneline
git tag --list v4.0.0
node -p "require('./package.json').version"
```

预期：

- `package.json` 当前版本为 `4.0.0`
- 本地能看到 `v4.0.0` tag
- 如开始下一版本开发，版本号保持 `4.0.0`，直到下一个发布收口阶段再更新
- 最新提交及未提交变更以 `git log -1 --oneline` 和 `git status --short` 为准

## 本地验证命令

```powershell
npm.cmd run docs:diagnostics
npm.cmd run docs:diagnostics:check
npm.cmd run check:data
npm.cmd run check:release -- --tag v4.0.0
npm.cmd test
npm.cmd run typecheck:analysis
npm.cmd run lint
npm.cmd run test:integration
npm.cmd run test:integration:navigation
npm.cmd run benchmark:analysis
npm.cmd run benchmark:parser
npm.cmd run compare:rust
npm.cmd run probe:rust:wasm
npm.cmd run benchmark:rust:wasm
npm.cmd run test:parser:tree-sitter
npm.cmd run package
npm.cmd run smoke:installed
```

> v4.0.0 起 JS analyzer 模块退役，`npm.cmd test` 已涵盖 `check:vsix` / `check:data` / `check:js-backend-retirement` / `check:rust:wasm:asset` / `typecheck:analysis`。补充脚本 `dashboardRustWasm` / `compare:rust` 需要 `$env:SYNTEC_RUST_CLI` 指向 `crates\\syntec-core\\target\\*\\debug\\syntec-core-cli.exe`.

## 当前架构入口

> v4.0.0 起 12 个 JS 分析器模块已退役 (净减 ~4218 行), 诊断/导航/格式化/补全主流程由 Rust/Wasm analyzer 产出, host provider 走 `src/hostRustAnalyzer.js` 同步路径.

### 扩展入口

- `src/extension.js`
  - VS Code provider 注册 (completion / hover / definition / references / workspace symbol / formatting / diagnostics / code-actions)
  - diagnostics collection 接管 worker 异步诊断结果
  - `setHostAnalyzerPolicy('empty')`: host 未就绪时 `getHostRustAnalyzer()` 返回 `null`, 所有 host provider 直接返回空结果, 不试图加载 JS fallback
- `src/validatorWorker.js`
  - 唯一 backend: `rust-wasm`, 通过 `src/rustWasmWorkerAdapter.js` 包装 wasm asset 加载 + analysis ABI
  - Worker 启动时加载生产 wasm asset (`assets/rust-wasm/`), 失败抛错由 `createAnalysisBackend` 回退

### Host-only helpers (Provider 层)

- `src/hostRustAnalyzer.js` — Rust 同步分析入口, 用于 host provider 即时响应
- `src/rustWasmAsset.js` — 生产 wasm asset 全链校验 (manifest/字节/SHA-256/instantiate/exports)
- `src/rustWasmWorkerAdapter.js` — Worker 端 Rust analyzer 高阶组合
- `src/diagnosticsProvider.js` / `src/completionProvider.js` / `src/hoverProvider.js` / `src/definitionProvider.js` / `src/navigationProvider.js` / `src/formattingProvider.js` — VS Code API 绑定, 把 host Rust 结果转成 VS Code 对象
- `src/pathResolver.js` — `collectNavigationIndexEntries`/`isPotentialNavigationFile`/`getProgramEntryName`/`getMacroProgramName`/`isMacroFileContent`/`extractNavigationSymbolsMetaOnly` 等 host-only 导航 helper
- `src/diagnosticCodes.js` — 稳定诊断 code 集合 (数据真源, 作 host/worker 共享)
- `src/diagnosticActions.js` — Quick Fix 替换规则、说明型 action 文案
- `src/providerShared.js` — Provider 共享缓存与开关 (`functionIndex` / `isFeatureEnabled` / 后端设置读取)

### 数据真源 (Phase R2: src/data/*.json + thin JSON loader)

- `src/data/systemVariables.json` + `src/systemVariables.js` — 系统变数编号语义 (hover)
- `src/data/functions.json` + `src/functions.js` — 内置函数表 (62 项 hover/completion)
- `src/data/hoverDocs.json` + `src/codeDocs.js` — G/M 码 hover 文档 (gCodeDocs/g10LCodeDocs/mCodeDocs 三表)
- `src/data/keywords.json` + `src/keywords.js` — 关键字分组 + keywordDocs (72 项 hover/completion)
- `src/completionSnippets.js` — `buildFunctionSnippet` 纯函数 (无数据真源, 不迁)
- `snippets/syntec-macro.json` — VSCode snippet 注册表 (已是 JSON)

### Rust/Wasm 核心

- `crates/syntec-core/src/lib.rs` — 唯一分析后端: 诊断 + 导航 + 格式化 + completion 元数据生成
- `assets/rust-wasm/{manifest.json, syntec_core.wasm}` — 生产 wasm 资产, 通过 `scripts/buildRustWasmAsset.js` 构建并 bundle 进 VSIX

### 文档与生成

- `docs/诊断规则与修复动作.md` — 诊断 code、分类、严重度、Quick Fix / CodeAction、说明
- `scripts/generateDiagnosticDocs.js` — 由 `diagnosticCodes` 与 `diagnosticActions` 生成诊断文档 (`--check` 模式比较不写入磁盘)
- `scripts/checkReleaseConsistency.js` — 校验 package 与 lockfile 一致性; `--tag vX.Y.Z` 模式校验 README/CHANGELOG 与发布 tag
- `scripts/checkVsixContents.js` — 严格校验 VSIX 必须包含与禁止包含的文件 (R2 后含 `src/data/*.json`)
- `scripts/reportVsixArtifact.js` — 输出 VSIX 字节数与 SHA-256
- `.github/workflows/ci.yml` — main 分支与 PR 自动运行测试、文档同步检查、ESLint 与 VS Code 集成测试
- `.github/workflows/rust-wasm.yml` — Rust/Wasm 跨平台构建矩阵 (Ubuntu + Windows) + smoke + perf-data 上传
- `.github/workflows/release.yml` — `--strict` readiness 门禁 + VSIX 打包 + 安装级 smoke + Release body 含 SHA-256

## 已发布里程碑

> 简要列表; 详细修订记录见 [`CHANGELOG.md`](../CHANGELOG.md)。

- **v4.0.0 (2026-09-22)** — R1.2 Stage B 收口: 12 个 JS 分析器模块删除 (净减 ~4218 行), host provider 走 hostRustAnalyzer, worker 硬切 rust-wasm, CI 全 PASS, VSIX 240510 B (SHA-256 `4d1e5331...`); Phase R2 数据表迁 `src/data/*.json` + thin loader; Phase 5.1~5.6 控制器实测复核与 Rust 跨行 emit 收口 (73/73 parity code)。
- **v3.1.0 (2026-09-21)** — 默认 backend 从 javascript 切到 rust-wasm (JS 保留 fallback 平滑过渡)。 Phase 1.5 / R1.1 准入全 PASS.
- **v3.0.0 (2026-09-20)** — Rust/Wasm 进 VSIX 作 experimental, 默认仍 javascript. CI/门禁/parity/Worker 全套就位.
- **v2.13 ~ v2.15 (2026-07 ~ 09)** — 诊断体验整成期: Modbus-TCP 静态诊断, 共享词法状态机, LTP 静态范围, 导航文件监听缓存, 语言数据一致性门禁, 发布脚本对齐.

## 发布流程

发布前固定执行：

```powershell
npm.cmd run docs:diagnostics
npm.cmd run docs:diagnostics:check
npm.cmd test
npm.cmd run test:integration
npm.cmd run package
```

然后更新：

- `package.json`
- `package-lock.json`
- `README.md` version badge
- `CHANGELOG.md`

提交并推送：

```powershell
git status --short
git add <files>
git commit -m "<message>"
git push origin main
```

创建 Release：

```powershell
# 域控环境阻止 gh CLI，使用项目脚本（git credential fill + curl.exe）
npm.cmd run release:create -- vX.Y.Z
```

确认：

```powershell
git status --short
git ls-remote --tags origin vX.Y.Z
```

## 注意事项

- 不要把旧版 `.vsix` 留在仓库根目录，避免误装。
- `tests/fixtures/test-demo.nc` 应保持无诊断，用作合法语法覆盖样例。
- 错误场景应放在测试内联样例中，不应长期留在 demo 文件里。
- Quick Fix 只做安全机械修复；需要用户判断语义的场景使用说明型 action。
- 动态跳转目标不做静态解析，避免误跳。
